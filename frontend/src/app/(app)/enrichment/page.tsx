import { EnrichmentWorkspace } from "@/features/enrichment/components/EnrichmentWorkspace";

export default function EnrichmentPage() {
  return (
    <div data-section="enrichment-workspace">
      <EnrichmentWorkspace />
    </div>
  );
}
