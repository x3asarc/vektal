# Planning Override Rules

1. Treat `.planning/ROADMAP.md` and `.planning/STATE.md` as protected governance records.
2. Do not rewrite historical phase details unless explicitly requested.
3. Additive updates are preferred over destructive edits.
4. Archive stale/failed planning artifacts under `.planning/archive/` instead of deleting.

