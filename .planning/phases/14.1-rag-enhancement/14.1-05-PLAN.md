---
phase: 14.1-rag-enhancement
plan: 05
type: execute
wave: 3
depends_on: [03,04]
files_modified:
  - src/graph/mcp_server.py
  - scripts/graph/start_mcp_server.sh
  - .claude/settings.local.json
  - requirements.txt
  - tests/unit/test_mcp_server_contract.py
autonomous: true
must_haves:
    truths:
      - Claude-facing MCP server exposes query_graph, get_dependencies, and retrieve_intent tools
      - MCP server runs on stdio transport and validates tool inputs
      - Tool responses include source and duration metadata for observability
      - Server degrades safely when graph services are unavailable
    artifacts:
      - path: src/graph/mcp_server.py
        provides: MCP stdio server with three graph tools
        exports: run_server,query_graph_tool,get_dependencies_tool,retrieve_intent_tool
      - path: scripts/graph/start_mcp_server.sh
        provides: Startup wrapper with env/bootstrap checks
      - path: .claude/settings.local.json
        provides: Local MCP server registration for development sessions
      - path: requirements.txt
        provides: MCP SDK dependency pin
    key_links:
      - from: src/graph/mcp_server.py
        to: src/graph/query_interface.py
        via: query_graph tool routing
        pattern: query_graph
      - from: src/graph/mcp_server.py
        to: src/graph/query_templates.py
        via: get_dependencies template execution
        pattern: imports
      - from: .claude/settings.local.json
        to: src/graph/mcp_server.py
        via: local MCP launch command
        pattern: mcp
---

<objective>
Implement the MCP tool server from Phase 14.1 plan item 05.

Purpose: expose knowledge graph capabilities to Claude Code without python imports or shell commands.
Output: project-local MCP server with three tools and robust startup/degrade behavior.
</objective>

<execution_context>
@.planning/phases/14.1-rag-enhancement/14.1-PLAN.md
@.planning/phases/14.1-rag-enhancement/14.1-CROSS-REFERENCE.md
@.planning/phases/14.1-rag-enhancement/14.1-RESEARCH.md
</execution_context>

<context>
@src/graph/query_interface.py
@src/graph/query_templates.py
@src/graph/intent_capture.py
@.claude/settings.local.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build MCP stdio server with three tools</name>
  <files>src/graph/mcp_server.py</files>
  <action>
Implement MCP server over stdio transport using the official Python MCP SDK.
Define tools:
1) query_graph(query: str)
2) get_dependencies(file_path: str, direction: imports|imported_by|both, depth: int)
3) retrieve_intent(query: str)
Route each tool through existing query/service modules to preserve cache/bridge/guardrail behavior.
  </action>
  <verify>python -c "import src.graph.mcp_server as s; print('mcp server module import ok')"</verify>
  <done>MCP server defines and registers all three required tools with validated schemas.</done>
</task>

<task type="auto">
  <name>Task 2: Wire runtime config and dependency</name>
  <files>requirements.txt, .claude/settings.local.json</files>
  <action>
Add MCP SDK dependency pin to requirements.
Add/extend local Claude settings to register the synthex knowledge-graph MCP server command.
Ensure config remains local-safe and does not break existing allowed permissions.
  </action>
  <verify>rg -n "mcp|synthex-knowledge-graph|python src/graph/mcp_server.py" .claude/settings.local.json requirements.txt -S</verify>
  <done>Dependency and local MCP registration are both present and discoverable.</done>
</task>

<task type="auto">
  <name>Task 3: Add startup wrapper and contract tests</name>
  <files>scripts/graph/start_mcp_server.sh, tests/unit/test_mcp_server_contract.py</files>
  <action>
Create startup script that checks Neo4j availability and starts server with safe fallback behavior.
Add lightweight contract tests for tool presence, input validation, and graceful no-graph behavior.
  </action>
  <verify>bash scripts/graph/start_mcp_server.sh --help || true</verify>
  <done>MCP server startup path is scripted and tool contracts are test-covered.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/unit/test_mcp_server_contract.py -q --tb=short`
2. Server module imports successfully with MCP SDK installed.
3. All three tools are listed by server introspection.
4. No-graph mode returns controlled fallback responses.
</verification>

<success_criteria>
- RAG-01 MCP tool access implemented
- Tool contracts map to 14.1 requirements and existing graph query modules
- Startup/degrade behavior is explicit and testable
</success_criteria>

<output>
After completion, create `.planning/phases/14.1-rag-enhancement/14.1-05-SUMMARY.md`
</output>

