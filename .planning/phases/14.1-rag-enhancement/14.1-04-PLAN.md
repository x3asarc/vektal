---
phase: 14.1-rag-enhancement
plan: 04
type: execute
wave: 2
depends_on: [02]
files_modified:
  - src/graph/semantic_cache.py
  - src/graph/query_interface.py
  - src/graph/incremental_sync.py
  - src/core/embeddings.py
  - tests/unit/test_semantic_cache.py
autonomous: true
must_haves:
    truths:
      - Query pipeline checks semantic cache before graph traversal
      - Cache hit threshold defaults to cosine similarity >= 0.92
      - Cache supports TTL + max-entry eviction with deterministic behavior
      - Changed files invalidate cache entries that reference those paths
    artifacts:
      - path: src/graph/semantic_cache.py
        provides: SemanticCache and CacheConfig implementation
        exports:
          - SemanticCache
          - CacheConfig
      - path: src/graph/query_interface.py
        provides: Cache-first query routing with source metadata
      - path: src/graph/incremental_sync.py
        provides: File-change driven cache invalidation hook
      - path: tests/unit/test_semantic_cache.py
        provides: Similarity threshold, eviction, and invalidation tests
    key_links:
      - from: src/graph/query_interface.py
        to: src/graph/semantic_cache.py
        via: cache lookup and store flow
        pattern: semantic_cache
      - from: src/graph/incremental_sync.py
        to: src/graph/semantic_cache.py
        via: invalidate changed referenced paths
        pattern: invalidate
---

<objective>
Implement the semantic cache layer from Phase 14.1 plan item 04.

Purpose: cut redundant graph work for repeated/near-identical questions.
Output: in-memory semantic cache with threshold, TTL, LRU behavior, and invalidation hooks.
</objective>

<execution_context>
@.planning/phases/14.1-rag-enhancement/14.1-PLAN.md
@.planning/phases/14.1-rag-enhancement/14.1-CROSS-REFERENCE.md
@.planning/phases/14.1-rag-enhancement/14.1-RESEARCH.md
</execution_context>

<context>
@src/graph/query_interface.py
@src/graph/incremental_sync.py
@src/core/embeddings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build semantic cache core</name>
  <files>src/graph/semantic_cache.py</files>
  <action>
Implement CacheConfig and SemanticCache with:
similarity_threshold=0.92, max_entries=1000, ttl_seconds=3600.
Use numpy cosine similarity on normalized vectors.
Track metrics: hit_count, miss_count, average hit/miss latency.
Support store(), lookup(), invalidate(), and get_cache_stats().
  </action>
  <verify>python -c "from src.graph.semantic_cache import CacheConfig; print(CacheConfig().similarity_threshold)"</verify>
  <done>Cache module supports thresholded lookup and deterministic eviction behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate cache into query flow</name>
  <files>src/graph/query_interface.py</files>
  <action>
Add cache lookup at the top of query pipeline.
On cache hit, return source=semantic_cache and avoid graph query execution.
On miss, execute query path and store result with referenced_paths metadata.
Ensure trace metadata includes was_cache_hit flag.
  </action>
  <verify>python -c "from src.graph.query_interface import QueryResult; print(QueryResult().query_type)"</verify>
  <done>Query pipeline is cache-first and returns explicit source metadata.</done>
</task>

<task type="auto">
  <name>Task 3: Wire invalidation and add tests</name>
  <files>src/graph/incremental_sync.py, tests/unit/test_semantic_cache.py</files>
  <action>
Call cache.invalidate(changed_files) from incremental sync flow after file changes are detected.
Add tests for threshold boundary behavior (0.91 miss, 0.93 hit), TTL eviction, LRU eviction, and file-path invalidation.
  </action>
  <verify>python -m pytest tests/unit/test_semantic_cache.py -q --tb=short</verify>
  <done>Cache behavior and invalidation are test-covered and deterministic.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/unit/test_semantic_cache.py -q --tb=short`
2. Cache hit path returns source `semantic_cache`.
3. Threshold behavior around 0.92 is verified by tests.
4. File change invalidation removes affected entries.
</verification>

<success_criteria>
- RAG-04 semantic caching implemented with threshold and invalidation
- Cache is integrated before traversal
- Stats and source metadata support operational verification
</success_criteria>

<output>
After completion, create `.planning/phases/14.1-rag-enhancement/14.1-04-SUMMARY.md`
</output>
