---
phase: 14.1-rag-enhancement
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/graph/search_expand_bridge.py
  - src/graph/query_interface.py
  - src/graph/query_templates.py
  - src/core/embeddings.py
  - tests/unit/test_search_expand_bridge.py
autonomous: true
must_haves:
    truths:
      - Retrieval uses two phases: vector seed then bounded graph expansion
      - Initial seed set is capped at 5 nodes
      - Expansion depth never exceeds 2 hops and follows allowed relationships only
      - Context assembly enforces token budget and reports truncation
    artifacts:
      - path: src/graph/search_expand_bridge.py
        provides: BridgeConfig, BridgeResult, and search_then_expand orchestration
        exports: BridgeConfig,BridgeResult,search_then_expand
      - path: src/graph/query_interface.py
        provides: query_with_bridge entry path for natural-language fallback
      - path: tests/unit/test_search_expand_bridge.py
        provides: Bridge constraints, dedupe, and token-budget tests
    key_links:
      - from: src/graph/search_expand_bridge.py
        to: src/core/embeddings.py
        via: query embedding and vector similarity seed
        pattern: similarity_search
      - from: src/graph/query_interface.py
        to: src/graph/search_expand_bridge.py
        via: bridge dispatch on non-template queries
        pattern: query_with_bridge
---

<objective>
Implement the search-then-expand bridge from Phase 14.1 plan item 03.

Purpose: combine semantic relevance and structural coverage in one bounded retrieval path.
Output: bridge module with strict limits and query interface integration.
</objective>

<execution_context>
@.planning/phases/14.1-rag-enhancement/14.1-PLAN.md
@.planning/phases/14.1-rag-enhancement/14.1-CROSS-REFERENCE.md
@.planning/phases/14.1-rag-enhancement/14.1-RESEARCH.md
</execution_context>

<context>
@src/core/embeddings.py
@src/graph/query_interface.py
@src/graph/query_templates.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement bridge config/result contracts and orchestration</name>
  <files>src/graph/search_expand_bridge.py</files>
  <action>
Create BridgeConfig and BridgeResult dataclasses with defaults:
max_initial_nodes=5, max_traversal_depth=2, max_context_tokens=8192.
Implement search_then_expand() to run vector seed, bounded graph traversal, dedupe, and truncation signaling.
Support relationship allowlist defaulting to IMPORTS/CALLS/IMPLEMENTS/EXPLAINS.
  </action>
  <verify>python -c "from src.graph.search_expand_bridge import BridgeConfig, BridgeResult; print(BridgeConfig().max_initial_nodes)"</verify>
  <done>Bridge module exists and exposes deterministic bounded retrieval contract.</done>
</task>

<task type="auto">
  <name>Task 2: Wire bridge path into query interface</name>
  <files>src/graph/query_interface.py, src/graph/query_templates.py</files>
  <action>
Add query_with_bridge() path and dispatch logic for non-template natural language queries.
Return source metadata indicating bridge path and include relationships followed + truncation indicators.
Ensure existing template queries remain unchanged and backward compatible.
  </action>
  <verify>python -c "from src.graph.query_interface import match_query_to_template; print(match_query_to_template('impact radius of src/core/embeddings.py') is not None)"</verify>
  <done>Query interface supports bridge retrieval without regressing template matching.</done>
</task>

<task type="auto">
  <name>Task 3: Add bridge constraint tests</name>
  <files>tests/unit/test_search_expand_bridge.py</files>
  <action>
Add tests for max_initial_nodes, depth cap, dedupe correctness, and token-budget truncation.
Include one test that verifies relationship allowlist filtering excludes non-allowed edge types.
  </action>
  <verify>python -m pytest tests/unit/test_search_expand_bridge.py -q --tb=short</verify>
  <done>Bridge behavior is covered by unit tests for all bounded constraints.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/unit/test_search_expand_bridge.py -q --tb=short`
2. Bridge initial node count never exceeds 5.
3. Traversal depth cap stays at 2.
4. Token budgeting reports truncation when budget exceeded.
</verification>

<success_criteria>
- RAG-03 search-then-expand implemented as a bounded two-stage retrieval path
- Bridge returns semantically seeded and structurally expanded context
- Constraint behavior is test-verified
</success_criteria>

<output>
After completion, create `.planning/phases/14.1-rag-enhancement/14.1-03-SUMMARY.md`
</output>

