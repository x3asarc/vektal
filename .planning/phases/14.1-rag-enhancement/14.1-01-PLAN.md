---
phase: 14.1-rag-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/synthex_entities.py
  - src/core/codebase_schema.py
  - src/core/codebase_entities.py
  - src/tasks/graphiti_sync.py
  - scripts/graph/seed_memory_nodes.py
  - tests/core/test_synthex_entities.py
autonomous: true
must_haves:
    truths:
      - Decision, Convention, and BugRootCause memory entities are modeled and importable
      - EXPLAINS, RESOLVED_BY, and SUPERCEDES edges are modeled and validated
      - Neo4j schema includes constraints and indexes for new memory nodes
      - Seeder can dry-run and report proposed Convention/Decision inserts
    artifacts:
      - path: src/core/synthex_entities.py
        provides: Memory entities, memory edges, and new EpisodeType values
        exports:
          - DecisionEntity
          - ConventionEntity
          - BugRootCauseEntity
          - ExplainsEdge
          - ResolvedByEdge
          - SupersedesEdge
      - path: src/core/codebase_schema.py
        provides: Constraint/index definitions for memory entities
      - path: scripts/graph/seed_memory_nodes.py
        provides: Idempotent memory node seeding entrypoint
      - path: src/tasks/graphiti_sync.py
        provides: Episode ID support for new memory episode types
    key_links:
      - from: scripts/graph/seed_memory_nodes.py
        to: src/core/graphiti_client.py
        via: graph client initialization
        pattern: get_graphiti_client
      - from: src/tasks/graphiti_sync.py
        to: src/core/synthex_entities.py
        via: EpisodeType enum extension
        pattern: EpisodeType
---

<objective>
Implement the memory engineering foundation from Phase 14.1 plan item 01.

Purpose: enable durable architectural memory (Decision, Convention, BugRootCause) that later bridge/cache/MCP features can retrieve.
Output: typed memory entities + edges, schema support, and a one-time seeding script.
</objective>

<execution_context>
@.planning/phases/14.1-rag-enhancement/14.1-PLAN.md
@.planning/phases/14.1-rag-enhancement/14.1-CROSS-REFERENCE.md
@.planning/phases/14.1-rag-enhancement/14.1-RESEARCH.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/core/synthex_entities.py
@src/core/codebase_schema.py
@src/tasks/graphiti_sync.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend synthex entities with memory node and edge families</name>
  <files>src/core/synthex_entities.py, src/core/codebase_entities.py, src/tasks/graphiti_sync.py</files>
  <action>
Add entity contracts for DecisionEntity, ConventionEntity, and BugRootCauseEntity.
Add edge contracts for ExplainsEdge, ResolvedByEdge, and SupersedesEdge.
Extend EpisodeType with decision/convention/bug-root-cause events while preserving existing enum values.
Update create_episode_payload docs and task ID generation branches so new episode types produce stable IDs.
  </action>
  <verify>python -c "from src.core.synthex_entities import DecisionEntity, ConventionEntity, BugRootCauseEntity, ExplainsEdge, ResolvedByEdge, SupersedesEdge; print('memory entities/edges importable')"</verify>
  <done>All memory entities/edges are defined with pydantic validation and import cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Extend codebase schema for memory entity indexing</name>
  <files>src/core/codebase_schema.py</files>
  <action>
Add unique constraints/indexes for Decision, Convention, BugRootCause, and Conversation nodes.
Add targeted indexes for Convention.scope and Decision.status.
Keep schema initialization idempotent and fail-open on unsupported Neo4j constraint types.
  </action>
  <verify>python -c "from src.core.codebase_schema import CODEBASE_INDEXES, CODEBASE_CONSTRAINTS; print(len(CODEBASE_INDEXES), len(CODEBASE_CONSTRAINTS))"</verify>
  <done>Schema declares all new memory constraints/indexes without removing existing phase 14 indexes.</done>
</task>

<task type="auto">
  <name>Task 3: Create idempotent memory seeder with dry-run mode</name>
  <files>scripts/graph/seed_memory_nodes.py, tests/core/test_synthex_entities.py</files>
  <action>
Create a CLI seeder that parses governance/standards sources and proposes Convention/Decision nodes.
Implement --dry-run to print operations without mutating graph.
Add/extend tests for new entity contracts and payload compatibility.
  </action>
  <verify>python scripts/graph/seed_memory_nodes.py --dry-run</verify>
  <done>Seeder executes in dry-run mode and test suite includes memory contract checks.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/core/test_synthex_entities.py -q --tb=short`
2. `python scripts/graph/seed_memory_nodes.py --dry-run`
3. New EpisodeType values are accepted by `src/tasks/graphiti_sync.py` without ValueError.
</verification>

<success_criteria>
- Memory entity types and edges from RAG-02 are modeled and testable
- Schema supports new memory families
- Seeder is safe to run repeatedly with dry-run visibility
</success_criteria>

<output>
After completion, create `.planning/phases/14.1-rag-enhancement/14.1-01-SUMMARY.md`
</output>
