---
phase: 14.1-rag-enhancement
plan: 06
type: execute
wave: 3
depends_on: [01,02,03,05]
files_modified:
  - src/graph/mcp_server.py
  - src/graph/convention_checker.py
  - src/graph/refactor_guard.py
  - src/graph/query_interface.py
  - tests/unit/test_convention_checker.py
  - tests/unit/test_refactor_guard.py
autonomous: true
must_haves:
    truths:
      - Session startup loads top conventions for architectural context
      - Convention guardrails run before architectural suggestions are returned
      - Refactor guard evaluates CALLS relationships and returns risk level
      - Intent retrieval includes convention context for architectural questions
    artifacts:
      - path: src/graph/convention_checker.py
        provides: Convention conflict detection model and checker API
        exports:
          - check_against_conventions
          - ConventionViolation
      - path: src/graph/refactor_guard.py
        provides: CALLS-based refactor risk report API
        exports:
          - verify_refactor_safety
          - RefactorSafetyReport
      - path: src/graph/mcp_server.py
        provides: Session init hook and convention-aware intent responses
      - path: src/graph/query_interface.py
        provides: On-query hook ordering cache -> conventions -> result metadata
    key_links:
      - from: src/graph/mcp_server.py
        to: src/graph/convention_checker.py
        via: convention validation in intent retrieval path
        pattern: check_against_conventions
      - from: src/graph/refactor_guard.py
        to: src/graph/query_templates.py
        via: function_callers/function_callees template usage
        pattern: function_callers
      - from: src/graph/query_interface.py
        to: src/graph/semantic_cache.py
        via: required cache-first hook before convention checks
        pattern: semantic_cache
---

<objective>
Implement lifecycle hooks and convention guardrails from Phase 14.1 plan item 06.

Purpose: make graph intelligence proactive across session lifecycle and refactor safety checks.
Output: session init conventions, convention checker, refactor guard, and query hook wiring.
</objective>

<execution_context>
@.planning/phases/14.1-rag-enhancement/14.1-PLAN.md
@.planning/phases/14.1-rag-enhancement/14.1-CROSS-REFERENCE.md
@.planning/phases/14.1-rag-enhancement/14.1-RESEARCH.md
</execution_context>

<context>
@src/graph/mcp_server.py
@src/graph/query_interface.py
@src/graph/query_templates.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement convention checker module</name>
  <files>src/graph/convention_checker.py, tests/unit/test_convention_checker.py</files>
  <action>
Create checker API that compares proposed change text against active Convention nodes and returns potential violations with confidence and suggested alternatives.
Default to advisory flags first (no hard-stop) and keep thresholds configurable.
Add tests for conflict detection and no-conflict cases.
  </action>
  <verify>python -m pytest tests/unit/test_convention_checker.py -q --tb=short</verify>
  <done>Convention checker API exists, returns structured violations, and passes baseline tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement CALLS-based refactor guard</name>
  <files>src/graph/refactor_guard.py, tests/unit/test_refactor_guard.py</files>
  <action>
Create verify_refactor_safety(file_path) that inspects callers/callees and outputs downstream impact plus risk level.
Apply thresholds: low (0-2), medium (3-10), high (11+).
Add tests for each risk band and empty graph behavior.
  </action>
  <verify>python -m pytest tests/unit/test_refactor_guard.py -q --tb=short</verify>
  <done>Refactor guard returns deterministic risk reports based on CALLS topology.</done>
</task>

<task type="auto">
  <name>Task 3: Wire lifecycle hooks in MCP + query interface</name>
  <files>src/graph/mcp_server.py, src/graph/query_interface.py</files>
  <action>
Add session initialization behavior that loads top Convention nodes once per server/session.
Update query flow to preserve hook ordering: semantic cache first, then convention checking for architectural suggestions.
Include conventions_checked metadata in results and include conventions in retrieve_intent output.
  </action>
  <verify>python -c "from src.graph.query_interface import QueryResult; q=QueryResult(); print(hasattr(q,'data'))"</verify>
  <done>Lifecycle and guardrail behavior is wired into both MCP and query interface paths.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/unit/test_convention_checker.py tests/unit/test_refactor_guard.py -q --tb=short`
2. MCP startup path returns top conventions in initial context metadata.
3. Query hook order honors cache-first convention.
4. retrieve_intent output includes convention context.
</verification>

<success_criteria>
- RAG-05 session lifecycle hooks implemented
- RAG-07 convention guardrails implemented
- CALLS risk checks are available for refactor safety decisions
</success_criteria>

<output>
After completion, create `.planning/phases/14.1-rag-enhancement/14.1-06-SUMMARY.md`
</output>
