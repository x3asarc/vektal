---
phase: 14.1-rag-enhancement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/synthex_entities.py
  - src/graph/query_interface.py
  - src/core/graphiti_client.py
  - src/graph/query_templates.py
  - src/tasks/graphiti_sync.py
  - tests/core/test_graphiti_client_contract.py
  - tests/tasks/test_graphiti_sync_contract.py
autonomous: true
must_haves:
    truths:
      - Successful graph queries emit reasoning trace episodes with query metadata
      - Null graph results that have filesystem hits are marked as discrepancies
      - Discrepancy events are queryable via a dedicated template
      - Fallback responses include explicit source metadata when graph misses
    artifacts:
      - path: src/core/synthex_entities.py
        provides: ReasoningTrace entity and GRAPH_DISCREPANCY episode type
      - path: src/graph/query_interface.py
        provides: Reasoning trace emission for query paths
      - path: src/core/graphiti_client.py
        provides: Discrepancy-aware fallback behavior
      - path: src/graph/query_templates.py
        provides: recent_discrepancies template
    key_links:
      - from: src/graph/query_interface.py
        to: src/tasks/graphiti_sync.py
        via: emit_episode dispatch
        pattern: emit_episode.delay
      - from: src/core/graphiti_client.py
        to: src/graph/query_templates.py
        via: discrepancy metadata consumption
        pattern: recent_discrepancies
---

<objective>
Implement reasoning provenance and discrepancy tracking from Phase 14.1 plan item 02.

Purpose: make query behavior observable and prevent silent graph staleness.
Output: ReasoningTrace emission + discrepancy detection + discrepancy query template.
</objective>

<execution_context>
@.planning/phases/14.1-rag-enhancement/14.1-PLAN.md
@.planning/phases/14.1-rag-enhancement/14.1-CROSS-REFERENCE.md
@.planning/phases/14.1-rag-enhancement/14.1-RESEARCH.md
</execution_context>

<context>
@src/graph/query_interface.py
@src/core/graphiti_client.py
@src/tasks/graphiti_sync.py
@tests/tasks/test_graphiti_sync_contract.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ReasoningTrace and discrepancy episode contracts</name>
  <files>src/core/synthex_entities.py, src/tasks/graphiti_sync.py</files>
  <action>
Add ReasoningTrace entity contract fields for query text, cypher/template, summary, duration, cache-hit flag.
Add GRAPH_DISCREPANCY and QUERY_REASONING_TRACE episode types.
Update deterministic episode ID generation branching for new types in graphiti_sync.
  </action>
  <verify>python -c "from src.core.synthex_entities import EpisodeType; print(EpisodeType.GRAPH_DISCREPANCY.value)"</verify>
  <done>New episode types are valid enum members and compatible with emission pipeline.</done>
</task>

<task type="auto">
  <name>Task 2: Emit reasoning traces from query interface</name>
  <files>src/graph/query_interface.py</files>
  <action>
Instrument successful query execution paths to emit QUERY_REASONING_TRACE episodes asynchronously.
Include query text, template/cypher info, result summary, duration, and cache metadata.
Ensure fail-open semantics: trace emission failure must not fail query response.
  </action>
  <verify>python -c "from src.graph.query_interface import query_graph; r=query_graph('imports'); print(hasattr(r,'duration_ms'))"</verify>
  <done>Query interface emits trace payloads without changing success/error behavior of existing callers.</done>
</task>

<task type="auto">
  <name>Task 3: Add discrepancy-aware fallback and dashboard template</name>
  <files>src/core/graphiti_client.py, src/graph/query_templates.py, tests/core/test_graphiti_client_contract.py</files>
  <action>
Extend fallback flow so null graph results with positive filesystem fallback emit GRAPH_DISCREPANCY episodes and annotate response source.
Add recent_discrepancies query template for operational inspection.
Add tests for timeout/error fallback behavior with discrepancy metadata path.
  </action>
  <verify>python -c "from src.graph.query_templates import QUERY_TEMPLATES; print('recent_discrepancies' in QUERY_TEMPLATES)"</verify>
  <done>Discrepancy events become observable and queryable without breaking fail-open guarantees.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/core/test_graphiti_client_contract.py tests/tasks/test_graphiti_sync_contract.py -q --tb=short`
2. Simulated null-graph/has-filesystem path returns source `filesystem_fallback`.
3. `recent_discrepancies` template exists and is callable.
</verification>

<success_criteria>
- RAG-06 discrepancy flagging is implemented
- RAG-08 reasoning provenance is captured
- Existing graph fail-open behavior remains intact
</success_criteria>

<output>
After completion, create `.planning/phases/14.1-rag-enhancement/14.1-02-SUMMARY.md`
</output>

