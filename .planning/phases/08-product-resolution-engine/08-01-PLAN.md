---
phase: 08-product-resolution-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/versions/<revision_id>_phase8_resolution_foundation.py
  - src/models/resolution_batch.py
  - src/models/resolution_rule.py
  - src/models/resolution_snapshot.py
  - src/models/recovery_log.py
  - src/models/__init__.py
  - src/resolution/contracts.py
  - src/resolution/policy.py
  - src/resolution/locks.py
  - src/api/v1/resolution/__init__.py
  - src/api/v1/resolution/schemas.py
  - src/api/v1/resolution/routes.py
  - src/api/__init__.py
  - tests/resolution/test_policy.py
  - tests/api/test_resolution_rules.py
autonomous: true

must_haves:
  truths:
    - "Phase 8 stores dry-run/apply governance state durably (batches, field changes, rules, snapshots, recovery logs)"
    - "Policy engine enforces locked context: source order, supplier verification gate, consent scopes, and exclusion precedence"
    - "Batch checkout locking prevents concurrent approval edits from multiple users"
  artifacts:
    - path: "src/models/resolution_batch.py"
      provides: "Batch/item/change/approval persistence primitives"
      contains: "class ResolutionBatch"
    - path: "src/resolution/policy.py"
      provides: "Deterministic policy evaluator"
      contains: "evaluate_change_policy"
    - path: "src/resolution/locks.py"
      provides: "Lease + heartbeat checkout lock operations"
      contains: "acquire_batch_lock"
  key_links:
    - from: "src/resolution/policy.py"
      to: "src/models/resolution_rule.py"
      via: "rule evaluation and exclusion precedence"
      pattern: "rule_scope"
    - from: "src/resolution/locks.py"
      to: "src/models/resolution_batch.py"
      via: "single-owner checkout lock lease"
      pattern: "lock_expires_at"
---

<objective>
Implement Phase 8 persistence and policy foundations first.

Purpose: establish durable contracts and guardrails before resolver/apply complexity.
Output: migration-backed models, deterministic policy engine, lock semantics, and v1 resolution rules API surface.
</objective>

<execution_context>
@codexclaude/get-shit-done/workflows/execute-plan.md
@codexclaude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-product-resolution-engine/08-CONTEXT.md
@.planning/phases/08-product-resolution-engine/08-RESEARCH.md
@src/models/product.py
@src/models/vendor.py
@src/models/shopify.py
@src/api/v1/jobs/routes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 8 resolution data model and migration</name>
  <files>
    - migrations/versions/<revision_id>_phase8_resolution_foundation.py
    - src/models/resolution_batch.py
    - src/models/resolution_rule.py
    - src/models/resolution_snapshot.py
    - src/models/recovery_log.py
    - src/models/__init__.py
  </files>
  <action>
Add migration + SQLAlchemy models for:
- `resolution_batches` (mode, status, scheduling, lock metadata, actor attribution)
- `resolution_items` (product-level unit of review/apply)
- `resolution_changes` (field-level proposals, reasons, confidence, approval status)
- `resolution_rules` (supplier+field-group scope, exclusion/negative rules, expiry, consent flags)
- `resolution_snapshots` (batch manifest + per-product pre-change state)
- `recovery_logs` (stale/deleted target payload preservation + recovery metadata)

Enforce invariants:
- single active editor lock per batch (`lock_owner_user_id`, lease expiry fields)
- exclusion rules precedence representable at schema level
- field-group enum constraints for (`images`, `text`, `pricing`, `ids`)
- audit attribution columns on manual decisions (`created_by_user_id`, `approved_by_user_id`)
  </action>
  <verify>
- `flask db upgrade` succeeds
- New tables and indexes are present
- `python -m pytest -q tests/models`
  </verify>
  <done>
- Resolution governance state is migration-backed and queryable
- Schema supports snapshots + recovery logs before any apply mutation
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement deterministic policy and lock services</name>
  <files>
    - src/resolution/contracts.py
    - src/resolution/policy.py
    - src/resolution/locks.py
    - tests/resolution/test_policy.py
  </files>
  <action>
Create domain contracts and services:
- normalized query/change contracts (`contracts.py`)
- policy evaluator (`policy.py`) implementing:
  - source trust order Shopify -> supplier -> web
  - supplier verification requirement before web
  - explicit approval required when no consented rule exists
  - exclusion rules overriding positive auto rules
  - variant structural conflict handling defaults
- checkout locking service (`locks.py`) with lease + heartbeat + reclaim behavior

Locking semantics:
- owner can edit/approve
- non-owners are read-only
- stale lock reclaim path without losing prior decision history
  </action>
  <verify>
- `python -m pytest -q tests/resolution/test_policy.py`
- Tests cover: exclusion precedence, unverified supplier web-block, missing-rule explicit approval, lock acquire/release/expiry
  </verify>
  <done>
- Locked context decisions are codified in reusable service contracts
- Concurrency safety baseline exists for multi-user dry-run review
  </done>
</task>

<task type="auto">
  <name>Task 3: Add resolution rules/settings API endpoints</name>
  <files>
    - src/api/v1/resolution/__init__.py
    - src/api/v1/resolution/schemas.py
    - src/api/v1/resolution/routes.py
    - src/api/__init__.py
    - tests/api/test_resolution_rules.py
  </files>
  <action>
Expose authenticated v1 endpoints for:
- supplier strategy quiz defaults (profile-level)
- CRUD for supplier + field-group rules
- CRUD for exclusion rules
- lock status/read-only state checks for batch review pages

Response contracts must use RFC 7807-compatible errors and include actionable metadata (`rule_scope`, `blocked_by_exclusion`, `lock_owner`).
Preserve user ownership boundaries for all reads/writes.
  </action>
  <verify>
- `python -m pytest -q tests/api/test_resolution_rules.py`
- Endpoints return 401/403/404/409 correctly for auth/ownership/lock conflicts
  </verify>
  <done>
- Settings/rules API exists for downstream resolver and frontend integration
- Rule scope + exclusion behavior is externally manageable
  </done>
</task>

</tasks>

<verification>
- [ ] Migration is reversible (upgrade/downgrade) without orphaning Phase 8 tables
- [ ] Policy tests prove all locked context rule precedence paths
- [ ] Lock conflicts return deterministic 409 responses with lock-owner context
- [ ] No regressions in existing auth/jobs/product endpoints
</verification>

<success_criteria>
- Phase 8 persistence and policy contracts are implemented and test-backed
- Multi-user dry-run edit collision is prevented by design
- Rule/exclusion management is ready for resolver and UI plans
</success_criteria>

<output>
After completion, create `.planning/phases/08-product-resolution-engine/08-01-SUMMARY.md`
</output>
