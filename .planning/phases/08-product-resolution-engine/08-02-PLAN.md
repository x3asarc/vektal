---
phase: 08-product-resolution-engine
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/resolution/normalize.py
  - src/resolution/adapters/shopify_adapter.py
  - src/resolution/adapters/supplier_adapter.py
  - src/resolution/adapters/web_adapter.py
  - src/resolution/scoring.py
  - src/resolution/structural.py
  - src/resolution/dry_run_compiler.py
  - src/resolution/lineage.py
  - src/api/v1/resolution/routes.py
  - src/api/v1/resolution/schemas.py
  - tests/resolution/test_resolution_pipeline.py
  - tests/api/test_resolution_dry_run.py
autonomous: true

must_haves:
  truths:
    - "Resolver searches sources in locked priority order with supplier verification gating for web"
    - "Dry-run previews persist field-level diffs, reason sentences, confidence factors, and structural conflicts"
    - "Variant mismatches and missing products are represented explicitly as structural states, not silent auto-mutations"
  artifacts:
    - path: "src/resolution/dry_run_compiler.py"
      provides: "Product-grouped dry-run materialization"
      contains: "compile_dry_run"
    - path: "src/resolution/structural.py"
      provides: "Variant/options conflict classification"
      contains: "class StructuralConflictType"
    - path: "src/resolution/scoring.py"
      provides: "Confidence scoring with reason factors"
      contains: "score_candidate"
  key_links:
    - from: "src/resolution/adapters/web_adapter.py"
      to: "src/resolution/policy.py"
      via: "web eligibility gate by supplier verification"
      pattern: "web_allowed"
    - from: "src/resolution/dry_run_compiler.py"
      to: "src/models/resolution_snapshot.py"
      via: "persist batch manifest + per-product snapshots"
      pattern: "snapshot_type"
---

<objective>
Implement core multi-source resolution and dry-run compilation for Phase 8.

Purpose: move from policy scaffolding to real candidate retrieval, conflict detection, and persisted preview output.
Output: resolver pipeline services and API endpoints for dry-run generation/retrieval.
</objective>

<execution_context>
@codexclaude/get-shit-done/workflows/execute-plan.md
@codexclaude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-product-resolution-engine/08-CONTEXT.md
@.planning/phases/08-product-resolution-engine/08-RESEARCH.md
@.planning/phases/08-product-resolution-engine/08-01-SUMMARY.md
@src/models/product.py
@src/models/vendor.py
@src/tasks/scrape_jobs.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build source adapters and normalized candidate retrieval</name>
  <files>
    - src/resolution/normalize.py
    - src/resolution/adapters/shopify_adapter.py
    - src/resolution/adapters/supplier_adapter.py
    - src/resolution/adapters/web_adapter.py
    - src/resolution/scoring.py
    - tests/resolution/test_resolution_pipeline.py
  </files>
  <action>
Implement normalized query builder and source adapters:
- Shopify adapter: search by SKU/EAN/barcode/title against user store catalog
- Supplier adapter: search parsed vendor catalog rows
- Web adapter: only callable when policy marks supplier verified and web allowed

Add candidate scoring model with:
- confidence score (numeric)
- reason factors (SKU match, barcode match, title/token similarity, supplier trust, variant option alignment)
- explainable reason sentence for each proposed field change

Strictly enforce locked source priority; do not allow medium-confidence auto-resolve by default.
  </action>
  <verify>
- `python -m pytest -q tests/resolution/test_resolution_pipeline.py`
- Tests prove source-order short-circuiting and supplier verification gating
  </verify>
  <done>
- RESOLVE-01/02/03/04 base retrieval behaviors implemented
- Candidate scoring is deterministic and explainable
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement structural conflict detection and dry-run compiler</name>
  <files>
    - src/resolution/structural.py
    - src/resolution/dry_run_compiler.py
    - src/resolution/lineage.py
    - tests/resolution/test_resolution_pipeline.py
  </files>
  <action>
Implement structural detection for:
- missing Shopify product -> `Draft New Product` flow marker
- variant mismatch (e.g., Shopify `Red`, supplier `Red+Blue`) -> `Structural Conflict: New Variants Detected`
- product type/option-schema mismatch -> `Structural Review`

Build dry-run compiler that:
- groups by product for review
- emits field-level proposed changes with status:
  - auto-applied-by-rule
  - awaiting-approval
  - blocked-by-exclusion
  - structural-conflict
- persists manifest and per-product pre-change snapshots before any apply path
- records lineage pointers needed for "Why changed?" drill-down
  </action>
  <verify>
- tests cover product grouping, field-level statuses, structural conflict classifications, snapshot persistence triggers
  </verify>
  <done>
- RESOLVE-05 and RESOLVE-08 data-path foundation is implemented for preview generation
- Structural integrity behaviors align with locked context decisions
  </done>
</task>

<task type="auto">
  <name>Task 3: Expose dry-run build/read APIs with robust error semantics</name>
  <files>
    - src/api/v1/resolution/routes.py
    - src/api/v1/resolution/schemas.py
    - tests/api/test_resolution_dry_run.py
  </files>
  <action>
Add endpoints:
- POST `/api/v1/resolution/dry-runs` (create batch preview from input set)
- GET `/api/v1/resolution/dry-runs/{batch_id}` (product-grouped diff view)
- GET `/api/v1/resolution/dry-runs/{batch_id}/lineage` (reason/trace details)

Error semantics:
- 409 for lock/contention or invalid transition
- 422 for unresolvable structural payload issues
- 403 for cross-user store access

Ensure response payload includes confidence score + badge metadata and human-readable reason sentences per change.
  </action>
  <verify>
- `python -m pytest -q tests/api/test_resolution_dry_run.py`
- Endpoint responses match schema contracts and RFC 7807 behavior
  </verify>
  <done>
- Resolver/dry-run backend contract is complete for frontend integration
- Dry-run output contains decision transparency required by context
  </done>
</task>

</tasks>

<verification>
- [ ] Dry-run generation persists snapshots before any apply-eligible state
- [ ] Structural conflict classes are explicit and test-covered
- [ ] Confidence + reason factors present in API response schema
- [ ] Web source cannot execute for unverified suppliers
</verification>

<success_criteria>
- Phase 8 has operational resolution + persisted dry-run generation flow
- Product and variant mismatch handling is explicit and governed
- API contract is ready for collaborative review UI
</success_criteria>

<output>
After completion, create `.planning/phases/08-product-resolution-engine/08-02-SUMMARY.md`
</output>
