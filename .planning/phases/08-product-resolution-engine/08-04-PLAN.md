---
phase: 08-product-resolution-engine
plan: 04
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - src/resolution/preflight.py
  - src/resolution/apply_engine.py
  - src/resolution/throttle.py
  - src/resolution/shopify_graphql.py
  - src/resolution/media_ingest.py
  - src/tasks/resolution_apply.py
  - src/jobs/orchestrator.py
  - src/api/v1/resolution/routes.py
  - src/models/job.py
  - tests/resolution/test_preflight.py
  - tests/resolution/test_apply_engine.py
  - tests/resolution/test_media_ingest.py
  - tests/api/test_recovery_logs.py
autonomous: true

must_haves:
  truths:
    - "Scheduled and immediate apply always run pre-flight validation before first mutation"
    - "Stale/deleted targets are not pushed and are preserved in Recovery Logs with restore context"
    - "Apply throughput adapts to Shopify throttle signals and pauses on configured critical-error threshold"
    - "Images are downloaded, hashed, deduplicated, and uploaded from controlled assets (not vendor URL pass-through)"
  artifacts:
    - path: "src/resolution/preflight.py"
      provides: "Target existence and stale-state conflict checks"
      contains: "run_preflight"
    - path: "src/resolution/apply_engine.py"
      provides: "Mutation orchestrator with idempotent apply lifecycle"
      contains: "apply_batch"
    - path: "src/resolution/media_ingest.py"
      provides: "Asset sovereignty ingestion and Shopify file push bridge"
      contains: "ingest_and_upload_media"
  key_links:
    - from: "src/resolution/preflight.py"
      to: "src/models/recovery_log.py"
      via: "stale target routing into recovery logs"
      pattern: "stale_target"
    - from: "src/resolution/throttle.py"
      to: "src/resolution/shopify_graphql.py"
      via: "dynamic backoff from throttle metadata"
      pattern: "throttleStatus"
    - from: "src/resolution/media_ingest.py"
      to: "src/resolution/shopify_graphql.py"
      via: "staged upload + file create + association flow"
      pattern: "stagedUploadsCreate"
---

<objective>
Implement guarded apply execution and asset-sovereign media handling for Phase 8.

Purpose: ensure approved dry-runs can be applied safely at scale with robust recovery and auditability.
Output: pre-flight/apply engine, queue task integration, recovery logs API, and media ingestion pipeline.
</objective>

<execution_context>
@codexclaude/get-shit-done/workflows/execute-plan.md
@codexclaude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-product-resolution-engine/08-CONTEXT.md
@.planning/phases/08-product-resolution-engine/08-RESEARCH.md
@.planning/phases/08-product-resolution-engine/08-02-SUMMARY.md
@src/jobs/orchestrator.py
@src/tasks/ingest.py
@src/models/job.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build pre-flight validator and recovery log routing</name>
  <files>
    - src/resolution/preflight.py
    - src/api/v1/resolution/routes.py
    - tests/resolution/test_preflight.py
    - tests/api/test_recovery_logs.py
  </files>
  <action>
Implement pre-flight control plane:
- run within 60 seconds of first apply mutation
- verify existence of every target Shopify product/variant
- validate structural assumptions still hold for conflicted items
- split batch into eligible vs conflicted target sets

For conflicted items:
- do not mutate
- persist detailed entry in `Recovery Logs` (reason, payload, last-known snapshot pointer, actor, timestamp)
- expose read APIs for recovery review and restore/recreate workflows
  </action>
  <verify>
- `python -m pytest -q tests/resolution/test_preflight.py`
- `python -m pytest -q tests/api/test_recovery_logs.py`
  </verify>
  <done>
- Stale snapshot race condition is explicitly handled
- Recovery Logs are operational and user-visible via API
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement apply engine with adaptive throughput and Shopify mutation adapters</name>
  <files>
    - src/resolution/apply_engine.py
    - src/resolution/throttle.py
    - src/resolution/shopify_graphql.py
    - src/tasks/resolution_apply.py
    - src/jobs/orchestrator.py
    - src/models/job.py
    - tests/resolution/test_apply_engine.py
  </files>
  <action>
Create apply engine that supports immediate and scheduled modes:
- executes only eligible items from pre-flight output
- uses adaptive concurrency based on throttle signals
- applies dynamic backoff on pressure
- auto-pauses when critical errors exceed threshold `N`
- reruns dry-run for conflicted items only (not full batch) on scheduled drift conflicts

Implement GraphQL mutation adapters with explicit strategy:
- product structure changes (`productSet`)
- variant create/update (`productVariantsBulkCreate` / `productVariantsBulkUpdate`)
- inventory with CAS-safe semantics (`inventorySetQuantities`) and idempotency requirements
- async operation polling where relevant (`productOperation`)

Integrate apply lifecycle into jobs/task orchestration and status visibility surfaces.
  </action>
  <verify>
- `python -m pytest -q tests/resolution/test_apply_engine.py`
- integration tests cover threshold pause, backoff behavior, and conflict-only rerun path
  </verify>
  <done>
- Safe, idempotent apply pipeline is operational with throughput governance
- Execution policy from context is fully represented in code contracts
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement image sovereignty ingestion pipeline</name>
  <files>
    - src/resolution/media_ingest.py
    - src/resolution/shopify_graphql.py
    - tests/resolution/test_media_ingest.py
  </files>
  <action>
Implement media pipeline:
- download source images to controlled storage
- compute hash and deduplicate
- persist source trace metadata for audit/debug
- push media to Shopify using staged upload/file workflows
- only associate READY/processed files to products/variants

Enforce no final apply path that references vendor image URLs directly.
Add audit metadata linking each media change to source URL hash and uploaded file identifier.
  </action>
  <verify>
- `python -m pytest -q tests/resolution/test_media_ingest.py`
- tests prove URL passthrough is rejected in final apply path
  </verify>
  <done>
- Image sovereignty requirement is enforced end-to-end
- Media associations are traceable and recoverable
  </done>
</task>

</tasks>

<verification>
- [ ] pre-flight executes before first mutation in both immediate and scheduled paths
- [ ] conflicted stale targets never mutate and always land in Recovery Logs
- [ ] adaptive concurrency/backoff is driven by runtime throttle signals
- [ ] inventory writes use CAS-safe semantics and idempotency where required
- [ ] media pipeline uses controlled assets with hash dedup + source trace
</verification>

<success_criteria>
- Phase 8 apply path is safe, auditable, and production-oriented
- Recovery behavior is deterministic for stale/deleted targets
- Image and inventory operations satisfy locked safety constraints
</success_criteria>

<output>
After completion, create `.planning/phases/08-product-resolution-engine/08-04-SUMMARY.md`
</output>
