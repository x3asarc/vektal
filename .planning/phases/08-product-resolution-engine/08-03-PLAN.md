---
phase: 08-product-resolution-engine
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - frontend/src/features/resolution/components/DryRunReview.tsx
  - frontend/src/features/resolution/components/ProductChangeCard.tsx
  - frontend/src/features/resolution/components/FieldChangeRow.tsx
  - frontend/src/features/resolution/components/TechnicalDetailsToggle.tsx
  - frontend/src/features/resolution/components/CollaborationBadge.tsx
  - frontend/src/features/resolution/components/ActivityPanels.tsx
  - frontend/src/features/resolution/api/resolution-api.ts
  - frontend/src/features/resolution/state/review-store.ts
  - frontend/src/features/settings/components/StrategyQuiz.tsx
  - frontend/src/features/settings/components/RuleSuggestionsInbox.tsx
  - frontend/src/app/(app)/settings/page.tsx
  - frontend/src/app/(app)/dashboard/page.tsx
  - frontend/src/shared/contracts/resolution.ts
  - frontend/src/lib/query/keys.ts
  - frontend/src/lib/api/problem-details.ts
  - frontend/src/lib/auth/guards.ts
  - tests/frontend/resolution/review.contract.test.tsx
  - tests/frontend/settings/strategy-quiz.contract.test.tsx
autonomous: false

must_haves:
  truths:
    - "Dry-run review is product-grouped with per-field approval and bulk actions"
    - "Multi-user review enforces read-only for non-lock owners and visibly shows lock ownership"
    - "Users can configure supplier behavior via structured strategy quiz and accept rule suggestions"
    - "Visibility surfaces show currently active work and upcoming scheduled batches"
  artifacts:
    - path: "frontend/src/features/resolution/components/DryRunReview.tsx"
      provides: "Main grouped review experience"
      contains: "Approve selected fields"
    - path: "frontend/src/features/settings/components/StrategyQuiz.tsx"
      provides: "Structured onboarding/settings rule capture"
      contains: "create as draft"
    - path: "frontend/src/features/resolution/components/ActivityPanels.tsx"
      provides: "Currently Happening and Coming Up Next surfaces"
      contains: "Coming Up Next"
  key_links:
    - from: "frontend/src/features/resolution/state/review-store.ts"
      to: "frontend/src/features/resolution/api/resolution-api.ts"
      via: "approval actions, lock heartbeat, and refresh"
      pattern: "heartbeat"
    - from: "frontend/src/features/settings/components/RuleSuggestionsInbox.tsx"
      to: "frontend/src/features/resolution/api/resolution-api.ts"
      via: "accept/reject supplier-scoped suggestions"
      pattern: "suggestion"
---

<objective>
Implement collaborative dry-run UX and settings-based preference capture for Phase 8.

Purpose: convert backend dry-run contracts into a user-safe review/approval workflow with explicit ownership and learning loops.
Output: complete review UI, lock-aware collaboration behavior, and strategy quiz/rule suggestion interfaces.
</objective>

<execution_context>
@codexclaude/get-shit-done/workflows/execute-plan.md
@codexclaude/get-shit-done/templates/summary.md
@codexclaude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-product-resolution-engine/08-CONTEXT.md
@.planning/phases/08-product-resolution-engine/08-RESEARCH.md
@.planning/phases/08-product-resolution-engine/08-02-SUMMARY.md
@frontend/src/features/onboarding/components/OnboardingWizard.tsx
@frontend/src/shell/components/NotificationStack.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build grouped dry-run review UI with explainability and field-level approvals</name>
  <files>
    - frontend/src/features/resolution/components/DryRunReview.tsx
    - frontend/src/features/resolution/components/ProductChangeCard.tsx
    - frontend/src/features/resolution/components/FieldChangeRow.tsx
    - frontend/src/features/resolution/components/TechnicalDetailsToggle.tsx
    - frontend/src/features/resolution/api/resolution-api.ts
    - frontend/src/shared/contracts/resolution.ts
    - frontend/src/lib/query/keys.ts
  </files>
  <action>
Implement review surface contracts:
- group by product (default)
- per-field approve/reject/edit actions
- bulk action support (selected rows/products)
- reason sentence + confidence score/badge + reason factors
- advanced technical trace in collapsible details toggle

Status styling must distinguish:
- auto-applied by rule
- awaiting approval
- blocked by exclusion
- structural conflict
  </action>
  <verify>
- `cd frontend && npm run test -- tests/frontend/resolution/review.contract.test.tsx`
- `cd frontend && npm run typecheck`
  </verify>
  <done>
- Review UI satisfies dry-run semantics and explainability requirements
- Field-level approvals are fully interactive with bulk support
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement collaboration ownership, lock-aware actions, and activity views</name>
  <files>
    - frontend/src/features/resolution/state/review-store.ts
    - frontend/src/features/resolution/components/CollaborationBadge.tsx
    - frontend/src/features/resolution/components/ActivityPanels.tsx
    - frontend/src/app/(app)/dashboard/page.tsx
    - frontend/src/lib/api/problem-details.ts
  </files>
  <action>
Implement lock-aware collaboration:
- lock owner badge visible on active batches
- non-owner users are read-only for edits/approvals
- lock heartbeat + stale lock refresh UX
- conflict toasts/banners on 409 lock responses

Expose activity views:
- `Currently Happening` for active reviews and in-progress apply jobs
- `Coming Up Next` for approved scheduled batches

Add user-attribution cues (e.g., color-coded change ownership metadata) in history/review views.
  </action>
  <verify>
- tests cover owner/non-owner mode transitions and lock-conflict handling
- dashboard renders both activity panels from API state
  </verify>
  <done>
- Multi-user interaction is deterministic and user-friendly without live co-edit complexity
- Ownership and activity visibility requirements are met
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement Strategy Quiz and batched rule suggestion inbox</name>
  <files>
    - frontend/src/features/settings/components/StrategyQuiz.tsx
    - frontend/src/features/settings/components/RuleSuggestionsInbox.tsx
    - frontend/src/app/(app)/settings/page.tsx
    - tests/frontend/settings/strategy-quiz.contract.test.tsx
  </files>
  <action>
Add structured settings UX for machine-actionable preference capture:
- strategy quiz with supplier behavior questions (new variants handling, variant scraping policy, first-batch immediate/scheduled default)
- explicit consent controls for auto-create variants and field-group auto-apply
- batched end-of-dry-run rule suggestions with accept/decline and editable expiry

Ensure no free-form-only inputs for critical policy fields; use constrained choices first with optional notes.
  </action>
  <verify>
- `cd frontend && npm run test -- tests/frontend/settings/strategy-quiz.contract.test.tsx`
- settings API integration paths exercised with mocked responses
  </verify>
  <done>
- Strategy quiz and suggestion inbox are operational and policy-safe
- User preference capture is explicit, structured, and editable
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human verify collaborative dry-run and settings UX</name>
  <files>
    - frontend/src/features/resolution/components/DryRunReview.tsx
    - frontend/src/features/settings/components/StrategyQuiz.tsx
    - frontend/src/features/resolution/components/ActivityPanels.tsx
  </files>
  <action>
Run manual product and collaboration verification:
1. Open a dry-run and verify product-grouped cards with per-field approval actions.
2. Confirm reason sentence + confidence + technical details are visible and coherent.
3. Simulate two users and verify lock owner can edit while non-owner is read-only.
4. Confirm `Currently Happening` and `Coming Up Next` panels populate correctly.
5. Open Settings and complete Strategy Quiz + accept/decline a suggested rule.
  </action>
  <verify>Type "approved" or list failures by page/component and lock state observed.</verify>
  <done>Human validation confirms UX and collaboration behavior is acceptable for execution completion.</done>
</task>

</tasks>

<verification>
- [ ] frontend lint/typecheck/build pass
- [ ] contract tests cover grouped review, lock states, and strategy quiz behavior
- [ ] non-owner approval attempts are blocked in UI and API response handling
- [ ] activity surfaces reflect backend state without stale ownership confusion
</verification>

<success_criteria>
- Collaborative dry-run UX meets locked Phase 8 behavior rules
- Settings capture + rule suggestion loop is in place for learning engine
- User-facing trust controls (reasons, confidence, lineage access) are available
</success_criteria>

<output>
After completion, create `.planning/phases/08-product-resolution-engine/08-03-SUMMARY.md`
</output>
