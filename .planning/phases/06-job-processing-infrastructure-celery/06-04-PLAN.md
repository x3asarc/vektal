---
phase: 06-job-processing-infrastructure-celery
plan: 04
type: execute
wave: 3
depends_on: ["06-01", "06-02", "06-03"]
files_modified:
  - src/jobs/checkpoints.py
  - src/jobs/dispatcher.py
  - src/jobs/finalizer.py
  - src/tasks/audits.py
  - src/tasks/control.py
  - tests/jobs/test_dispatcher.py
  - tests/jobs/test_finalizer.py
autonomous: true

must_haves:
  truths:
    - "Checkpoint intents are persisted then dispatched through lock-safe outbox flow"
    - "Dispatcher retries are durable and idempotent"
    - "Finalizer transitions only when chunk and outbox terminal conditions are met"
    - "Strict vs lenient terminal-failure policy is explicitly implemented with strict as default"
  artifacts:
    - path: "src/jobs/dispatcher.py"
      provides: "SKIP LOCKED claim/publish/retry pipeline"
      contains: "skip_locked"
    - path: "src/jobs/finalizer.py"
      provides: "terminal state convergence logic"
      contains: "finalize_job"
  key_links:
    - from: "src/tasks/audits.py"
      to: "src/jobs/dispatcher.py"
      via: "dispatcher task entrypoint"
      pattern: "dispatch_pending_audits"
---

<objective>
Implement checkpoint outbox dispatch and finalizer convergence.

Purpose: close JOBS-07 reliability/audit behavior and completion correctness.

Output: durable dispatcher + finalizer services with tests.
</objective>

<execution_context>
@codexclaude/get-shit-done/workflows/execute-plan.md
@codexclaude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-job-processing-infrastructure-celery/06-CONTEXT.md
@.planning/phases/06-job-processing-infrastructure-celery/06-RESEARCH.md
@.planning/phases/06-job-processing-infrastructure-celery/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement lock-safe checkpoint dispatcher</name>
  <files>
    - src/jobs/checkpoints.py
    - src/jobs/dispatcher.py
    - src/tasks/audits.py
    - tests/jobs/test_dispatcher.py
  </files>
  <action>
Implement dispatcher with `FOR UPDATE SKIP LOCKED` claim semantics, durable attempt accounting, retry scheduling (`next_dispatch_at`), and idempotent dispatch behavior.
Apply locked audit trigger policy exactly:
- `<1000` products: checkpoints at 25,35,45,55,65,75,85,95,100
- `>=1000` products: checkpoint at 100 only.
  </action>
  <verify>
- concurrent dispatcher tests pass
- failed publish schedules retry without data loss
  </verify>
  <done>
- checkpoint outbox is reliable under broker/process failure
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement finalizer state transitions</name>
  <files>
    - src/jobs/finalizer.py
    - src/tasks/control.py
    - tests/jobs/test_finalizer.py
  </files>
  <action>
Implement finalizer rules for:
- `running -> completed|failed_terminal`
- `cancel_requested -> cancelled`
with strict checks for chunk terminality and due pending checkpoints.
Resolve open decision #2 from context by implementing strict mode as default and exposing clear configuration path for lenient mode.
  </action>
  <verify>
- finalizer tests pass for completion/failure/cancel branches
  </verify>
  <done>
- terminal state convergence is deterministic and DB-authoritative
  </done>
</task>

</tasks>

<verification>
- [ ] dispatcher/finalizer tests pass
- [ ] no duplicate logical checkpoint dispatch outcomes
- [ ] completion blocked when due pending checkpoints exist
</verification>

<success_criteria>
- JOBS-07 auditing flow and completion correctness are implemented
</success_criteria>

<output>
After completion, create `.planning/phases/06-job-processing-infrastructure-celery/06-04-SUMMARY.md`
</output>
