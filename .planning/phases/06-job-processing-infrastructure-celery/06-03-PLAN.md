---
phase: 06-job-processing-infrastructure-celery
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - src/jobs/orchestrator.py
  - src/tasks/ingest.py
  - src/tasks/__init__.py
  - src/api/v1/jobs/routes.py
  - tests/jobs/test_ingest_chunk_flow.py
autonomous: true

must_haves:
  truths:
    - "Ingest chunk processing is idempotent under duplicate delivery"
    - "Chunk membership is frozen and not recomputed on retry"
    - "API-triggered ingest runs asynchronously without blocking request lifecycle"
    - "Job and per-item results are persisted in PostgreSQL as work progresses"
  artifacts:
    - path: "src/jobs/orchestrator.py"
      provides: "start_ingest and ingest_chunk transaction logic"
      contains: "start_ingest"
    - path: "src/tasks/ingest.py"
      provides: "Celery ingest task wrappers"
      contains: "ingest_chunk"
  key_links:
    - from: "src/tasks/ingest.py"
      to: "src/jobs/orchestrator.py"
      via: "service-layer delegation"
      pattern: "orchestrator"
---

<objective>
Implement ingest orchestration and chunk execution path.

Purpose: close JOBS-01/JOBS-02/JOBS-04/JOBS-06 foundations with real async processing semantics.

Output: start_ingest + ingest_chunk service and task contracts wired into job API flow.
</objective>

<execution_context>
@codexclaude/get-shit-done/workflows/execute-plan.md
@codexclaude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-job-processing-infrastructure-celery/06-CONTEXT.md
@.planning/phases/06-job-processing-infrastructure-celery/06-RESEARCH.md
@.planning/phases/06-job-processing-infrastructure-celery/06-01-SUMMARY.md
@.planning/phases/06-job-processing-infrastructure-celery/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement start_ingest orchestration contract</name>
  <files>
    - src/jobs/orchestrator.py
    - src/tasks/ingest.py
  </files>
  <action>
Implement `start_ingest(job_id, store_id, user_id)`:
- enforce active-ingest guard
- fetch/freeze ordered membership
- create chunk rows (`pending`) with frozen `product_ids_json`
- enqueue chunk tasks using tier-aware queue routing.
  </action>
  <verify>
- job launch produces chunk rows and enqueued tasks
- duplicate starts are blocked by active-ingest guard
  </verify>
  <done>
- ingest launch path is deterministic and async-ready
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement claim/reclaim and chunk completion transaction</name>
  <files>
    - src/jobs/orchestrator.py
    - src/tasks/ingest.py
    - tests/jobs/test_ingest_chunk_flow.py
  </files>
  <action>
Implement `ingest_chunk(job_id, store_id, chunk_idx)` with:
- stale-aware claim/reclaim semantics
- no dynamic membership recomputation on retries
- single transaction completion path updating progress and creating checkpoint intents
- persistence of job/item outcomes in existing PostgreSQL job tables
- explicit job/chunk status progression for JOBS-02 tracking (`queued/running/completed/failed/cancelled` user-facing mapping)
- idempotent no-op when chunk is already completed or not owned.
  </action>
  <verify>
- duplicate task execution does not double-count progress
- chunk state transitions obey contract
- tests validate stale reclaim behavior
  </verify>
  <done>
- chunk processing is safe under at-least-once delivery
  </done>
</task>

<task type="auto">
  <name>Task 3: Keep API compatibility while enabling async launch</name>
  <files>src/api/v1/jobs/routes.py</files>
  <action>
Wire launch/cancel/list endpoints so existing contracts remain stable while new async orchestration executes in background.
Use accepted-response semantics where flow initiates long-running work.
  </action>
  <verify>
- `/api/v1/jobs` behavior remains backward-compatible
- launch endpoint does not block on full ingest completion
  </verify>
  <done>
- API is orchestration-aware and non-blocking
  </done>
</task>

</tasks>

<verification>
- [ ] orchestrator unit/integration tests pass
- [ ] duplicate-delivery idempotency verified
- [ ] API launch path is non-blocking under test
</verification>

<success_criteria>
- JOBS-01/JOBS-02/JOBS-04/JOBS-06 core ingest path is implemented and test-covered
</success_criteria>

<output>
After completion, create `.planning/phases/06-job-processing-infrastructure-celery/06-03-SUMMARY.md`
</output>
