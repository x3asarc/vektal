---
phase: 06-job-processing-infrastructure-celery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/versions/<revision_id>_phase6_ingest_chunks_audit_checkpoints.py
  - src/models/job.py
  - src/models/ingest_chunk.py
  - src/models/audit_checkpoint.py
  - src/models/__init__.py
autonomous: true

must_haves:
  truths:
    - "Chunk and checkpoint state is DB-enforced with unique/check constraints"
    - "Job progress invariants are validated at schema level"
    - "Schema supports idempotent at-least-once execution"
  artifacts:
    - path: "migrations/versions/<revision_id>_phase6_ingest_chunks_audit_checkpoints.py"
      provides: "Phase 6 schema contracts"
      contains: "ingest_chunks"
    - path: "src/models/ingest_chunk.py"
      provides: "Chunk claim/complete persistence model"
      contains: "IngestChunk"
    - path: "src/models/audit_checkpoint.py"
      provides: "Durable checkpoint outbox model"
      contains: "AuditCheckpoint"
  key_links:
    - from: "src/models/ingest_chunk.py"
      to: "src/models/job.py"
      via: "job relationship and progress coupling"
      pattern: "job_id"
---

<objective>
Implement the Phase 6 schema foundation only.

Purpose: establish durable persistence and invariants before queue routing and task integration.

Output: migration + models for `ingest_chunks` and `audit_checkpoints`, plus required `jobs` constraints/guards.
</objective>

<execution_context>
@codexclaude/get-shit-done/workflows/execute-plan.md
@codexclaude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-job-processing-infrastructure-celery/06-CONTEXT.md
@.planning/phases/06-job-processing-infrastructure-celery/06-RESEARCH.md
@src/models/job.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `ingest_chunks` and `audit_checkpoints` schema</name>
  <files>
    - migrations/versions/<revision_id>_phase6_ingest_chunks_audit_checkpoints.py
    - src/models/ingest_chunk.py
    - src/models/audit_checkpoint.py
    - src/models/__init__.py
  </files>
  <action>
Create the two new Phase 6 tables and SQLAlchemy models exactly per `06-CONTEXT.md`:
- `ingest_chunks` with status/claim/progress fields and required invariants
- `audit_checkpoints` outbox with dispatch state, attempts, and retry scheduling
- enforce `UNIQUE(job_id, chunk_idx)` and `UNIQUE(job_id, checkpoint)`
- enforce progress and completion check constraints.
  </action>
  <verify>
- `flask db upgrade` succeeds
- constraints/indexes exist in DB
  </verify>
  <done>
- chunk/outbox persistence primitives are migration-managed and enforceable
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend jobs guardrails for Phase 6 state machine</name>
  <files>src/models/job.py</files>
  <action>
Add required job-level constraints and enum/state support:
- progress bounds (`processed_count` contract)
- active-ingest lock scope per store for ingest-class jobs
- status semantics needed for `cancel_requested` and terminal transitions.
  </action>
  <verify>
- model imports cleanly
- migration round-trip (upgrade/downgrade) is reversible
  </verify>
  <done>
- job table can safely support Phase 6 orchestration semantics
  </done>
</task>

</tasks>

<verification>
- [ ] Migration upgrade/downgrade succeeds
- [ ] All required constraints exist and are test-verified
- [ ] Existing Phase 5 job read paths remain compatible
</verification>

<success_criteria>
- Phase 6 schema invariants are implemented and enforceable
- Idempotency primitives are present at the data layer
</success_criteria>

<output>
After completion, create `.planning/phases/06-job-processing-infrastructure-celery/06-01-SUMMARY.md`
</output>
