# Task Summary: 14.2-03 - Deferred Loading + schema_json Column

## Status: COMPLETE

## Changes
- **migrations/versions/g14_2_03_schema_json.py**: Created Alembic migration to add `schema_json` JSONB column to `assistant_tool_registry` and populate it from existing tool data.
- **src/models/assistant_tool_registry.py**: Added `schema_json` field and `to_tool_schema` method to the SQLAlchemy model.
- **.claude/settings.local.json**: Enabled `deferred_loading` and configured `base_tools` and `discovery_threshold`.
- **src/graph/mcp_server.py**: Refactored `list_tool_contracts` to support conditional loading based on configuration. Enhanced `_search_tools_handler` to return `fullSchema` for discovered tools.
- **tests/graph/test_deferred_loading.py**: Created unit tests verifying conditional loading and schema retrieval.

## Verification Result
- **tests/graph/test_deferred_loading.py**: 3 passed.
- **Manual Verification**: Confirmed configuration flags and model schema updates.

## Artifacts
- Reports created at `reports/14.2/14.2-03/`.
