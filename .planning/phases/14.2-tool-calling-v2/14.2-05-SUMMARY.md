# Task Summary: 14.2-05 - compact_output Mode + Edge-Type Scoring

## Status: COMPLETE

## Changes
- **src/graph/search_expand_bridge.py**: Implemented `compact` mode in `BridgeConfig` and `search_then_expand`. Added `serialize_node` to strip verbose fields and `_generate_summary` for nodes. Added `EDGE_SCORE_MULTIPLIER` and `apply_temporal_decay` for improved node ranking.
- **src/graph/query_interface.py**: Updated `query_graph` and `query_with_bridge` to support and propagate the `compact` parameter. Added template result serialization for compact mode.
- **src/graph/mcp_server.py**: Added `compact_output` parameter to `query_graph` tool schema and handler. Updated `dispatch_tool_call` to pass the new argument.
- **tests/graph/test_compact_output.py**: Created unit tests verifying compact serialization, edge multipliers, and temporal decay.

## Verification Result
- **tests/graph/test_compact_output.py**: 3 passed.
- **Manual Verification**: Confirmed parameter propagation and scoring logic.

## Artifacts
- Reports created at `reports/14.2/14.2-05/`.
