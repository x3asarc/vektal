# Task Summary: 14.2-01 - Input Examples on Tool Schemas

## Status: COMPLETE

## Changes
- **src/graph/mcp_server.py**: Added `input_examples` to `query_graph`, `get_dependencies`, and `retrieve_intent` tool contracts. Updated `run_server` to propagate these examples to the MCP `types.Tool` objects.
- **src/assistant/tool_projection.py**: Added `input_examples` to `_DEFAULT_TOOLS`. Updated `_load_registry` and `project_effective_toolset` to include `input_examples` from `AssistantToolRegistry.metadata_json` in the final tool payload.
- **migrations/versions/g14_2_01_tool_input_examples.py**: Created Alembic migration to seed `input_examples` for the 6 default assistant tools in the `assistant_tool_registry` table using safe `jsonb` merge operations.
- **tests/graph/test_mcp_tool_examples.py**: Created unit tests to verify MCP tool contracts and tool projection propagation.

## Verification Result
- **tests/graph/test_mcp_tool_examples.py**: 2 passed.
- **Manual Verification**: Inspected tool contracts in `mcp_server.py` and projection logic in `tool_projection.py`.

## Artifacts
- Reports created at `reports/14.2/14.2-01/`.
