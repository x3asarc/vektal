---
phase: 10-conversational-ai-interface
plan: 03
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
requirements:
  - CHAT-05
  - CHAT-06
  - CHAT-02
files_modified:
  - src/api/v1/chat/routes.py
  - src/api/v1/chat/orchestrator.py
  - src/api/v1/chat/bulk.py
  - src/jobs/dispatcher.py
  - src/jobs/progress.py
  - src/tasks/chat_bulk.py
  - src/celery_app.py
  - src/models/chat_action.py
  - tests/api/test_chat_bulk_workflow.py
  - tests/jobs/test_chat_bulk_chunking.py
  - tests/jobs/test_chat_bulk_fairness.py
autonomous: true

must_haves:
  truths:
    - "Bulk chat requests support up to 1000 SKUs through auto-chunked orchestration."
    - "Chunk payloads honor hard input bounds (max 250 per operation)."
    - "Concurrency is adaptive by throttle signals and always bounded by safety caps."
    - "Worker fairness settings prevent starvation under mixed chunk durations."
    - "Conflicts remain user-gated and cannot bypass dry-run or approval policy."
  artifacts:
    - path: "src/api/v1/chat/bulk.py"
      provides: "Chunk planner and concurrency controller"
      contains: "plan_chunks"
    - path: "src/tasks/chat_bulk.py"
      provides: "Background execution for chunked bulk actions"
      contains: "run_chat_bulk_action"
    - path: "tests/jobs/test_chat_bulk_fairness.py"
      provides: "No-starvation verification"
      contains: "mixed duration"
  key_links:
    - from: "src/api/v1/chat/bulk.py"
      to: "src/jobs/progress.py"
      via: "aggregate and per-chunk progress emission"
      pattern: "announce_job_progress"
    - from: "src/tasks/chat_bulk.py"
      to: "src/celery_app.py"
      via: "fair worker profile"
      pattern: "task_acks_late"
---

<objective>
Implement safe, high-throughput bulk conversational orchestration for up to 1000 SKUs.

Purpose: provide scalable throughput with strict safety invariants, clear observability, and deterministic conflict handling.
Output: queue-backed chunked workflow with adaptive concurrency and fairness-proven execution.
</objective>

<execution_context>
@codexclaude/get-shit-done/workflows/execute-plan.md
@codexclaude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-conversational-ai-interface/10-CONTEXT.md
@.planning/phases/10-conversational-ai-interface/10-RESEARCH.md
@.planning/phases/09-real-time-progress-tracking/09-RESEARCH.md
@src/jobs/progress.py
@src/api/v1/jobs/routes.py
@src/api/jobs/events.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build 1000-SKU chunk planner with hard payload bounds</name>
  <files>
    - src/api/v1/chat/bulk.py
    - src/api/v1/chat/orchestrator.py
    - src/models/chat_action.py
    - tests/jobs/test_chat_bulk_chunking.py
  </files>
  <action>
Implement bulk intake contracts:
- accept up to 1000 SKUs
- normalize, deduplicate, and validate identifiers
- build chunk plan with dynamic target range and hard cap <= 250 inputs per operation
- persist chunk metadata for retry lineage and auditability
  </action>
  <verify>
- `python -m pytest -q tests/jobs/test_chat_bulk_chunking.py`
- tests assert payload upper bounds and chunk lineage integrity
  </verify>
  <done>
- Chunk planner enforces hard limits and traceable execution structure.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement throttle-driven adaptive concurrency and fair worker profile</name>
  <files>
    - src/api/v1/chat/bulk.py
    - src/tasks/chat_bulk.py
    - src/celery_app.py
    - tests/jobs/test_chat_bulk_fairness.py
  </files>
  <action>
Implement adaptive execution controller:
- consume GraphQL throttle signals (`extensions.cost.throttleStatus`) with REST fallback
- adjust concurrency downward aggressively on throttle/429 and upward conservatively on sustained headroom
- enforce admin concurrency cap and queue-health guards

Configure chat-bulk fairness profile:
- `task_acks_late = True`
- `worker_prefetch_multiplier = 1`
- optional fair scheduling mode for mixed-duration loads

Ensure chunk handlers are idempotent for replay safety.
  </action>
  <verify>
- tests assert throttle-driven concurrency decisions
- tests assert no-starvation behavior under mixed chunk durations
- tests assert replay/idempotency safety with late ack profile
  </verify>
  <done>
- Throughput scales safely while preserving fairness and reliability.
  </done>
</task>

<task type="auto">
  <name>Task 3: Execute chunk workflows with conflict isolation and summary integrity</name>
  <files>
    - src/tasks/chat_bulk.py
    - src/api/v1/chat/routes.py
    - src/jobs/progress.py
    - tests/api/test_chat_bulk_workflow.py
  </files>
  <action>
Run chunked dry-run/approval/apply execution:
- emit aggregate and per-chunk progress states
- isolate low-confidence/structural conflicts for explicit user decision
- continue safe eligible work without unsafe fallback
- produce terminal summary with applied/skipped/conflicted/failed counts and recovery links

Keep chat event volume bounded so one session can be monitored with a single stream.
  </action>
  <verify>
- `python -m pytest -q tests/api/test_chat_bulk_workflow.py`
- `python -m pytest -q tests/api/test_jobs_progress_contract.py`
  </verify>
  <done>
- Bulk orchestration is observable, safe, and operationally deterministic.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Wave 3 go-live gate for high-volume bulk operations</name>
  <files>
    - src/api/v1/chat/bulk.py
    - src/tasks/chat_bulk.py
    - tests/jobs/test_chat_bulk_fairness.py
  </files>
  <action>
Verify go-live criteria:
1. 1000-SKU simulation completes with no unsafe mutation bypass.
2. Conflicted items remain approval-gated and linked to Recovery Logs.
3. No starvation pattern under mixed chunk durations.
4. Progress summaries match chunk outcomes exactly.
  </action>
  <verify>Type "approved" or provide failed gate checks with exact failing signal.</verify>
  <done>Wave 3 is cleared for frontend-facing bulk UX exposure.</done>
</task>

</tasks>

<verification>
- [ ] Up to 1000 SKUs accepted and auto-chunked with <= 250 per operation
- [ ] Adaptive bounded concurrency enforced from throttle signals
- [ ] Fairness profile validated under mixed chunk durations
- [ ] Conflict gating and recovery linkage verified
</verification>

<success_criteria>
- CHAT-05 and CHAT-06 are implemented with production-safe throughput behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/10-conversational-ai-interface/10-03-SUMMARY.md`
</output>
