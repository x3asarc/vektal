---
phase: 10-conversational-ai-interface
plan: 01
type: execute
wave: 1
depends_on: []
requirements:
  - CHAT-02
  - CHAT-03
  - CHAT-07
  - CHAT-08
files_modified:
  - src/api/v1/chat/__init__.py
  - src/api/v1/chat/routes.py
  - src/api/v1/chat/schemas.py
  - src/api/__init__.py
  - src/core/chat/router.py
  - src/models/chat_session.py
  - src/models/chat_message.py
  - src/models/chat_action.py
  - migrations/versions/<new>_phase10_chat_foundation.py
  - tests/api/test_chat_contract.py
  - tests/api/test_chat_stream.py
autonomous: true

must_haves:
  truths:
    - "Chat APIs exist under /api/v1/chat/* with authenticated ownership scope."
    - "Conversation state machine is explicit (`at_door`, `in_house`) and persisted."
    - "Assistant output uses typed blocks (text/table/diff/action/progress/alert) for deterministic UI rendering."
    - "Chat streaming is implemented with request-safe generator handling and test-backed event contracts."
  artifacts:
    - path: "src/api/v1/chat/routes.py"
      provides: "Session/message/stream API contracts"
      contains: "POST /api/v1/chat/sessions"
    - path: "src/models/chat_action.py"
      provides: "Action lineage and execution state"
      contains: "status"
    - path: "tests/api/test_chat_stream.py"
      provides: "SSE contract verification"
      contains: "named event"
  key_links:
    - from: "src/api/v1/chat/routes.py"
      to: "src/core/chat/router.py"
      via: "intent and entity extraction"
      pattern: "route(message)"
    - from: "src/api/v1/chat/routes.py"
      to: "src/api/core/sse.py"
      via: "chat stream event delivery"
      pattern: "event: chat_"
---

<objective>
Stand up backend chat contracts and stream foundation with explicit session state and deterministic response blocks.

Purpose: establish a production-safe control plane for conversational orchestration before workflow execution logic.
Output: authenticated `/api/v1/chat/*` contract surface ready for single-SKU and bulk orchestration.
</objective>

<execution_context>
@codexclaude/get-shit-done/workflows/execute-plan.md
@codexclaude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-conversational-ai-interface/10-CONTEXT.md
@.planning/phases/10-conversational-ai-interface/10-RESEARCH.md
@src/core/chat/router.py
@src/api/jobs/events.py
@src/api/v1/jobs/routes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create chat session/message/action persistence contracts</name>
  <files>
    - src/models/chat_session.py
    - src/models/chat_message.py
    - src/models/chat_action.py
    - migrations/versions/<new>_phase10_chat_foundation.py
    - src/api/v1/chat/schemas.py
  </files>
  <action>
Implement persisted chat primitives:
- sessions scoped to user/store
- messages with typed blocks and source metadata
- actions with deterministic lifecycle states

Persist context state on session:
- `at_door`: classify/clarify/propose only
- `in_house`: approved execution workflow active

Add idempotency keys for action apply requests to prevent duplicate execution on retries.
  </action>
  <verify>
- `python -m pytest -q tests/api/test_chat_contract.py`
  </verify>
  <done>
- Chat contract persistence is stable and execution-safe.
  </done>
</task>

<task type="auto">
  <name>Task 2: Expose /api/v1/chat endpoints with deterministic error behavior</name>
  <files>
    - src/api/v1/chat/__init__.py
    - src/api/v1/chat/routes.py
    - src/api/__init__.py
    - tests/api/test_chat_contract.py
  </files>
  <action>
Implement endpoints:
- create/get/list sessions
- list/post messages
- read action state

Return ProblemDetails-compatible machine-readable errors for authz, validation, and state-transition violations.
Ensure access is ownership-scoped and deny-by-default.
  </action>
  <verify>
- `python -m pytest -q tests/api/test_chat_contract.py`
- `python -m pytest -q tests/api/test_endpoints.py`
  </verify>
  <done>
- Chat APIs are authenticated, ownership-scoped, and contract-stable.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement chat SSE stream contract with context-safe generator handling</name>
  <files>
    - src/api/v1/chat/routes.py
    - src/api/core/sse.py
    - tests/api/test_chat_stream.py
  </files>
  <action>
Add SSE endpoint for chat session events:
- named events for assistant updates and action state changes
- deterministic payload schema for frontend hooks
- anti-buffering/no-cache headers for proxy-safe delivery

If request/session access is needed during streaming, wrap generator with `stream_with_context`.
  </action>
  <verify>
- `python -m pytest -q tests/api/test_chat_stream.py`
  </verify>
  <done>
- Chat streaming contract is robust and ready for UI integration.
  </done>
</task>

</tasks>

<verification>
- [ ] /api/v1/chat endpoints are authenticated and ownership-scoped
- [ ] Session state machine (`at_door`/`in_house`) is persisted and surfaced
- [ ] Typed message block schema is deterministic
- [ ] SSE contract works with named events and proxy-safe headers
</verification>

<success_criteria>
- Backend chat foundation is complete with stable contracts, stream transport, and action lineage safety.
</success_criteria>

<output>
After completion, create `.planning/phases/10-conversational-ai-interface/10-01-SUMMARY.md`
</output>
