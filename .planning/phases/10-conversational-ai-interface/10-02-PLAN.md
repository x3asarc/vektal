---
phase: 10-conversational-ai-interface
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
requirements:
  - CHAT-04
  - CHAT-07
  - CHAT-08
files_modified:
  - src/api/v1/chat/routes.py
  - src/api/v1/chat/schemas.py
  - src/api/v1/chat/orchestrator.py
  - src/api/v1/chat/approvals.py
  - src/api/v1/resolution/routes.py
  - src/resolution/shopify_graphql.py
  - frontend/src/shared/contracts/chat.ts
  - tests/api/test_chat_single_sku_workflow.py
autonomous: true

must_haves:
  truths:
    - "Single-SKU chat writes always go through dry-run before apply."
    - "Approval is product-scoped with selective field-level override support."
    - "Product creation defaults to draft-first and publish is explicit or policy-gated."
    - "Variant expansion uses dedicated variant bulk mutation paths, not implicit initial create assumptions."
  artifacts:
    - path: "src/api/v1/chat/orchestrator.py"
      provides: "Single-SKU orchestration from intent to dry-run and apply"
      contains: "prepare_single_sku_action"
    - path: "src/api/v1/chat/approvals.py"
      provides: "Product-scoped approval contracts"
      contains: "approve_product_action"
    - path: "tests/api/test_chat_single_sku_workflow.py"
      provides: "Safety and semantic workflow tests"
      contains: "requires_dry_run"
  key_links:
    - from: "src/api/v1/chat/orchestrator.py"
      to: "src/api/v1/resolution/routes.py"
      via: "dry-run/preflight/apply lifecycle"
      pattern: "/api/v1/resolution/dry-runs"
    - from: "src/api/v1/chat/orchestrator.py"
      to: "src/resolution/shopify_graphql.py"
      via: "draft-first create and variant bulk create path"
      pattern: "productVariantsBulkCreate"
---

<objective>
Implement single-SKU conversational orchestration with mandatory dry-run and product-scoped approvals.

Purpose: make chat-driven write operations explicit, auditable, and safe while retaining operator speed.
Output: deterministic single-SKU lifecycle from request -> review -> approval -> preflight -> apply.
</objective>

<execution_context>
@codexclaude/get-shit-done/workflows/execute-plan.md
@codexclaude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-conversational-ai-interface/10-CONTEXT.md
@.planning/phases/10-conversational-ai-interface/10-RESEARCH.md
@.planning/phases/08-product-resolution-engine/08-04-SUMMARY.md
@src/api/v1/resolution/routes.py
@src/models/recovery_log.py
@src/models/resolution_snapshot.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert single-SKU intents into dry-run action proposals</name>
  <files>
    - src/api/v1/chat/orchestrator.py
    - src/api/v1/chat/routes.py
    - src/api/v1/chat/schemas.py
    - tests/api/test_chat_single_sku_workflow.py
  </files>
  <action>
Implement deterministic single-SKU orchestration:
- parse SKU/URL/intent from chat message
- validate store ownership and action eligibility
- create dry-run proposal with before/after references
- emit structured action blocks for chat UI

Every mutating path must create a dry-run action record before apply is possible.
  </action>
  <verify>
- `python -m pytest -q tests/api/test_chat_single_sku_workflow.py`
  </verify>
  <done>
- Single-SKU prompts produce actionable dry-run proposals consistently.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enforce draft-first create semantics and variant-safe expansion</name>
  <files>
    - src/api/v1/chat/orchestrator.py
    - src/resolution/shopify_graphql.py
    - src/api/v1/chat/schemas.py
    - tests/api/test_chat_single_sku_workflow.py
  </files>
  <action>
Implement create/update semantics aligned to research constraints:
- new product creation defaults to draft-first behavior
- publish remains explicit or policy-gated, never implicit
- additional variants use dedicated variant bulk mutation path
- variant strategy respects quiz policy (`ask` default until rule exists)
  </action>
  <verify>
- tests assert draft-first create behavior
- tests assert explicit publish gate
- tests assert multi-variant flow routes through variant bulk path
  </verify>
  <done>
- Product create/update behavior is predictable and safe.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add product-scoped approval and apply gating with recovery references</name>
  <files>
    - src/api/v1/chat/approvals.py
    - src/api/v1/chat/routes.py
    - src/api/v1/chat/orchestrator.py
    - tests/api/test_chat_single_sku_workflow.py
  </files>
  <action>
Implement approval and execution gates:
- one approval per product action scope
- optional selective overrides and comments
- preflight enforcement immediately before apply
- conflict and stale-target handling linked to Recovery Logs

No mutation occurs when approval is absent or conflict requires user decision.
  </action>
  <verify>
- tests cover missing approval, conflict hold, successful apply, and recovery linkage
  </verify>
  <done>
- Single-SKU workflow is end-to-end safe and auditable.
  </done>
</task>

</tasks>

<verification>
- [ ] Dry-run required for all single-SKU mutating operations
- [ ] Product-scoped approvals enforced server-side
- [ ] Draft-first create and explicit publish gating enforced
- [ ] Conflict/low-confidence actions require explicit decision
</verification>

<success_criteria>
- CHAT-04 is operational with strict safety and approval semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/10-conversational-ai-interface/10-02-SUMMARY.md`
</output>
