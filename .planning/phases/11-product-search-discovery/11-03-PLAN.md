---
phase: 11-product-search-discovery
plan: 03
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
requirements:
  - SNAP-01
  - SNAP-02
  - SNAP-03
  - SNAP-04
  - REL-02
  - REL-03
files_modified:
  - src/models/resolution_snapshot.py
  - src/models/recovery_log.py
  - migrations/versions/<new>_phase11_snapshot_lifecycle.py
  - src/resolution/dry_run_compiler.py
  - src/resolution/preflight.py
  - src/resolution/apply_engine.py
  - src/resolution/snapshot_lifecycle.py
  - src/resolution/audit_export.py
  - src/resolution/progress_contract.py
  - src/api/v1/resolution/routes.py
  - src/api/v1/products/staging.py
  - tests/resolution/test_snapshot_lifecycle.py
  - tests/api/test_snapshot_chain_contract.py
  - tests/api/test_audit_export_contract.py
  - tests/api/test_apply_progress_contract.py
autonomous: true

must_haves:
  truths:
    - "Snapshot lifecycle is efficient and deterministic: baseline + batch manifest + touched-product pre-image."
    - "Snapshot writes are deduplicated by checksum with pointer reuse where payloads are identical."
    - "Dry-run TTL and preflight revalidation are enforced before apply."
    - "Audit retention/export contracts and recovery-chain traversal are machine-verifiable."
    - "Transient failures use bounded retry/backoff with deterministic defer-to-recovery behavior on exhaustion."
    - "Apply progress and terminal summaries are emitted with stable machine-readable contracts."
  artifacts:
    - path: "src/resolution/snapshot_lifecycle.py"
      provides: "baseline scheduling, dedupe, chain resolution helpers"
      contains: "resolve_snapshot_chain"
    - path: "src/models/resolution_snapshot.py"
      provides: "snapshot type and dedupe data model constraints"
      contains: "baseline"
    - path: "tests/resolution/test_snapshot_lifecycle.py"
      provides: "snapshot tiering and dedupe verification"
      contains: "checksum reuse"
  key_links:
    - from: "src/api/v1/products/staging.py"
      to: "src/resolution/snapshot_lifecycle.py"
      via: "dry-run TTL + preflight revalidation checks"
      pattern: "expires_at"
    - from: "src/api/v1/resolution/routes.py"
      to: "src/models/recovery_log.py"
      via: "recovery chain and export payload retrieval"
      pattern: "recovery-logs"
---

<objective>
Implement Phase 11 snapshot lifecycle optimization and reliability controls required for high-scale precision operations.

Purpose: enforce efficient storage, deterministic recovery, and auditable export contracts without sacrificing apply safety.
Output: baseline+delta snapshot lifecycle, dedupe, TTL/revalidation gating, and exportable audit chain contracts.
</objective>

<execution_context>
@codexclaude/get-shit-done/workflows/execute-plan.md
@codexclaude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-product-search-discovery/11-CONTEXT.md
@.planning/phases/11-product-search-discovery/11-RESEARCH.md
@src/models/resolution_snapshot.py
@src/resolution/dry_run_compiler.py
@src/resolution/preflight.py
@src/resolution/apply_engine.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend snapshot data model for baseline+delta lifecycle and dedupe</name>
  <files>
    - src/models/resolution_snapshot.py
    - migrations/versions/<new>_phase11_snapshot_lifecycle.py
    - src/resolution/snapshot_lifecycle.py
  </files>
  <action>
Add lifecycle capabilities:
- support periodic baseline snapshot type
- retain existing batch manifest and product pre-change snapshot semantics
- implement checksum-based dedupe and pointer reuse
- expose deterministic chain traversal helpers (`batch -> pre-image -> baseline`)
- persist sufficient lineage pointers for replay and export joins

Ensure schema constraints and indexes support efficient chain lookups at scale.
  </action>
  <verify>
- `python -m pytest -q tests/resolution/test_snapshot_lifecycle.py`
  </verify>
  <done>
- Snapshot storage is tiered and dedupe-aware without losing recovery determinism.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enforce dry-run TTL and preflight edge revalidation in staging/apply flow</name>
  <files>
    - src/api/v1/products/staging.py
    - src/resolution/preflight.py
    - src/resolution/apply_engine.py
    - tests/api/test_snapshot_chain_contract.py
  </files>
  <action>
Implement enforced freshness guards:
- dry-run TTL default of 60 minutes on staged mutation sets
- mandatory preflight revalidation immediately before apply
- hold conflicted/stale targets and route to Recovery Logs
- never bypass snapshot existence checks before mutating apply
- transient retry policy:
  - retry classes: 429, timeout, 5xx
  - exponential backoff + jitter
  - max attempts bounded at chunk scope
  - on exhaustion: deterministic deferred record with replay metadata
  </action>
  <verify>
- `python -m pytest -q tests/api/test_snapshot_chain_contract.py`
  </verify>
  <done>
- Apply readiness is freshness-validated and conflict-safe by contract.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add audit retention/export and recovery-chain API contracts</name>
  <files>
    - src/resolution/audit_export.py
    - src/resolution/progress_contract.py
    - src/api/v1/resolution/routes.py
    - src/models/recovery_log.py
    - tests/api/test_audit_export_contract.py
    - tests/api/test_apply_progress_contract.py
  </files>
  <action>
Provide export and chain visibility features:
- audit export endpoints for CSV and JSON
- batch manifest + per-product diff + actor/rule metadata in export payload
- recovery-chain retrieval endpoint for operational restore/debug workflows
- retention policy hooks and metadata fields needed for 24-month audit window
- apply progress contract endpoint/stream:
  - processed/total counters
  - ETA
  - current chunk/item metadata
  - terminal summary (`success`, `failed`, `deferred`, `retryable`)
  </action>
  <verify>
- `python -m pytest -q tests/api/test_audit_export_contract.py`
- `python -m pytest -q tests/api/test_apply_progress_contract.py`
  </verify>
  <done>
- Audit and recovery evidence is exportable, traceable, and policy-compliant.
  </done>
</task>

</tasks>

<verification>
- [ ] Baseline+delta snapshot lifecycle is implemented and dedupe-aware
- [ ] Dry-run TTL and preflight revalidation are enforced before apply
- [ ] Recovery chain traversal is deterministic and test-covered
- [ ] CSV/JSON audit export contracts are available with required fields
- [ ] Transient retry/backoff policy is bounded and routes exhausted items to recovery deterministically
- [ ] Apply progress and terminal summary contracts are test-covered
</verification>

<success_criteria>
- Phase 11 wave-3 capability is live: safe, efficient snapshot lifecycle and auditable recovery/export posture for precision operations.
</success_criteria>

<output>
After completion, create `.planning/phases/11-product-search-discovery/11-03-SUMMARY.md`
</output>
