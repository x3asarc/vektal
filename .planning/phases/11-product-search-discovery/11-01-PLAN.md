---
phase: 11-product-search-discovery
plan: 01
type: execute
wave: 1
depends_on: []
requirements:
  - SEARCH-01
  - SEARCH-02
  - SEARCH-03
files_modified:
  - src/api/v1/products/routes.py
  - src/api/v1/products/schemas.py
  - src/api/v1/products/search_query.py
  - src/models/product.py
  - frontend/src/app/(app)/search/page.tsx
  - frontend/src/features/search/components/SearchWorkspace.tsx
  - frontend/src/features/search/components/SearchResultGrid.tsx
  - frontend/src/features/search/api/search-api.ts
  - frontend/src/features/search/hooks/useSearchWorkspace.ts
  - tests/api/test_products_search_contract.py
  - frontend/src/app/(app)/search/page.test.tsx
autonomous: true

must_haves:
  truths:
    - "Users can search products by identifiers and textual/catalog fields using a deterministic filter contract."
    - "Search results support explicit scope semantics (visible, filtered, explicit) with stable selection behavior."
    - "Search result workspace renders production product cards/grid with configurable columns and protected-column indicators."
  artifacts:
    - path: "src/api/v1/products/routes.py"
      provides: "Search endpoint with cursor pagination and filter grammar"
      contains: "/api/v1/products/search"
    - path: "frontend/src/features/search/components/SearchWorkspace.tsx"
      provides: "Precision search/filter/scope UI"
      contains: "selection scope banner"
    - path: "tests/api/test_products_search_contract.py"
      provides: "Search query and cursor contract verification"
      contains: "filter + cursor"
  key_links:
    - from: "frontend/src/features/search/api/search-api.ts"
      to: "src/api/v1/products/routes.py"
      via: "query/filter/sort contract"
      pattern: "GET /api/v1/products/search"
---

<objective>
Build Phase 11 search foundation: multi-field product search, advanced filtering, and precision scope-aware grid workspace.

Purpose: deliver a production search control surface that is fast, explicit about blast radius, and ready for staging/diff workflows.
Output: `/search` route with real backend search contracts and deterministic selection scope behavior.
</objective>

<execution_context>
@codexclaude/get-shit-done/workflows/execute-plan.md
@codexclaude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-product-search-discovery/11-CONTEXT.md
@.planning/phases/11-product-search-discovery/11-RESEARCH.md
@src/api/v1/products/routes.py
@src/api/v1/products/schemas.py
@frontend/src/lib/auth/guards.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement backend multi-field search/filter contract with deterministic cursor behavior</name>
  <files>
    - src/api/v1/products/routes.py
    - src/api/v1/products/schemas.py
    - src/api/v1/products/search_query.py
  </files>
  <action>
Create `/api/v1/products/search` contract with:
- identifier filters (`sku`, `barcode`, `hs_code`, `vendor_code`)
- text and metadata filters (`title`, `tags`, `product_type`, `status`)
- numeric/range filters (`price`, `inventory_total` where available)
- sort contract with deterministic tie-breaker
- cursor/keyset pagination compatible with large datasets

Ensure filter parsing is explicit and safe (reject malformed operators with machine-readable validation errors).
  </action>
  <verify>
- `python -m pytest -q tests/api/test_products_search_contract.py`
  </verify>
  <done>
- Search API contract supports Phase 11 precision query surface with deterministic paging.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build `/search` workspace route and scope-aware search UI</name>
  <files>
    - frontend/src/app/(app)/search/page.tsx
    - frontend/src/features/search/components/SearchWorkspace.tsx
    - frontend/src/features/search/components/SearchResultGrid.tsx
    - frontend/src/features/search/api/search-api.ts
    - frontend/src/features/search/hooks/useSearchWorkspace.ts
  </files>
  <action>
Implement production search workspace:
- query + advanced filter controls
- results grid/cards with configurable columns
- explicit selection scope banner (`visible`, `filtered`, `explicit`)
- persistent in-session selection during filtering
- protected column visual markers

Keep keyboard-friendly interactions and predictable loading/empty/error states.
  </action>
  <verify>
- `cd frontend && npm.cmd run test -- "src/app/(app)/search/page.test.tsx"`
- `cd frontend && npm.cmd run typecheck`
  </verify>
  <done>
- `/search` is operational and exposes precision selection/scoping semantics for catalog operations.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add baseline contract tests for search/filter/scope parity</name>
  <files>
    - tests/api/test_products_search_contract.py
    - frontend/src/app/(app)/search/page.test.tsx
  </files>
  <action>
Add focused tests covering:
- combined filter behavior and malformed filter rejection
- cursor progression and sort determinism
- frontend scope indicator and selection persistence across filter changes
- backend/frontend contract shape alignment
  </action>
  <verify>
- `python -m pytest -q tests/api/test_products_search_contract.py`
- `cd frontend && npm.cmd run test -- "src/app/(app)/search/page.test.tsx"`
  </verify>
  <done>
- Phase 11 search foundation has executable evidence for API and UX contracts.
  </done>
</task>

</tasks>

<verification>
- [ ] Search API supports multi-field filters and deterministic cursor pagination
- [ ] `/search` route exists and renders precision workspace controls
- [ ] Selection scope semantics are explicit and stable in UI state
- [ ] Backend/frontend search contracts are test-covered
</verification>

<success_criteria>
- Phase 11 wave-1 capability is live: users can discover products quickly and safely define operation scope.
</success_criteria>

<output>
After completion, create `.planning/phases/11-product-search-discovery/11-01-SUMMARY.md`
</output>

