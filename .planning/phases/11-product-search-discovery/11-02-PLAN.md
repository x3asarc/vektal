---
phase: 11-product-search-discovery
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
requirements:
  - SEARCH-04
  - SEARCH-05
  - SEARCH-06
  - SEARCH-07
  - GOV-01
  - GOV-02
  - GOV-03
  - REL-01
files_modified:
  - src/models/product_change_event.py
  - src/models/vendor_field_mapping.py
  - src/models/__init__.py
  - migrations/versions/<new>_phase11_product_history_and_staging.py
  - src/api/v1/products/routes.py
  - src/api/v1/products/schemas.py
  - src/api/v1/products/staging.py
  - src/api/v1/vendors/routes.py
  - frontend/src/features/search/components/ProductDetailPanel.tsx
  - frontend/src/features/search/components/ProductDiffPanel.tsx
  - frontend/src/features/search/components/BulkActionBuilder.tsx
  - frontend/src/features/search/components/ApprovalBlockCard.tsx
  - frontend/src/features/search/hooks/useBulkStaging.ts
  - tests/api/test_products_history_diff_contract.py
  - tests/api/test_products_bulk_staging_contract.py
  - frontend/src/features/search/components/BulkActionBuilder.test.tsx
autonomous: true

must_haves:
  truths:
    - "Product detail endpoint returns complete product field payload for precision review."
    - "Product history and diff views expose clear before/after lineage for operator trust."
    - "Bulk operations are staged as semantic action blocks and reviewed through dry-run diffs before apply."
    - "Approval scope is action-block with per-product include/exclude overrides."
    - "Admission controller gates (schema, policy, conflict, eligibility) are enforced before apply eligibility."
    - "Vendor field mappings are versioned and unmapped required fields block dry-run completion."
    - "Alt-text preservation defaults are explicit, with overwrite only via explicit rule/action approval."
  artifacts:
    - path: "src/api/v1/products/routes.py"
      provides: "detail/history/diff/staging API contracts"
      contains: "/api/v1/products/{id}/history"
    - path: "src/api/v1/products/staging.py"
      provides: "semantic bulk action staging orchestration"
      contains: "action blocks"
    - path: "frontend/src/features/search/components/BulkActionBuilder.tsx"
      provides: "operation-first bulk staging UX"
      contains: "set/add/remove/increase/decrease"
  key_links:
    - from: "src/api/v1/products/staging.py"
      to: "src/api/v1/resolution/routes.py"
      via: "dry-run and preflight safety orchestration reuse"
      pattern: "create_dry_run_batch_for_user"
---

<objective>
Deliver product detail lineage and precision staging workflows: history, side-by-side diff, and action-block bulk approval surfaces.

Purpose: move from search-only discovery to safe, explainable mutation preparation.
Output: users can inspect product history and build semantic bulk actions with mandatory dry-run review.
</objective>

<execution_context>
@codexclaude/get-shit-done/workflows/execute-plan.md
@codexclaude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-product-search-discovery/11-CONTEXT.md
@.planning/phases/11-product-search-discovery/11-RESEARCH.md
@src/api/v1/resolution/routes.py
@src/models/recovery_log.py
@src/models/resolution_snapshot.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add product history/lineage model and contracts</name>
  <files>
    - src/models/product_change_event.py
    - src/models/__init__.py
    - migrations/versions/<new>_phase11_product_history_and_staging.py
    - src/api/v1/products/routes.py
    - src/api/v1/products/schemas.py
  </files>
  <action>
Introduce persistent change-event lineage for products:
- actor/source metadata
- before/after payload snapshots (or field-level deltas)
- linkage to action/rule/batch identifiers where available
- actor and source attribution fields suitable for long-term audit exports

Expose APIs for:
- full product detail
- product history timeline
- version-to-version diff retrieval
  </action>
  <verify>
- `python -m pytest -q tests/api/test_products_history_diff_contract.py`
  </verify>
  <done>
- Detail/history/diff contracts exist and provide explainable product lineage for operators.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement semantic bulk action staging + dry-run compile contracts</name>
  <files>
    - src/models/vendor_field_mapping.py
    - src/api/v1/products/staging.py
    - src/api/v1/products/routes.py
    - src/api/v1/products/schemas.py
    - src/api/v1/vendors/routes.py
    - tests/api/test_products_bulk_staging_contract.py
  </files>
  <action>
Implement staging endpoints that accept:
- frozen selection snapshot
- semantic action blocks (`set`, `replace`, `add`, `remove`, `clear`, `increase/decrease`, `conditional_set`)

Compile staged actions through existing resolution safety stack:
- dry-run generation
- conflict classification
- action-block approval metadata
- per-product include/exclude override support
- admission controller output (`schema_ok`, `policy_ok`, `conflict_state`, `eligible_to_apply`)

Add vendor mapping governance:
- versioned mapping lookup by `store + supplier + field_group`
- deterministic transform path (`supplier raw -> canonical -> Shopify payload`)
- required-field mapping gaps block dry-run with machine-readable remediation payload

Do not add a mutation path that bypasses existing preflight/apply safety.
  </action>
  <verify>
- `python -m pytest -q tests/api/test_products_bulk_staging_contract.py`
  </verify>
  <done>
- Precision bulk operations are staged and reviewed safely before apply.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build detail/diff and action-block approval UX surfaces</name>
  <files>
    - frontend/src/features/search/components/ProductDetailPanel.tsx
    - frontend/src/features/search/components/ProductDiffPanel.tsx
    - frontend/src/features/search/components/BulkActionBuilder.tsx
    - frontend/src/features/search/components/ApprovalBlockCard.tsx
    - frontend/src/features/search/hooks/useBulkStaging.ts
    - frontend/src/features/search/components/BulkActionBuilder.test.tsx
  </files>
  <action>
Add operator UI for:
- product detail side panel (all fields)
- history timeline and side-by-side diffs with risk badges
- semantic operation builder
- action-block approval cards with per-product override controls
- mapping status and admission outcomes (pass/fail by gate)
- protected field lock indicators inside staging/approval surfaces
- alt-text diff row semantics (`preserved`, `candidate`, `approved overwrite`)

Surface explicit conflict and blast-radius metadata before any apply transition.
  </action>
  <verify>
- `cd frontend && npm.cmd run test -- "src/features/search/components/BulkActionBuilder.test.tsx"`
- `cd frontend && npm.cmd run typecheck`
  </verify>
  <done>
- Operators can inspect lineage and stage/approve bulk changes with deterministic UX.
  </done>
</task>

</tasks>

<verification>
- [ ] Product detail endpoint returns full product payload
- [ ] Product history/diff contracts are available and test-covered
- [ ] Bulk staging accepts semantic operations and frozen scope snapshots
- [ ] Action-block approvals support per-product include/exclude overrides
- [ ] Admission controller gates are exposed and enforced before apply eligibility
- [ ] Vendor mapping gaps block dry-run and return remediation data
- [ ] Alt-text overwrite only occurs through explicit approval path
</verification>

<success_criteria>
- Phase 11 wave-2 capability is live: explainable, approval-gated mutation staging from search workspace.
</success_criteria>

<output>
After completion, create `.planning/phases/11-product-search-discovery/11-02-SUMMARY.md`
</output>
