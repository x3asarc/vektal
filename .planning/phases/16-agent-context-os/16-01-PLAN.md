---
phase: 16-agent-context-os
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/memory/event_schema.py
  - src/memory/event_log.py
  - scripts/memory/record_event.py
  - tests/unit/test_memory_event_log.py
autonomous: true

must_haves:
  truths:
    - "Append-only event stream is the canonical source of truth for memory state"
    - "Every event written includes provenance, timestamp, session metadata, and schema version"
    - "Event writes are deterministic and safe under concurrent terminal activity"
    - "Invalid events are rejected with explicit validation errors"
  artifacts:
    - path: "src/memory/event_schema.py"
      provides: "Typed event contracts and validation"
      exports: ["EventEnvelope", "EventType", "validate_event"]
    - path: "src/memory/event_log.py"
      provides: "Append-only event writer/reader"
      exports: ["append_event", "iter_events", "event_log_path_for_day"]
    - path: "scripts/memory/record_event.py"
      provides: "CLI entrypoint for manual and hook-driven writes"
  key_links:
    - from: "scripts/memory/record_event.py"
      to: "src/memory/event_log.py"
      via: "Canonical write path"
      pattern: "append_event"
---

<objective>
Create the Phase 16 source-of-truth event layer that all later memory/context features consume.
</objective>

<execution_context>
@C:/Users/Hp/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/16-agent-context-os/16-EXECUTION-CONTRACT.md
</execution_context>

<context>
@.planning/phases/16-agent-context-os/16-PLAN.md
@.planning/phases/16-agent-context-os/16-RESEARCH.md
@.planning/memory-system-design.md
@src/memory/memory_manager.py
@scripts/memory/pre_tool_update.py
</context>

<tasks>

<task type="auto">
  <name>Create typed event schema and validator</name>
  <files>src/memory/event_schema.py</files>
  <action>
Implement strongly-typed memory event contracts:
1. Event types for lifecycle and memory transitions:
   - session_start, pre_tool, post_tool, session_end, task_complete, phase_complete
   - context_refresh, graph_retrieval, fallback_retrieval
2. Envelope fields (required):
   - event_id, event_type, created_at, schema_version, source, provider, session_id
   - scope (project/phase/task), payload, provenance
3. Validation rules:
   - required fields must exist and be non-empty
   - payload size limits to avoid event bloat
   - stable schema_version enforcement
4. Utility for generating canonical event_id and timestamp values.
  </action>
  <verify>python -c "from src.memory.event_schema import EventType, validate_event; print('ok')"</verify>
  <done>Typed schema exists with deterministic validation behavior</done>
</task>

<task type="auto">
  <name>Implement append-only event log primitives</name>
  <files>src/memory/event_log.py</files>
  <action>
Implement append/read primitives over `.memory/events/YYYY-MM-DD.jsonl`:
1. `append_event(envelope)` with atomic append and flush.
2. `iter_events(...)` filters by time range, session, and event_type.
3. deterministic file layout and automatic directory bootstrap.
4. fail-open mode for hooks with explicit warning path.
5. minimal lock strategy to support concurrent terminals safely.
6. helper metrics:
   - write_duration_ms
   - write_success/failure
   - bytes_written
  </action>
  <verify>python -c "from src.memory.event_log import append_event, iter_events; print('ok')"</verify>
  <done>Append-only event log works and is safe under concurrent session writes</done>
</task>

<task type="auto">
  <name>Create CLI writer and hook-ready adapter</name>
  <files>scripts/memory/record_event.py</files>
  <action>
Create CLI for controlled event writes:
1. accepts event_type, provider, session_key, scope, payload_json.
2. writes via `append_event` only (single canonical path).
3. prints compact success/failure message for hook logging.
4. supports dry-run validation mode for testing.
  </action>
  <verify>python scripts/memory/record_event.py --event-type session_start --provider codex --session-key test --scope '{\"phase\":\"16\"}' --payload '{\"message\":\"smoke\"}' --dry-run</verify>
  <done>CLI can validate and write events through the canonical schema pipeline</done>
</task>

<task type="auto">
  <name>Add unit tests for schema and append-only behavior</name>
  <files>tests/unit/test_memory_event_log.py</files>
  <action>
Add tests for:
1. valid events pass schema validation.
2. missing required fields fail validation.
3. append + read round-trip.
4. concurrent append smoke (multiple writes produce valid JSONL lines).
5. payload size limit enforcement.
  </action>
  <verify>pytest -q tests/unit/test_memory_event_log.py</verify>
  <done>Event-layer contracts are test-covered and deterministic</done>
</task>

</tasks>

<verification>
1. `pytest -q tests/unit/test_memory_event_log.py` passes.
2. `scripts/memory/record_event.py` dry-run and write modes both work.
3. `.memory/events/YYYY-MM-DD.jsonl` receives valid envelopes.
4. Event schema version is present on every written event.
</verification>

<success_criteria>
- Event layer is the only write path for lifecycle memory events.
- Event files are append-only, parseable JSONL, and concurrency-safe.
- APIs are exported for downstream materializers.
</success_criteria>

<output>
After completion, create:
1. `.planning/phases/16-agent-context-os/16-01-SUMMARY.md`
2. `.planning/phases/16-agent-context-os/16-01-to-16-02-REPLAN.md`

The replan file must define concrete replacements for plan 02 placeholders:
- `[[UPSTREAM_EVENT_API]]`
- `[[UPSTREAM_EVENT_TYPES]]`
- `[[UPSTREAM_EVENT_PATH_PATTERN]]`
</output>

