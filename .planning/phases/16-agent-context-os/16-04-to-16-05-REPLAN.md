# Replan: 16-04 -> 16-05

Status: READY  
Date: 2026-03-03

## Upstream Deliverables Consumed

1. `src/assistant/context_broker.py`
2. `scripts/context/context_query_probe.py`
3. `16-04-SUMMARY.md`

## Placeholder Resolution for 16-05

1. `[[UPSTREAM_BROKER_API]]` ->
   - `src.assistant.context_broker.assemble_context`
2. `[[UPSTREAM_TELEMETRY_FIELDS]]` ->
   - `graph_attempted`
   - `graph_used`
   - `fallback_used`
   - `fallback_reason`
   - `latency_ms`
   - `assembled_tokens`
   - `query_class`
   - `target_tokens`
   - `hard_cap_tokens`
   - `compaction_applied`
3. `[[UPSTREAM_CONTEXT_PROBE_COMMAND]]` ->
   - `python scripts/context/context_query_probe.py --query "<text>"`

## Required Edits to 16-05 Before Execution

1. Replace broker import/API placeholders in key links and dependency gate.
2. Ensure hook payloads include required broker telemetry fields.
3. Keep unresolved-placeholder gate as `RED`.

## Execution Note

16-05 is approved to start after placeholder replacement is applied in `16-05-PLAN.md`.

