# Replan: 16-01 -> 16-02

Status: READY  
Date: 2026-03-03

## Upstream Deliverables Consumed

1. `src/memory/event_schema.py`
2. `src/memory/event_log.py`
3. `.memory/events/YYYY-MM-DD.jsonl` path contract
4. `16-01-SUMMARY.md` exported API list

## Placeholder Resolution for 16-02

1. `[[UPSTREAM_EVENT_API]]` ->
   - `src.memory.event_log.iter_events`
   - `src.memory.event_log.append_event`
   - `src.memory.event_schema.create_event`
   - `src.memory.event_schema.validate_event`
2. `[[UPSTREAM_EVENT_TYPES]]` ->
   - `src.memory.event_schema.EventType`
   - values:
     - `session_start`
     - `pre_tool`
     - `post_tool`
     - `session_end`
     - `task_complete`
     - `phase_complete`
     - `context_refresh`
     - `graph_retrieval`
     - `fallback_retrieval`
3. `[[UPSTREAM_EVENT_PATH_PATTERN]]` ->
   - `.memory/events/YYYY-MM-DD.jsonl`

## Required Edits to 16-02 Before Execution

1. Replace placeholder references in `key_links` and task descriptions with concrete imports above.
2. Set replay input source in materializers to `iter_events(...)`.
3. Keep binary gate: `RED` if any unresolved placeholder remains.

## Execution Note

16-02 is now approved to start after placeholder replacement is applied in `16-02-PLAN.md`.

