---
phase: 16-agent-context-os
plan: 05
type: execute
wave: 3
depends_on: ["16-04"]
files_modified:
  - scripts/memory/session_start.py
  - scripts/memory/pre_tool_update.py
  - scripts/memory/session_end.py
  - scripts/memory/task_complete.py
  - scripts/memory/phase_complete.py
  - .codex/settings.json
  - .claude/settings.json
  - .gemini/settings.json
  - .codex/preToolUseHook.sh
  - tests/unit/test_memory_hook_lifecycle.py
autonomous: true

must_haves:
  truths:
    - "Lifecycle hooks write events via canonical event API and never block command flow"
    - "Cross-terminal memory visibility updates within one command cycle or daemon tick"
    - "SessionStart refresh regenerates onboarding docs when stale or SHA changes"
  artifacts:
    - path: "scripts/memory/pre_tool_update.py"
      provides: "Low-latency event append on command activity"
    - path: "scripts/memory/session_start.py"
      provides: "Hydration + doc refresh at session bootstrap"
    - path: "tests/unit/test_memory_hook_lifecycle.py"
      provides: "Lifecycle integration and latency contract tests"
  key_links:
    - from: "scripts/memory/pre_tool_update.py"
      to: "src/assistant/context_broker.py::assemble_context"
      via: "Records broker-relevant context request metadata"
      pattern: "graph_attempted|fallback_used"
---

<objective>
Integrate full lifecycle hooks so memory and context stay live across sessions and terminals.
</objective>

<execution_context>
@C:/Users/Hp/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/16-agent-context-os/16-EXECUTION-CONTRACT.md
</execution_context>

<context>
@.planning/phases/16-agent-context-os/16-PLAN.md
@.planning/phases/16-agent-context-os/16-RESEARCH.md
@.planning/phases/16-agent-context-os/16-04-SUMMARY.md
@.planning/phases/16-agent-context-os/16-04-to-16-05-REPLAN.md
@.codex/preToolUseHook.sh
</context>

<adaptive_dependency_gate>
Upstream references resolved from `16-04-SUMMARY.md` and `16-04-to-16-05-REPLAN.md`:
1. Broker API:
   - `src.assistant.context_broker.assemble_context`
2. Telemetry fields:
   - `graph_attempted`, `graph_used`, `fallback_used`, `fallback_reason`
   - `latency_ms`, `assembled_tokens`, `query_class`
   - `target_tokens`, `hard_cap_tokens`, `compaction_applied`
3. Probe command:
   - `python scripts/context/context_query_probe.py --query \"<text>\"`
</adaptive_dependency_gate>

<tasks>

<task type="auto">
  <name>Wire lifecycle scripts to canonical event + materialization pipeline</name>
  <files>scripts/memory/session_start.py, scripts/memory/pre_tool_update.py, scripts/memory/session_end.py, scripts/memory/task_complete.py, scripts/memory/phase_complete.py</files>
  <action>
Update lifecycle hooks:
1. SessionStart:
   - append session_start event
   - hydrate working/short/long views
   - refresh `AGENT_START_HERE.md` and summaries if stale or git SHA changed
2. PreTool:
   - append pre_tool event via canonical writer
   - keep write path p95 under 20ms local target
3. Post-like flows (session_end/task_complete/phase_complete):
   - append outcome events
   - trigger materializer updates
4. all hooks remain fail-open and non-blocking.
  </action>
  <verify>python scripts/memory/session_start.py && python scripts/memory/pre_tool_update.py --provider codex --session-key test-16 --window-hint test --payload '{}' </verify>
  <done>Lifecycle scripts consistently use event pipeline and refresh contract</done>
</task>

<task type="auto">
  <name>Provider wiring for always-on hook behavior</name>
  <files>.codex/settings.json, .claude/settings.json, .gemini/settings.json, .codex/preToolUseHook.sh</files>
  <action>
Ensure provider settings include:
1. SessionStart memory bootstrap.
2. PreTool event write/update flow.
3. best-effort non-blocking invocation semantics.
4. consistent session-key strategy for cross-terminal visibility.
  </action>
  <verify>rg -n \"session_start.py|pre_tool_update.py\" .codex .claude .gemini -S</verify>
  <done>All configured providers invoke lifecycle hooks consistently</done>
</task>

<task type="auto">
  <name>Add lifecycle and latency tests</name>
  <files>tests/unit/test_memory_hook_lifecycle.py</files>
  <action>
Add tests for:
1. lifecycle event coverage (start, pre_tool, end, task, phase).
2. cross-terminal sync visibility using distinct session keys.
3. non-blocking behavior under event write failure.
4. pre-tool local write latency contract assertion.
  </action>
  <verify>pytest -q tests/unit/test_memory_hook_lifecycle.py</verify>
  <done>Hook lifecycle behavior is testable and aligned to latency/non-blocking goals</done>
</task>

</tasks>

<verification>
1. lifecycle events are appended across all expected hook points.
2. provider configs include hook scripts for codex/claude/gemini.
3. cross-terminal state visibility works in expected time window.
</verification>

<success_criteria>
- Memory system is live and always updating during normal terminal usage.
- Hooks do not block user workflows.
- SessionStart keeps onboarding docs fresh.
</success_criteria>

<output>
After completion, create:
1. `.planning/phases/16-agent-context-os/16-05-SUMMARY.md`
2. `.planning/phases/16-agent-context-os/16-05-to-16-06-REPLAN.md`

The replan file must define concrete replacements for plan 06 placeholders:
- `[[UPSTREAM_HOOK_METRICS_PATH]]`
- `[[UPSTREAM_EVENT_HEALTH_SIGNALS]]`
- `[[UPSTREAM_REFRESH_COMMANDS]]`
</output>
