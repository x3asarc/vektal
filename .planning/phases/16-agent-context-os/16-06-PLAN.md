---
phase: 16-agent-context-os
plan: 06
type: execute
wave: 3
depends_on: ["16-05"]
files_modified:
  - scripts/governance/context_os_gate.py
  - scripts/governance/context_os_report.py
  - docs/runbooks/context-os-operations.md
  - tests/unit/test_context_os_gate.py
autonomous: true

must_haves:
  truths:
    - "Phase 16 health must be gateable as GREEN or RED using explicit metrics"
    - "Gate checks context freshness, graph-first attempt rate, token budgets, and hook latency"
    - "Gate reports include reason codes and remediation hints"
  artifacts:
    - path: "scripts/governance/context_os_gate.py"
      provides: "Binary gate evaluator for Context OS"
      exports: ["run_gate", "GateResult"]
    - path: "scripts/governance/context_os_report.py"
      provides: "Human-readable and JSON gate report emitter"
    - path: "docs/runbooks/context-os-operations.md"
      provides: "Operational runbook for failures and degraded mode"
  key_links:
    - from: "scripts/governance/context_os_gate.py"
      to: ".memory/events/*.jsonl, .memory/working/*.json, .memory/materializers/checkpoint.json"
      via: "Reads lifecycle and broker telemetry"
      pattern: "graph_attempted|assembled_tokens|write_duration_ms"
---

<objective>
Create governance gates and runbooks so Context OS can be operated with auditable GREEN/RED outcomes.
</objective>

<execution_context>
@C:/Users/Hp/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/16-agent-context-os/16-EXECUTION-CONTRACT.md
</execution_context>

<context>
@.planning/phases/16-agent-context-os/16-PLAN.md
@.planning/phases/16-agent-context-os/16-RESEARCH.md
@.planning/phases/16-agent-context-os/16-05-SUMMARY.md
@.planning/phases/16-agent-context-os/16-05-to-16-06-REPLAN.md
@scripts/governance/health_gate.py
</context>

<adaptive_dependency_gate>
Resolved from `16-05-SUMMARY.md` and `16-05-to-16-06-REPLAN.md`:
1. Hook metrics paths:
   - `.memory/events/YYYY-MM-DD.jsonl`
   - `.memory/working/{session_id}.json`
   - `.memory/materializers/checkpoint.json`
2. Event health signals:
   - `event_type`, `created_at`, `provider`, `session_id`
   - `payload.broker_telemetry.graph_attempted`
   - `payload.broker_telemetry.graph_used`
   - `payload.broker_telemetry.fallback_used`
   - `payload.broker_telemetry.fallback_reason`
   - `payload.broker_telemetry.latency_ms`
   - `payload.broker_telemetry.assembled_tokens`
   - `event_write.ok`, `event_write.write_duration_ms`
3. Refresh commands:
   - `python scripts/memory/session_start.py`
   - `python scripts/context/build_agent_primer.py`
   - `python scripts/context/build_folder_summaries.py`
   - `python scripts/context/build_context_link_map.py`
4. Unresolved placeholders -> `RED`.
</adaptive_dependency_gate>

<tasks>

<task type="auto">
  <name>Implement Context OS binary gate evaluator</name>
  <files>scripts/governance/context_os_gate.py</files>
  <action>
Implement gate logic with explicit thresholds:
1. single-entrypoint freshness:
   - `docs/AGENT_START_HERE.md` and summary docs freshness <= 24h or regenerated this session.
2. graph-first attempt rate:
   - target >= 95% for context retrieval calls.
3. token budget:
   - median <= 2500 and p95 <= 4000.
4. hook latency:
   - pre-tool write p95 < 20ms, blocking incidents = 0.
5. cross-terminal visibility:
   - update seen within one command cycle or <= 5s configured tick.
6. output binary result with failing reasons.
  </action>
  <verify>python scripts/governance/context_os_gate.py --window-hours 24</verify>
  <done>Context OS can be evaluated with deterministic GREEN/RED output</done>
</task>

<task type="auto">
  <name>Create gate report command and operational runbook</name>
  <files>scripts/governance/context_os_report.py, docs/runbooks/context-os-operations.md</files>
  <action>
Create operator-facing outputs:
1. machine report JSON with per-metric pass/fail.
2. markdown summary report with remediation actions.
3. runbook sections:
   - degraded graph mode
   - stale-doc recovery
   - hook latency spike triage
   - fallback-rate spike triage
  </action>
  <verify>python scripts/governance/context_os_report.py --format markdown</verify>
  <done>Operators can diagnose and recover Context OS failures quickly</done>
</task>

<task type="auto">
  <name>Add gate tests and failure-path coverage</name>
  <files>tests/unit/test_context_os_gate.py</files>
  <action>
Add tests for:
1. all metrics passing => GREEN.
2. any critical metric miss => RED.
3. stale docs trigger RED with explicit reason.
4. graph-attempt rate below threshold triggers RED.
5. token and latency threshold checks are correctly computed.
  </action>
  <verify>pytest -q tests/unit/test_context_os_gate.py</verify>
  <done>Gate behavior is deterministic and fully test-covered</done>
</task>

</tasks>

<verification>
1. Gate command returns binary status and structured reasons.
2. Reports are produced in JSON and markdown formats.
3. Runbook exists with concrete recovery steps.
</verification>

<success_criteria>
- Context OS health becomes enforceable, not qualitative.
- Failing metrics map to clear remediation guidance.
- Governance artifacts support auditable phase closure.
</success_criteria>

<output>
After completion, create:
1. `.planning/phases/16-agent-context-os/16-06-SUMMARY.md`
2. `.planning/phases/16-agent-context-os/16-06-to-16-07-REPLAN.md`

The replan file must define concrete replacements for plan 07 placeholders:
- `[[UPSTREAM_GATE_COMMAND]]`
- `[[UPSTREAM_METRICS_SCHEMA]]`
- `[[UPSTREAM_RUNBOOK_PATH]]`
</output>
