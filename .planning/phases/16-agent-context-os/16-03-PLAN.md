---
phase: 16-agent-context-os
plan: 03
type: execute
wave: 2
depends_on: ["16-02"]
files_modified:
  - scripts/context/build_agent_primer.py
  - scripts/context/build_folder_summaries.py
  - scripts/context/build_context_link_map.py
  - docs/AGENT_START_HERE.md
  - docs/FOLDER_SUMMARIES.md
  - docs/CONTEXT_LINK_MAP.md
  - tests/unit/test_context_doc_generators.py
autonomous: true

must_haves:
  truths:
    - "New-agent onboarding starts from one canonical file"
    - "Folder summaries are generated from stable metadata, not full recursive reads every run"
    - "Generated docs include freshness timestamp and source provenance"
  artifacts:
    - path: "scripts/context/build_agent_primer.py"
      provides: "Canonical AGENT_START_HERE doc generator"
      exports: ["build_agent_primer"]
    - path: "scripts/context/build_folder_summaries.py"
      provides: "Top-level folder summary generator"
      exports: ["build_folder_summaries"]
    - path: "scripts/context/build_context_link_map.py"
      provides: "Cross-link map of key plans/docs/runtime files"
      exports: ["build_context_link_map"]
  key_links:
    - from: "scripts/context/build_agent_primer.py"
      to: "src/memory/materializers.py::build_working_view|build_short_term_view|build_long_term_index"
      via: "Reads materialized memory views for primer composition"
      pattern: "build_working_view|build_short_term_view|build_long_term_index"
---

<objective>
Create the single canonical onboarding doc and lightweight folder summary map for fast agent orientation.
</objective>

<execution_context>
@C:/Users/Hp/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/16-agent-context-os/16-EXECUTION-CONTRACT.md
</execution_context>

<context>
@.planning/phases/16-agent-context-os/16-PLAN.md
@.planning/phases/16-agent-context-os/16-RESEARCH.md
@.planning/phases/16-agent-context-os/16-02-SUMMARY.md
@.planning/phases/16-agent-context-os/16-02-to-16-03-REPLAN.md
@docs/MASTER_MAP.md
</context>

<adaptive_dependency_gate>
Upstream references resolved from `16-02-SUMMARY.md` and `16-02-to-16-03-REPLAN.md`:
1. Materializer APIs:
   - `src.memory.materializers.build_working_view`
   - `src.memory.materializers.build_short_term_view`
   - `src.memory.materializers.build_long_term_index`
   - `src.memory.materializers.discover_event_days`
   - `src.memory.materializers.discover_sessions`
2. View paths:
   - `.memory/working/{session_id}.json`
   - `.memory/short-term/{date}.jsonl`
   - `.memory/long-term/index.json`
3. View metrics fields:
   - `mode`, `timestamp`, `days`, `sessions`, `event_count_total`, `changed_views`
</adaptive_dependency_gate>

<tasks>

<task type="auto">
  <name>Create AGENT_START_HERE generator</name>
  <files>scripts/context/build_agent_primer.py, docs/AGENT_START_HERE.md</files>
  <action>
Implement generator for `docs/AGENT_START_HERE.md`:
1. include current phase/task/status and blockers.
2. include top links to plans/runtime/tests/governance docs.
3. include memory snapshot section from `.memory/working/{session_id}.json`, `.memory/short-term/{date}.jsonl`, and `.memory/long-term/index.json`.
4. include last_refreshed timestamp and source commit sha.
5. output remains compact and token-efficient for session priming.
  </action>
  <verify>python scripts/context/build_agent_primer.py --dry-run</verify>
  <done>Canonical onboarding doc is generated from current runtime/planning state</done>
</task>

<task type="auto">
  <name>Create folder summary and context link map generators</name>
  <files>scripts/context/build_folder_summaries.py, scripts/context/build_context_link_map.py, docs/FOLDER_SUMMARIES.md, docs/CONTEXT_LINK_MAP.md</files>
  <action>
Implement two generators:
1. `FOLDER_SUMMARIES.md`:
   - coverage for `src/`, `scripts/`, `.planning/`, `docs/`, `tests/`, `ops/`
   - each row includes purpose, key files, owner role, volatility, last verified
2. `CONTEXT_LINK_MAP.md`:
   - deterministic map of critical plan -> code -> tests -> reports links
3. apply bounded file-system scanning and metadata caching to avoid heavy reads.
  </action>
  <verify>python scripts/context/build_folder_summaries.py --dry-run && python scripts/context/build_context_link_map.py --dry-run</verify>
  <done>Folder summaries and link map are generated and refreshable</done>
</task>

<task type="auto">
  <name>Add tests for doc generation and freshness markers</name>
  <files>tests/unit/test_context_doc_generators.py</files>
  <action>
Add tests for:
1. required sections exist in each generated doc.
2. all six core directories are present in folder summary output.
3. freshness and source fields are present and parseable.
4. repeated generation is stable when input unchanged.
  </action>
  <verify>pytest -q tests/unit/test_context_doc_generators.py</verify>
  <done>Generated context docs are stable, complete, and test-verified</done>
</task>

</tasks>

<verification>
1. `AGENT_START_HERE.md` is generated and readable as a primary entrypoint.
2. Folder summaries cover all required core directories.
3. Context link map resolves key lifecycle files and plan artifacts.
</verification>

<success_criteria>
- One file can orient a new agent in under two minutes.
- Summary docs are regenerated without expensive full-content ingestion.
- Freshness/provenance metadata is explicit.
</success_criteria>

<output>
After completion, create:
1. `.planning/phases/16-agent-context-os/16-03-SUMMARY.md`
2. `.planning/phases/16-agent-context-os/16-03-to-16-04-REPLAN.md`

The replan file must define concrete replacements for plan 04 placeholders:
- `[[UPSTREAM_PRIMER_GENERATOR]]`
- `[[UPSTREAM_SUMMARY_DOC_PATHS]]`
- `[[UPSTREAM_LINKMAP_FIELDS]]`
</output>
