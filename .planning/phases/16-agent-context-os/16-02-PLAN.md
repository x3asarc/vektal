---
phase: 16-agent-context-os
plan: 02
type: execute
wave: 1
depends_on: ["16-01"]
files_modified:
  - src/memory/materializers.py
  - scripts/memory/materialize_views.py
  - tests/unit/test_memory_materializers.py
autonomous: true

must_haves:
  truths:
    - "Working, short-term, and long-term memory are materialized views derived from events"
    - "Materialization is deterministic and replayable from append-only logs"
    - "No direct writes to view files bypass event replay logic"
  artifacts:
    - path: "src/memory/materializers.py"
      provides: "Deterministic view builders for working/short/long memory"
      exports: ["build_working_view", "build_short_term_view", "build_long_term_index"]
    - path: "scripts/memory/materialize_views.py"
      provides: "CLI to rebuild views from event logs"
    - path: "tests/unit/test_memory_materializers.py"
      provides: "Replay determinism and view integrity tests"
  key_links:
    - from: "src/memory/materializers.py"
      to: "src/memory/event_log.py::iter_events"
      via: "Event replay input source"
      pattern: "iter_events|append_event"
---

<objective>
Build deterministic memory materializers on top of plan 01 event primitives.
</objective>

<execution_context>
@C:/Users/Hp/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/16-agent-context-os/16-EXECUTION-CONTRACT.md
</execution_context>

<context>
@.planning/phases/16-agent-context-os/16-PLAN.md
@.planning/phases/16-agent-context-os/16-RESEARCH.md
@.planning/phases/16-agent-context-os/16-01-SUMMARY.md
@.planning/phases/16-agent-context-os/16-01-to-16-02-REPLAN.md
@src/memory/memory_manager.py
</context>

<adaptive_dependency_gate>
Upstream references resolved from `16-01-SUMMARY.md` and `16-01-to-16-02-REPLAN.md`:
1. Event API imports:
   - `src.memory.event_log.iter_events`
   - `src.memory.event_log.append_event`
   - `src.memory.event_schema.create_event`
   - `src.memory.event_schema.validate_event`
2. Event types source:
   - `src.memory.event_schema.EventType`
3. Event path pattern:
   - `.memory/events/YYYY-MM-DD.jsonl`
</adaptive_dependency_gate>

<tasks>

<task type="auto">
  <name>Implement replay-based materializers</name>
  <files>src/memory/materializers.py</files>
  <action>
Create deterministic materialization logic using event APIs from `src.memory.event_log` and `src.memory.event_schema`:
1. Working view builder:
   - input: session_id
   - output: `.memory/working/{session_id}.json`
   - derives current task, recent files/commands, active context
2. Short-term view builder:
   - input: day/task filters
   - output: `.memory/short-term/{date}.jsonl`
   - compacts relevant events into structured task/day entries
3. Long-term index builder:
   - output: `.memory/long-term/index.json`
   - promotes validated stable patterns/decisions with provenance
4. Determinism:
   - same event slice must always produce byte-equivalent output.
5. Provenance:
   - every materialized record retains source event references.
  </action>
  <verify>python -c "from src.memory.materializers import build_working_view, build_short_term_view, build_long_term_index; print('ok')"</verify>
  <done>Replay-based view builders exist for all three memory tiers</done>
</task>

<task type="auto">
  <name>Create materialization CLI for full and incremental rebuilds</name>
  <files>scripts/memory/materialize_views.py</files>
  <action>
Create CLI commands:
1. `--mode full`: rebuild all views from all events.
2. `--mode incremental`: rebuild views from latest event offset/checkpoint.
3. supports session-specific and date-specific rebuild.
4. emits compact metrics (duration, event_count, changed_views).
  </action>
  <verify>python scripts/memory/materialize_views.py --mode full --dry-run</verify>
  <done>Operational CLI exists for deterministic view rebuild workflows</done>
</task>

<task type="auto">
  <name>Add determinism and replay tests</name>
  <files>tests/unit/test_memory_materializers.py</files>
  <action>
Add tests for:
1. identical event input => identical view output hash.
2. out-of-order/invalid events are ignored or rejected consistently.
3. working/short/long outputs include provenance references.
4. incremental mode matches full mode after same event set.
  </action>
  <verify>pytest -q tests/unit/test_memory_materializers.py</verify>
  <done>Materialization pipeline is deterministic and test-backed</done>
</task>

</tasks>

<verification>
1. Materializers consume only event stream input.
2. Full and incremental rebuilds are functionally equivalent.
3. Views are reproducible and include provenance links.
</verification>

<success_criteria>
- Memory tiers are generated from canonical events, not ad-hoc writes.
- Replay determinism is verified by tests.
- Tooling exists for operational rebuild and debugging.
</success_criteria>

<output>
After completion, create:
1. `.planning/phases/16-agent-context-os/16-02-SUMMARY.md`
2. `.planning/phases/16-agent-context-os/16-02-to-16-03-REPLAN.md`

The replan file must define concrete replacements for plan 03 placeholders:
- `[[UPSTREAM_MATERIALIZER_API]]`
- `[[UPSTREAM_VIEW_PATHS]]`
- `[[UPSTREAM_VIEW_METRICS_FIELDS]]`
</output>
