---
phase: 16-agent-context-os
plan: 07
type: execute
wave: 4
depends_on: ["16-06"]
files_modified:
  - scripts/context/verify_phase16.py
  - reports/meta/journey-synthesis-14-16.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - tests/integration/test_phase16_context_os_e2e.py
autonomous: false

must_haves:
  truths:
    - "Phase closure requires hard evidence for all Owner Success Metrics"
    - "Verification must include cross-terminal and graph-first behavior under realistic flow"
    - "Phase state updates happen only after gate evidence is GREEN"
  artifacts:
    - path: "scripts/context/verify_phase16.py"
      provides: "Automated verification harness for Phase 16 outcomes"
    - path: "tests/integration/test_phase16_context_os_e2e.py"
      provides: "End-to-end behavioral validation of context OS"
    - path: "reports/meta/journey-synthesis-14-16.md"
      provides: "Cross-phase synthesis and operational lessons"
  key_links:
    - from: "scripts/context/verify_phase16.py"
      to: "python scripts/governance/context_os_gate.py --window-hours 24 --json"
      via: "Consumes gate output as closure input"
      pattern: "GREEN|RED"
---

<objective>
Produce final verification evidence and close Phase 16 with auditable GREEN/RED outcomes.
</objective>

<execution_context>
@C:/Users/Hp/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/16-agent-context-os/16-EXECUTION-CONTRACT.md
</execution_context>

<context>
@.planning/phases/16-agent-context-os/16-PLAN.md
@.planning/phases/16-agent-context-os/16-RESEARCH.md
@.planning/phases/16-agent-context-os/16-06-SUMMARY.md
@.planning/phases/16-agent-context-os/16-06-to-16-07-REPLAN.md
@ops/governance/roles/phase-manager.md
</context>

<adaptive_dependency_gate>
Resolved from `16-06-SUMMARY.md` and `16-06-to-16-07-REPLAN.md`:
1. Gate command:
   - `python scripts/governance/context_os_gate.py --window-hours 24 --json`
2. Metrics schema:
   - top-level: `status`, `checked_at`, `window_hours`, `metrics`, `failed_reasons`
   - per metric: `name`, `passed`, `threshold`, `value`, `reason_code`, `remediation`
3. Runbook path:
   - `docs/runbooks/context-os-operations.md`
4. Unresolved placeholders -> `RED`.
</adaptive_dependency_gate>

<tasks>

<task type="auto">
  <name>Create Phase 16 verification harness</name>
  <files>scripts/context/verify_phase16.py</files>
  <action>
Implement verification harness that executes:
1. `python scripts/governance/context_os_gate.py --window-hours 24 --json` and parses binary status.
2. scenario checks for:
   - new-agent warm start from AGENT_START_HERE
   - folder summary coverage
   - graph-first attempt rate
   - fallback reason quality
   - cross-terminal memory visibility
   - token and latency budgets
3. outputs machine-readable verification bundle for reporting.
  </action>
  <verify>python scripts/context/verify_phase16.py --mode quick</verify>
  <done>Single command exists to verify Phase 16 readiness status</done>
</task>

<task type="auto">
  <name>Add end-to-end integration tests</name>
  <files>tests/integration/test_phase16_context_os_e2e.py</files>
  <action>
Add integration tests proving:
1. graph attempt occurs on every broker call.
2. fallback engages safely with reason code when graph unavailable.
3. hook pipeline updates memory and docs between simulated sessions.
4. non-blocking hook contract is preserved.
  </action>
  <verify>pytest -q tests/integration/test_phase16_context_os_e2e.py</verify>
  <done>Phase-level behavior is validated in end-to-end flow</done>
</task>

<task type="auto">
  <name>Prepare closure artifacts and lifecycle state update</name>
  <files>reports/meta/journey-synthesis-14-16.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
Prepare closure package:
1. journey synthesis for phases 14-16 including graph and memory evolution.
2. update state/roadmap only after verification harness returns GREEN.
3. attach evidence pointers to required reports per governance baseline.
  </action>
  <verify>python scripts/context/verify_phase16.py --mode full</verify>
  <done>Phase closure package is complete and governance-compatible</done>
</task>

</tasks>

<verification>
1. Verification harness returns GREEN with full evidence bundle.
2. Integration tests pass for graph-first + lifecycle + latency behavior.
3. STATE/ROADMAP updates align with PhaseManager authority and gate evidence.
</verification>

<success_criteria>
- All Phase 16 Owner Success Metrics are evidenced and pass.
- Closure artifacts are auditable and reproducible.
- Phase transitions to completed only with GREEN verification.
</success_criteria>

<output>
After completion, create:
1. `.planning/phases/16-agent-context-os/16-07-SUMMARY.md`
2. Required task reports under `reports/16-agent-context-os/16-07/`:
   - `self-check.md`
   - `review.md`
   - `structure-audit.md`
   - `integrity-audit.md`
</output>
