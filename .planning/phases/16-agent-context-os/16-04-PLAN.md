---
phase: 16-agent-context-os
plan: 04
type: execute
wave: 2
depends_on: ["16-03"]
files_modified:
  - src/assistant/context_broker.py
  - src/assistant/memory_retrieval.py
  - scripts/context/context_query_probe.py
  - tests/unit/test_context_broker.py
autonomous: true

must_haves:
  truths:
    - "Graph is attempted first for every context retrieval request"
    - "Fallback is allowed but always reason-coded and measured"
    - "Context assembly enforces token budgets before final prompt handoff"
  artifacts:
    - path: "src/assistant/context_broker.py"
      provides: "Graph-first context broker with fallback cascade"
      exports: ["assemble_context", "ContextBundle", "FallbackReason"]
    - path: "src/assistant/memory_retrieval.py"
      provides: "Broker integration point for runtime retrieval"
    - path: "scripts/context/context_query_probe.py"
      provides: "Smoke probe for graph/fallback telemetry behavior"
  key_links:
    - from: "src/assistant/context_broker.py"
      to: "scripts/context/build_agent_primer.py::build_agent_primer"
      via: "Uses generated docs/views as fallback context sources"
      pattern: "AGENT_START_HERE|FOLDER_SUMMARIES|CONTEXT_LINK_MAP"
---

<objective>
Implement graph-first retrieval broker with strict telemetry, fallback logic, and token/latency controls.
</objective>

<execution_context>
@C:/Users/Hp/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/16-agent-context-os/16-EXECUTION-CONTRACT.md
</execution_context>

<context>
@.planning/phases/16-agent-context-os/16-PLAN.md
@.planning/phases/16-agent-context-os/16-RESEARCH.md
@.planning/phases/16-agent-context-os/16-03-SUMMARY.md
@.planning/phases/16-agent-context-os/16-03-to-16-04-REPLAN.md
@src/assistant/memory_retrieval.py
</context>

<adaptive_dependency_gate>
Upstream references resolved from `16-03-SUMMARY.md` and `16-03-to-16-04-REPLAN.md`:
1. Primer generator:
   - `scripts.context.build_agent_primer.build_agent_primer`
2. Summary doc paths:
   - `docs/AGENT_START_HERE.md`
   - `docs/FOLDER_SUMMARIES.md`
   - `docs/CONTEXT_LINK_MAP.md`
3. Link-map fields:
   - `Group`, `Path`, `Purpose`, `Status`
</adaptive_dependency_gate>

<tasks>

<task type="auto">
  <name>Create graph-first context broker</name>
  <files>src/assistant/context_broker.py</files>
  <action>
Implement broker with:
1. classifier: relational | factual | procedural | status.
2. mandatory graph attempt for all classes.
3. fallback cascade:
   - graph -> local graph snapshot -> generated docs/views -> minimal baseline
4. telemetry on every request:
   - graph_attempted
   - graph_used
   - fallback_used
   - fallback_reason
   - latency_ms
   - assembled_tokens
5. budget enforcement:
   - target <= 2500 tokens
   - hard cap <= 4000 tokens
   - compaction order per phase plan.
  </action>
  <verify>python -c "from src.assistant.context_broker import assemble_context; print('ok')"</verify>
  <done>Graph-first broker exists with reason-coded fallback and budget controls</done>
</task>

<task type="auto">
  <name>Integrate broker into runtime retrieval path</name>
  <files>src/assistant/memory_retrieval.py</files>
  <action>
Replace direct retrieval fan-out with broker mediation where applicable:
1. route context assembly through `assemble_context`.
2. preserve existing scoped memory protections.
3. keep fail-open behavior when graph is unavailable.
4. include provenance fields in final response payload for downstream explainability.
  </action>
  <verify>python -c "from src.assistant.memory_retrieval import retrieve_memory_facts; print('ok')"</verify>
  <done>Runtime retrieval path now consistently goes through graph-first broker policy</done>
</task>

<task type="auto">
  <name>Add broker tests and probe command</name>
  <files>tests/unit/test_context_broker.py, scripts/context/context_query_probe.py</files>
  <action>
Add tests and probe for:
1. graph-attempt flag is true on all retrieval classes.
2. fallback reason codes are set when graph path is not used.
3. token cap is enforced.
4. telemetry fields are emitted and parseable.
  </action>
  <verify>pytest -q tests/unit/test_context_broker.py && python scripts/context/context_query_probe.py --query \"what triggers what for session hooks\"</verify>
  <done>Broker behavior is executable and verifiable with deterministic telemetry</done>
</task>

</tasks>

<verification>
1. Broker attempts graph retrieval before fallback in every retrieval request.
2. Fallback behavior is reason-coded and observable.
3. Token budgets are enforced by tests.
</verification>

<success_criteria>
- Graph-first contract becomes executable policy, not a guideline.
- Runtime retrieval can explain source and fallback behavior.
- Context assembly stays within defined limits.
</success_criteria>

<output>
After completion, create:
1. `.planning/phases/16-agent-context-os/16-04-SUMMARY.md`
2. `.planning/phases/16-agent-context-os/16-04-to-16-05-REPLAN.md`

The replan file must define concrete replacements for plan 05 placeholders:
- `[[UPSTREAM_BROKER_API]]`
- `[[UPSTREAM_TELEMETRY_FIELDS]]`
- `[[UPSTREAM_CONTEXT_PROBE_COMMAND]]`
</output>
