---
phase: 13-integration-hardening-deployment
plan: 03
type: execute
wave: 3
depends_on:
  - 13-01
  - 13-02
requirements:
  - INTEGRATE-05
  - INTEGRATE-06
  - INTEGRATE-07
  - DEPLOY-01
  - DEPLOY-02
  - DEPLOY-04
  - DEPLOY-05
  - DEPLOY-06
  - DEPLOY-07
  - DEPLOY-08
files_modified:
  - src/models/assistant_provider_route_event.py
  - src/models/assistant_deployment_policy.py
  - src/models/__init__.py
  - migrations/versions/<new>_phase13_deploy_observability.py
  - src/assistant/deployment/provider_router.py
  - src/assistant/deployment/observability.py
  - src/assistant/deployment/redaction.py
  - src/assistant/deployment/canary_guard.py
  - src/api/v1/chat/routes.py
  - src/api/v1/ops/routes.py
  - scripts/governance/phase13_canary_gate.py
  - docker-compose.yml
  - .github/workflows/phase13-deploy-guard.yml
  - tests/api/test_provider_fallback_contract.py
  - tests/api/test_observability_correlation_contract.py
  - tests/api/test_redaction_retention_contract.py
  - tests/jobs/test_canary_rollback_contract.py
autonomous: true

must_haves:
  truths:
    - "Provider routing and fallback are policy-controlled with deterministic escalation."
    - "Every execution is traceable end-to-end by correlation_id."
    - "Canary rollback gates are machine-checkable using baseline/scope/sample-floor math."
    - "Security redaction and retention controls are enforced before logs leave runtime boundaries."
  artifacts:
    - path: "src/assistant/deployment/provider_router.py"
      provides: "Provider routing ladder and failure-stage fallback policy"
      contains: "primary/fallback selection + cost/risk-aware route metadata"
    - path: "src/assistant/deployment/canary_guard.py"
      provides: "SLO-linked release gate and rollback trigger enforcement"
      contains: "baseline scope check + N>100 sample floor + >5% drop logic"
    - path: "tests/api/test_redaction_retention_contract.py"
      provides: "Structured masking, regex fallback, and retention policy assertions"
      contains: "secret/PII scrub + retention window checks"
---

<objective>
Implement deployment, observability, and security hardening contracts.

Purpose: make runtime behavior measurable, rollback-safe, and compliant before broad rollout.
</objective>

<execution_context>
@codexclaude/get-shit-done/workflows/execute-plan.md
@codexclaude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-integration-hardening-deployment/13-CONTEXT.md
@.planning/phases/13-integration-hardening-deployment/13-RESEARCH.md
@docker-compose.yml
@scripts/backup_db.sh
@scripts/restore_db.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deployment policy persistence and provider fallback routing contract</name>
  <files>
    - src/models/assistant_provider_route_event.py
    - src/models/assistant_deployment_policy.py
    - src/models/__init__.py
    - migrations/versions/<new>_phase13_deploy_observability.py
    - src/assistant/deployment/provider_router.py
    - src/api/v1/chat/routes.py
  </files>
  <action>
Add deployment policy and provider route contracts:
- provider ladder policy with versioned config lineage
- fallback-stage reason codes and route metadata persistence
- deterministic downgrade path when primary provider fails policy gates
- route decision snapshots tied to correlation_id and tenant scope
  </action>
  <verify>
- `python -m pytest -q tests/api/test_provider_fallback_contract.py`
  </verify>
  <done>
- Provider fallback behavior is deterministic, auditable, and test-covered.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement observability and canary rollback guards</name>
  <files>
    - src/assistant/deployment/observability.py
    - src/assistant/deployment/canary_guard.py
    - src/api/v1/ops/routes.py
    - scripts/governance/phase13_canary_gate.py
    - tests/api/test_observability_correlation_contract.py
    - tests/jobs/test_canary_rollback_contract.py
  </files>
  <action>
Implement deploy-safe observability:
- enforce correlation_id propagation across request -> route -> tool -> provider paths
- compute availability SLI using locked formula:
  - successful_requests / (total_requests - user_errors)
  - target 99.9%
- enforce error budget gate: 43m 12s downtime per 30-day window before feature freeze
- evaluate canary baseline (N-1), scope match, and sample floor (N>100)
- auto-trigger rollback gate when availability drops >5% vs baseline
  </action>
  <verify>
- `python -m pytest -q tests/api/test_observability_correlation_contract.py`
- `python -m pytest -q tests/jobs/test_canary_rollback_contract.py`
  </verify>
  <done>
- SLI math and rollback gates are deterministic and visible to operators.
  </done>
</task>

<task type="auto">
  <name>Task 3: Enforce redaction, retention, and deploy pipeline hardening</name>
  <files>
    - src/assistant/deployment/redaction.py
    - docker-compose.yml
    - .github/workflows/phase13-deploy-guard.yml
    - tests/api/test_redaction_retention_contract.py
  </files>
  <action>
Apply security and retention contracts:
- structured field-level masking + regex fallback on unstructured traces
- retention windows:
  - traces 14 days
  - audit logs 1 year
- right-to-be-forgotten propagation:
  - live store data purge within 48h
  - backup/snapshot delayed purge SLA <=14 days with audit evidence
- deployment guard workflow with backup/restore check hooks and secrets scanning
- non-root and environment-separation checks in deploy guard
  </action>
  <verify>
- `python -m pytest -q tests/api/test_redaction_retention_contract.py`
  </verify>
  <done>
- Deployment path enforces observability + redaction + retention + security gates.
  </done>
</task>

</tasks>

<verification>
- [ ] provider fallback ladder is policy-driven and auditable
- [ ] correlation_id lineage spans API/router/tool/provider layers
- [ ] canary rollback gate enforces baseline, threshold, and sample floor constraints
- [ ] structured + regex redaction is applied before persistence/export
- [ ] retention and deletion propagation contracts are enforced
- [ ] deployment guard checks include backup/restore and security hardening gates
</verification>

<success_criteria>
- Phase 13 wave-3 capability is live: deploy/observability/security controls are production enforceable.
</success_criteria>

<output>
After completion, create `.planning/phases/13-integration-hardening-deployment/13-03-SUMMARY.md`
and governance evidence:
- `reports/13/13-03/self-check.md`
- `reports/13/13-03/review.md`
- `reports/13/13-03/structure-audit.md`
- `reports/13/13-03/integrity-audit.md`
</output>
