---
phase: 14.3-graph-availability-sync
plan: 04
type: execute
wave: 2
depends_on: ['14.3-01']
files_modified:
  - .claude/settings.local.json
  - .gemini/preToolUseHook.sh
  - .codex/preToolUseHook.sh
  - docs/ops/graph-availability-runbook.md
autonomous: true

must_haves:
  truths:
    - "Session start output displays active graph backend (aura/local_neo4j/snapshot) with timestamp"
    - "Bootstrap script executes successfully when invoked manually (exit code 0, runtime manifest written to .graph/runtime-backend.json)"
    - "Runbook procedures produce expected outcomes (backend switches, sync completes, status commands return valid JSON)"
    - "Graph queries succeed after following runbook intervention steps (verifiable via graph_status.py --json)"
  artifacts:
    - path: ".claude/settings.local.json"
      provides: "Claude Code PreToolUse hook"
      exports: []
    - path: "docs/ops/graph-availability-runbook.md"
      provides: "Operator runbook"
      exports: []
  key_links:
    - from: ".claude/settings.local.json"
      to: "scripts/graph/bootstrap_graph_backend.py"
      via: "preToolUseHooks"
      pattern: "bootstrap_graph_backend"
---

<objective>
Wire bootstrap command into agent hooks and create operator runbook.
</objective>

<execution_context>
@C:/Users/Hp/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/14.3-graph-availability-sync/14.3-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Configure Claude Code PreToolUse hook</name>
  <files>.claude/settings.local.json</files>
  <action>
Add preToolUseHooks entry calling scripts/graph/bootstrap_graph_backend.py.
Ensure hook runs before first MCP tool call.
  </action>
  <verify>grep "bootstrap_graph_backend" .claude/settings.local.json</verify>
  <done>Claude Code hook configured</done>
</task>

<task type="auto">
  <name>Configure Gemini and Codex hooks</name>
  <files>.gemini/preToolUseHook.sh, .codex/preToolUseHook.sh</files>
  <action>
Create shell wrappers that call bootstrap_graph_backend.py.
Make executable (chmod +x on Unix).
  </action>
  <verify>test -f .gemini/preToolUseHook.sh && test -f .codex/preToolUseHook.sh</verify>
  <done>Multi-agent hooks configured</done>
</task>

<task type="auto">
  <name>Create operator runbook</name>
  <files>docs/ops/graph-availability-runbook.md</files>
  <action>
Create runbook with:
1. Prerequisites (env vars, Docker, Aura credentials)
2. Fallback behavior explanation
3. Manual intervention procedures
4. Troubleshooting guide
5. Status command reference
  </action>
  <verify>test -f docs/ops/graph-availability-runbook.md</verify>
  <done>Runbook created</done>
</task>

</tasks>

<verification>
1. Bootstrap script runs at session start
2. Runbook procedures work as documented
</verification>

<success_criteria>
- All agents invoke bootstrap before first tool call
- Runbook provides working procedures
- Session start shows backend status
</success_criteria>

<output>
After completion, create `.planning/phases/14.3-graph-availability-sync/14.3-04-SUMMARY.md`
</output>
