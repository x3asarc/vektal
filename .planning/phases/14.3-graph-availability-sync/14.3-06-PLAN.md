---
phase: 14.3-graph-availability-sync
plan: 06
type: execute
wave: 3
depends_on: ['14.3-01', '14.3-02', '14.3-05']
files_modified:
  - scripts/governance/graph_availability_gate.py
  - tests/integration/test_graph_resilience.py
autonomous: true

must_haves:
  truths:
    - "Governance gate executes 5 resilience checks and outputs GREEN/RED"
    - "Gate verifies resolver health, fallback cascade order, snapshot read-only enforcement, bootstrap success, MCP metadata presence"
    - "Integration tests verify Docker Neo4j auto-start and full fallback loop"
    - "Gate provides specific failure reasons when RED"
  artifacts:
    - path: "scripts/governance/graph_availability_gate.py"
      provides: "Phase 14.3 governance gate"
      exports: []
    - path: "tests/integration/test_graph_resilience.py"
      provides: "Graph resilience smoke tests"
      exports: []
  key_links:
    - from: "graph_availability_gate.py"
      to: "backend_resolver.py"
      via: "resolver health check"
      pattern: "resolve_backend"
---

<objective>
Establish formal governance gate to verify graph availability and resilience before phase closure.
</objective>

<execution_context>
@C:/Users/Hp/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/14.3-graph-availability-sync/14.3-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Create governance availability gate</name>
  <files>scripts/governance/graph_availability_gate.py</files>
  <action>
Create governance gate with 5 resilience checks:
1. Resolver Health - Verify GraphBackendResolver correctly identifies backends
2. Fallback Integrity - Mock failures and verify cascade order (aura → local → snapshot)
3. Snapshot Read-Only - Verify mutation blocking in snapshot mode
4. Bootstrap Success - Run bootstrap and verify it exits 0
5. Metadata Presence - Verify MCP responses contain required provenance fields
Output: GREEN or RED with specific failure reasons.
  </action>
  <verify>python scripts/governance/graph_availability_gate.py</verify>
  <done>Governance gate exists and executes all checks</done>
</task>

<task type="auto">
  <name>Create integration test suite</name>
  <files>tests/integration/test_graph_resilience.py</files>
  <action>
Create integration tests for graph resilience:
1. Test Docker auto-start of local Neo4j
2. Verify full fallback loop: Aura Down → Local Starts → Query Works
3. Smoke test that exercises entire availability cascade
Use pytest with Docker fixtures if available in test environment.
  </action>
  <verify>python -m pytest tests/integration/test_graph_resilience.py -v</verify>
  <done>Integration tests exist and pass</done>
</task>

</tasks>

<verification>
1. python scripts/governance/graph_availability_gate.py returns GREEN
2. Integration tests pass in CI environment
</verification>

<success_criteria>
- Governance gate executes all 5 checks
- Gate outputs specific failure reasons when RED
- Integration tests verify fallback loop end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/14.3-graph-availability-sync/14.3-06-SUMMARY.md`
</output>
