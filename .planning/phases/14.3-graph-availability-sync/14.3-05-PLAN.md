---
phase: 14.3-graph-availability-sync
plan: 05
type: execute
wave: 3
depends_on: ['14.3-01', '14.3-03']
files_modified:
  - src/graph/mcp_response_metadata.py
  - src/assistant/governance/mutation_guard.py
  - src/graph/mcp_server.py
autonomous: true

must_haves:
  truths:
    - "All MCP responses include backend_source field"
    - "Responses include freshness indicators (last_sync_timestamp, staleness_hours)"
    - "Mutations blocked when backend_source == 'snapshot'"
    - "is_stale flag present when snapshot >24h old"
  artifacts:
    - path: "src/graph/mcp_response_metadata.py"
      provides: "Response enrichment"
      exports: ["enrich_response"]
    - path: "src/assistant/governance/mutation_guard.py"
      provides: "Snapshot mutation blocking"
      exports: ["check_mutation_allowed"]
  key_links:
    - from: "mcp_server.py"
      to: "mcp_response_metadata.py"
      via: "enrich_response"
      pattern: "enrich_response"
---

<objective>
Enrich MCP responses with backend metadata and block mutations in degraded mode.
</objective>

<execution_context>
@C:/Users/Hp/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/14.3-graph-availability-sync/14.3-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Create MCP response metadata enricher</name>
  <files>src/graph/mcp_response_metadata.py</files>
  <action>
Create enrich_response() that adds backend_source, last_sync_timestamp, staleness_hours, is_stale to all MCP tool responses.
Read from runtime manifest.
  </action>
  <verify>python -c "from src.graph.mcp_response_metadata import enrich_response; print('ok')"</verify>
  <done>Metadata enricher exists</done>
</task>

<task type="auto">
  <name>Create mutation guard</name>
  <files>src/assistant/governance/mutation_guard.py</files>
  <action>
Create check_mutation_allowed() that:
1. Reads runtime manifest
2. Returns False if backend_source == 'snapshot'
3. Emits warning log
Used by governance layer before apply operations.
  </action>
  <verify>python -c "from src.assistant.governance.mutation_guard import check_mutation_allowed; print('ok')"</verify>
  <done>Mutation guard exists</done>
</task>

<task type="auto">
  <name>Integrate metadata into MCP server</name>
  <files>src/graph/mcp_server.py</files>
  <action>
Add enrich_response() call to all tool response paths.
Ensure metadata present in query_graph, get_dependencies, etc.
  </action>
  <verify>grep "enrich_response" src/graph/mcp_server.py</verify>
  <done>MCP server enriches responses</done>
</task>

</tasks>

<verification>
1. MCP responses contain backend_source
2. Mutations fail when backend is snapshot
</verification>

<success_criteria>
- All MCP responses have metadata
- Snapshot mode is read-only
- Freshness disclosed to users
</success_criteria>

<output>
After completion, create `.planning/phases/14.3-graph-availability-sync/14.3-05-SUMMARY.md`
</output>
