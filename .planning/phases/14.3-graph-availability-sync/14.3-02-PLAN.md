---
phase: 14.3-graph-availability-sync
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/graph/remediators/aura_remediator.py
  - src/graph/remediators/snapshot_remediator.py
  - src/graph/remediators/sync_remediator.py
  - src/graph/remediation_registry.py
  - src/graph/infra_probe.py
autonomous: true

must_haves:
  truths:
    - "AuraRemediator uses Console API v1 to resume paused instances with token caching"
    - "SnapshotRemediator rebuilds corrupted snapshots from git HEAD"
    - "SyncRemediator diagnoses and resolves sync pipeline failures"
    - "All existing tools (DockerRemediator, RedisRemediator, GitStalenessGuard) are registered in remediation_registry.py"
    - "No script imports remediator classes directly - all use registry.get_tool()"
  artifacts:
    - path: "src/graph/remediators/aura_remediator.py"
      provides: "Aura instance resume/pause remediation"
      exports: ["AuraRemediator"]
    - path: "src/graph/remediators/snapshot_remediator.py"
      provides: "Snapshot rebuild from git HEAD"
      exports: ["SnapshotRemediator"]
    - path: "src/graph/remediators/sync_remediator.py"
      provides: "Sync pipeline diagnostics and retry"
      exports: ["SyncRemediator"]
  key_links:
    - from: "src/graph/remediators/*_remediator.py"
      to: "src/graph/remediation_registry.py"
      via: "auto_discover() dynamic import"
      pattern: "registry.auto_discover"
    - from: "src/graph/infra_probe.py"
      to: "src/graph/remediation_registry.py"
      via: "registry.get_tool() dispatch"
      pattern: "registry\\.get_tool"
---

<objective>
Create new remediation tools and migrate existing tools to registry as single source of truth.
</objective>

<execution_context>
@C:/Users/Hp/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Hp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14.3-graph-availability-sync/14.3-CONTEXT.md
@.planning/phases/14.3-graph-availability-sync/14.3-RESEARCH.md
@src/graph/universal_fixer.py
@src/graph/remediation_registry.py
</context>

<tasks>

<task type="auto">
  <name>Create AuraRemediator for cloud instance management</name>
  <files>src/graph/remediators/aura_remediator.py</files>
  <action>
Create AuraRemediator extending UniversalRemediator with:
1. OAuth token caching (1hr TTL per RESEARCH.md rate limit)
2. Actions: resume, status
3. Uses httpx for Aura Console API v1
4. Env vars: AURA_CLIENT_ID, AURA_CLIENT_SECRET, AURA_TENANT_ID, AURA_INSTANCE_ID
  </action>
  <verify>python -c "from src.graph.remediators.aura_remediator import AuraRemediator; print(AuraRemediator().service_name)"</verify>
  <done>AuraRemediator exists with OAuth caching</done>
</task>

<task type="auto">
  <name>Create SnapshotRemediator for corrupt snapshot recovery</name>
  <files>src/graph/remediators/snapshot_remediator.py</files>
  <action>
Create SnapshotRemediator extending UniversalRemediator with:
1. Git HEAD hash fingerprinting
2. Snapshot validation (JSON structure check)
3. Rebuild from git HEAD on corruption
4. Creates .graph/snapshots/ directory
  </action>
  <verify>python -c "from src.graph.remediators.snapshot_remediator import SnapshotRemediator; print('ok')"</verify>
  <done>SnapshotRemediator exists</done>
</task>

<task type="auto">
  <name>Create SyncRemediator for sync pipeline failures</name>
  <files>src/graph/remediators/sync_remediator.py</files>
  <action>
Create SyncRemediator extending UniversalRemediator with:
1. Diagnoses sync failures (timeout, connection, schema mismatch)
2. Retry logic with exponential backoff
3. Triggers incremental_sync.py with correct parameters
  </action>
  <verify>python -c "from src.graph.remediators.sync_remediator import SyncRemediator; print('ok')"</verify>
  <done>SyncRemediator exists</done>
</task>

<task type="auto">
  <name>Register existing tools in registry</name>
  <files>src/graph/remediation_registry.py</files>
  <action>
Ensure ALL existing tools are registered via auto_discover():
1. Verify DockerRemediator is in src/graph/remediators/
2. Verify RedisRemediator is in src/graph/remediators/
3. Verify GitStalenessGuard is in src/graph/remediators/
4. Update auto_discover() to scan src/graph/remediators/ directory
5. Add validation method to verify all expected tools registered
  </action>
  <verify>python -c "from src.graph.remediation_registry import registry; registry.auto_discover(); print(len(registry._tools))"</verify>
  <done>All existing tools registered in registry</done>
</task>

<task type="auto">
  <name>Refactor call sites to use registry</name>
  <files>src/graph/infra_probe.py, scripts/graph/*</files>
  <action>
Refactor all direct remediator imports to use registry:
1. Update src/graph/infra_probe.py to use registry.get_tool() instead of direct imports
2. Update active_remediation_loop() to use registry dispatch
3. Search scripts/graph/ for direct imports and refactor
4. Verify no direct imports remain: grep -r "from.*remediator import" src/graph/ scripts/graph/
  </action>
  <verify>grep -r "from.*remediator import" src/graph/ scripts/graph/ || echo "No direct imports found"</verify>
  <done>All call sites refactored to use registry</done>
</task>

</tasks>

<verification>
1. All 3 new remediators import successfully
2. Registry auto_discover() finds all tools
3. No direct remediator imports outside src/graph/remediators/
4. infra_probe.py uses registry.get_tool()
</verification>

<success_criteria>
- Three new remediators follow UniversalRemediator pattern
- All existing tools registered in registry
- No direct imports - all use registry dispatch
- Registry is single source of truth
</success_criteria>

<output>
After completion, create `.planning/phases/14.3-graph-availability-sync/14.3-02-SUMMARY.md`
</output>
