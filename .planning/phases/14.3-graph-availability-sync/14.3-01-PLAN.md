---
phase: 14.3-graph-availability-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/graph/backend_resolver.py
  - .graph/runtime-backend.json
  - scripts/graph/bootstrap_graph_backend.py
autonomous: true

must_haves:
  truths:
    - "Backend resolver probes Aura with 2s HTTP Query API timeout before Bolt"
    - "Resolver returns canonical backend enum: aura, local_neo4j, or local_snapshot"
    - "Runtime manifest (.graph/runtime-backend.json) contains backend, checked_at, reason, freshness_hours, is_degraded"
    - "Failed probe results are cached for 60s TTL to skip known-dead endpoints"
    - "Docker auto-start is attempted when local Neo4j probe fails (5s budget)"
  artifacts:
    - path: "src/graph/backend_resolver.py"
      provides: "Backend resolution with fallback cascade"
      exports: ["resolve_backend", "BackendStatus", "BACKEND_ENUM"]
    - path: "scripts/graph/bootstrap_graph_backend.py"
      provides: "Shared bootstrap command for all agents"
      exports: ["bootstrap_graph"]
  key_links:
    - from: "src/graph/backend_resolver.py"
      to: "src/graph/infra_probe.py"
      via: "probe_neo4j_lightweight reuse"
      pattern: "probe_tcp|probe_neo4j"
---

<objective>
Implement the backend resolver contract with runtime manifest generation and Docker auto-start integration.
</objective>

<execution_context>
@C:/Users/Hp/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Hp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14.3-graph-availability-sync/14.3-CONTEXT.md
@.planning/phases/14.3-graph-availability-sync/14.3-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create backend resolver with probe cascade</name>
  <files>src/graph/backend_resolver.py</files>
  <action>
Create backend resolver with:
1. BACKEND_ENUM: "aura", "local_neo4j", "local_snapshot"
2. BackendStatus dataclass with backend, checked_at, reason, freshness_hours, is_degraded, probe_latency_ms
3. Probe functions: probe_aura_http() using httpx (2s timeout), probe_bolt() as secondary
4. Failure cache: 60s TTL, module-level dict
5. resolve_backend() with cascade: aura → local_neo4j → snapshot (with Docker auto-start on local failure)
  </action>
  <verify>python -c "from src.graph.backend_resolver import resolve_backend, BackendStatus, BACKEND_ENUM; print('ok')"</verify>
  <done>Backend resolver module exists with probe cascade and failure caching</done>
</task>

<task type="auto">
  <name>Create runtime manifest writer</name>
  <files>src/graph/backend_resolver.py, .graph/.gitkeep</files>
  <action>
Add write_runtime_manifest(status) and read_runtime_manifest() functions.
Write to .graph/runtime-backend.json with all BackendStatus fields.
Add .graph/ to .gitignore.
  </action>
  <verify>python -c "from src.graph.backend_resolver import write_runtime_manifest; print('ok')"</verify>
  <done>Runtime manifest read/write functions exist</done>
</task>

<task type="auto">
  <name>Create shared bootstrap command with Docker auto-start</name>
  <files>scripts/graph/bootstrap_graph_backend.py</files>
  <action>
Create bootstrap script that:
1. Calls resolve_backend() which includes Docker auto-start logic:
   - Probe Aura (2s)
   - If Aura fails, probe local Neo4j
   - If local probe fails, attempt "docker compose up -d neo4j" (5s budget)
   - Re-probe local after Docker start
   - If both fail, use snapshot
2. Writes runtime manifest
3. Prints backend status with emoji (✅ or ⚠️)
4. Always exits 0 (fail-open)
  </action>
  <verify>python scripts/graph/bootstrap_graph_backend.py</verify>
  <done>Bootstrap script exists, runs without error, writes manifest, includes Docker auto-start</done>
</task>

</tasks>

<verification>
1. python -c "from src.graph.backend_resolver import *" imports successfully
2. python scripts/graph/bootstrap_graph_backend.py completes without error
3. .graph/runtime-backend.json contains valid JSON with required fields
4. Docker auto-start is attempted when local probe fails
</verification>

<success_criteria>
- Backend resolver implements full probe cascade (Aura HTTP → Bolt → local → Docker start → snapshot)
- Runtime manifest written with all required fields
- Bootstrap script is canonical entry point for all agents
- Docker auto-start integrated before snapshot fallback
</success_criteria>

<output>
After completion, create `.planning/phases/14.3-graph-availability-sync/14.3-01-SUMMARY.md`
</output>
