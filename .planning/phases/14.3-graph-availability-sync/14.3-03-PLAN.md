---
phase: 14.3-graph-availability-sync
plan: 03
type: execute
wave: 2
depends_on: ['14.3-01']
files_modified:
  - src/graph/sync_status.py
  - scripts/graph/graph_status.py
  - src/graph/incremental_sync.py
autonomous: true

must_haves:
  truths:
    - "Sync status tracks last_sync_at, sync_mode (auto/manual), last_source"
    - "Status command returns machine-readable JSON contract"
    - "Sync metadata updates on success/failure"
  artifacts:
    - path: "src/graph/sync_status.py"
      provides: "Sync status tracking"
      exports: ["SyncStatus", "update_sync_status", "get_sync_status"]
    - path: "scripts/graph/graph_status.py"
      provides: "Operator CLI for status"
      exports: []
  key_links:
    - from: "sync_status.py"
      to: "backend_resolver.py"
      via: "freshness metadata"
      pattern: "freshness_hours"
---

<objective>
Implement sync status tracking and operator CLI.
</objective>

<execution_context>
@C:/Users/Hp/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/14.3-graph-availability-sync/14.3-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Create sync status module</name>
  <files>src/graph/sync_status.py</files>
  <action>
Create SyncStatus dataclass with last_sync_at, sync_mode, last_source.
Implement update_sync_status() and get_sync_status().
Store in .graph/sync-status.json.
  </action>
  <verify>python -c "from src.graph.sync_status import get_sync_status; print('ok')"</verify>
  <done>Sync status module exists</done>
</task>

<task type="auto">
  <name>Update incremental sync to emit status</name>
  <files>src/graph/incremental_sync.py</files>
  <action>
Add update_sync_status() calls on sync success/failure.
Record timestamp, mode, source.
  </action>
  <verify>grep "update_sync_status" src/graph/incremental_sync.py</verify>
  <done>Sync emits status updates</done>
</task>

<task type="auto">
  <name>Create graph status CLI</name>
  <files>scripts/graph/graph_status.py</files>
  <action>
Create CLI that shows current backend, last sync, sync mode.
Support --json flag for machine-readable output.
  </action>
  <verify>python scripts/graph/graph_status.py --json</verify>
  <done>Status CLI exists</done>
</task>

</tasks>

<verification>
1. graph_status.py --json returns valid JSON
2. Sync status persists across restarts
</verification>

<success_criteria>
- Sync status queryable via CLI
- Machine-readable JSON output
- Status updates on sync events
</success_criteria>

<output>
After completion, create `.planning/phases/14.3-graph-availability-sync/14.3-03-SUMMARY.md`
</output>
