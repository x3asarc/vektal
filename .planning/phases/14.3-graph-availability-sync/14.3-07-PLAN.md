---
phase: 14.3-graph-availability-sync
plan: 07
type: execute
wave: 4
depends_on: ['14.3-01', '14.3-02']
files_modified:
  - scripts/observability/sentry_issue_puller.py
  - src/graph/sentry_ingestor.py
  - scripts/graph/orchestrate_healers.py
autonomous: true

must_haves:
  truths:
    - "Sentry puller polls REST API with cursor pagination and filters is:unresolved level:error"
    - "Normalization maps issue titles/tags to taxonomy: AURA_UNREACHABLE, LOCAL_NEO4J_START_FAIL, SNAPSHOT_CORRUPT"
    - "Ingestor appends to FAILURE_JOURNEY.md and emits knowledge graph episodes"
    - "Orchestrator has cooldown/throttling to prevent flapping remediation"
    - "Whitelisted failure categories trigger autonomous healing"
  artifacts:
    - path: "scripts/observability/sentry_issue_puller.py"
      provides: "Sentry issue polling and normalization"
      exports: []
    - path: "src/graph/sentry_ingestor.py"
      provides: "Failure event ingestion"
      exports: ["ingest_failure_event"]
    - path: "scripts/graph/orchestrate_healers.py"
      provides: "Autonomous healing orchestration (updated)"
      exports: []
  key_links:
    - from: "sentry_issue_puller.py"
      to: "sentry_ingestor.py"
      via: "FailureEvent normalization"
      pattern: "ingest_failure_event"
    - from: "sentry_ingestor.py"
      to: "orchestrate_healers.py"
      via: "healing trigger"
      pattern: "orchestrate_healers"
---

<objective>
Connect external failure signals from Sentry to autonomous self-healing layer for production infrastructure failures.
</objective>

<execution_context>
@C:/Users/Hp/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/14.3-graph-availability-sync/14.3-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Create Sentry issue puller with normalization</name>
  <files>scripts/observability/sentry_issue_puller.py</files>
  <action>
Create Sentry issue puller with:
1. httpx client for Sentry REST API (/projects/{org}/{project}/issues/)
2. Filter: is:unresolved level:error
3. Cursor pagination using .graph/sentry-cursor.json
4. Normalization logic:
   - Parse issue titles and tags
   - Map to taxonomy: AURA_UNREACHABLE, LOCAL_NEO4J_START_FAIL, SNAPSHOT_CORRUPT, etc.
   - Create FailureEvent dataclass
5. Env vars: SENTRY_ORG, SENTRY_PROJECT, SENTRY_AUTH_TOKEN
  </action>
  <verify>python scripts/observability/sentry_issue_puller.py --manual</verify>
  <done>Sentry puller exists with normalization</done>
</task>

<task type="auto">
  <name>Create Sentry ingestor</name>
  <files>src/graph/sentry_ingestor.py</files>
  <action>
Create ingestor that:
1. Appends normalized FailureEvent records to FAILURE_JOURNEY.md
2. Triggers orchestrate_healers.py for whitelisted failure categories
3. Emits "Failure Ingested" episodes to knowledge graph
4. Integrates with existing failure tracking infrastructure
  </action>
  <verify>python -c "from src.graph.sentry_ingestor import ingest_failure_event; print('ok')"</verify>
  <done>Sentry ingestor exists</done>
</task>

<task type="auto">
  <name>Update orchestrate_healers with throttling</name>
  <files>scripts/graph/orchestrate_healers.py</files>
  <action>
Update orchestrate_healers.py to:
1. Accept signals from Sentry ingestor
2. Implement cooldown/throttling mechanism (e.g., 5min cooldown per failure category)
3. Prevent "flapping" remediation
4. Store throttle state in .graph/healer-throttle.json
  </action>
  <verify>grep "throttle" scripts/graph/orchestrate_healers.py</verify>
  <done>Orchestrator has throttling mechanism</done>
</task>

</tasks>

<verification>
1. Sentry puller successfully polls API and normalizes issues
2. FAILURE_JOURNEY.md updated with ingested failures
3. Orchestrator throttling prevents rapid-fire remediation
</verification>

<success_criteria>
- Sentry issues normalized to failure taxonomy
- Autonomous healing triggered for whitelisted categories
- Throttling prevents remediation loops
- Full observability-to-healing pipeline working
</success_criteria>

<output>
After completion, create `.planning/phases/14.3-graph-availability-sync/14.3-07-SUMMARY.md`
</output>
