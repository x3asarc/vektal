# Phase 14.3: Graph Availability + Sync Reliability - Context

**Gathered:** 2026-03-01
**Status:** Ready for planning

<domain>
## Phase Boundary

Infrastructure resilience layer that guarantees graph context availability through a three-tier fallback strategy (Aura → local Neo4j → snapshot) with autonomous failure resolution and learning-driven process improvements.

</domain>

<decisions>
## Implementation Decisions

### Fallback Cascade Timing
- **Aura timeout:** 2 seconds max (matches Phase 13.2 graph query timeout)
- **Local Neo4j startup:** Auto-attempt on Aura failure, 5-second startup budget
- **Snapshot fallback:** Immediate if both fail
- **Failure caching:** Yes — cache backend availability status (60s TTL) to skip known-dead endpoints
- **Cascade order:** Always `aura → local_neo4j → snapshot`, no shortcuts

### Sync Mode Behavior
- **Default mode:** Auto-sync (background, non-blocking)
- **Auto-sync triggers:**
  - Session bootstrap (if snapshot >24h stale)
  - After code changes detected (git hook integration)
  - Periodic daemon (every 6 hours)
- **Manual sync:** Available via `gsd-tools.js sync-graph` command
- **Mid-session sync failure:** Emit to failure log, continue on current backend, retry next trigger
- **Blocking behavior:** Never block session start — degrade gracefully and sync in background

### Status Visibility
- **Active backend indicator:** All MCP responses include `backend_source` field (`aura`, `local_neo4j`, `snapshot`)
- **Freshness metadata:** Include `last_sync_timestamp` and `staleness_hours` in status queries
- **Stale snapshot warning:** Emit warning log if snapshot >48h old and being used
- **Status command:** `gsd-tools.js graph-status` shows current backend, last sync, sync mode
- **Session bootstrap log:** Print which backend resolved (visible in Claude Code session start)

### Degraded Mode Guardrails
- **Snapshot mode restrictions:** Read-only queries allowed, no writes attempted
- **Staleness threshold:** If snapshot >7 days old, emit ERROR-level log recommending manual sync
- **Query compatibility:** All MCP queries must work on all three backends (schema parity enforced)
- **Data freshness disclosure:** MCP responses include `is_stale: true` flag when snapshot >24h old
- **Operation safety:** No behavior changes based on backend — queries return same structure regardless

### Autonomous Self-Healing Architecture (NullClaw Pattern)

**Architectural Foundation:** Phase 14.2 established the NullClaw pattern (inspired by github.com/nullclaw/nullclaw) for hyper-modular, VTable-based remediation with explicit tool dispatch.

**Existing Implementation (Phase 14.2):**
- `src/graph/infra_probe.py` — lightweight TCP probes + `active_remediation_loop()`
- `src/graph/universal_fixer.py` — `NanoFixerLoop` (Probe → Validate → Act → Verify protocol)
- `src/graph/remediation_registry.py` — Dynamic "Heap" that auto-discovers remediation tools
- **Recursive dependency resolution:** If Neo4j fix fails → auto-attempt Docker fix → re-try Neo4j
- **Current tools:** DockerRemediator, RedisRemediator, GitStalenessGuard

**Phase 14.3 Extensions (build on NullClaw foundation):**

#### Registry as Single Source of Truth (CRITICAL REQUIREMENT)
- **Central registry:** `src/graph/remediation_registry.py` is the **ONLY** source of truth for ALL remediation tools
- **Migration requirement:** ALL existing tools must be registered in the Heap (not just new ones)
- **Refactor all call sites:** Any script/module currently calling tools directly must be updated to call through registry
- **Discovery pattern:** Scripts invoke `registry.auto_discover()` → query registry → dispatch tool
- **No direct imports:** No script should import `DockerRemediator`, `RedisRemediator`, etc. directly — always go through registry

**Current tools requiring registry migration:**
- `DockerRemediator` (location to be verified during research)
- `RedisRemediator` (location to be verified during research)
- `GitStalenessGuard` (location to be verified during research)
- Any other existing remediation tools discovered during research

**Call sites requiring refactor to use registry:**
- `src/graph/infra_probe.py` — may be importing tools directly
- Any scripts in `scripts/graph/` that invoke remediators
- Any other modules calling remediation logic (to be identified during research)

**Research task:** Audit all current tool locations and call sites, document in RESEARCH.md before planning

#### Failure Resolution Loop
- **Failure log:** `FAILURE_JOURNEY.md` (existing Phase 13.2 pattern) captures all graph availability failures
- **Failure categories:**
  - `AURA_UNREACHABLE` — network/auth failure
  - `LOCAL_NEO4J_START_FAIL` — Docker/port conflict
  - `SNAPSHOT_CORRUPT` — invalid/missing snapshot file
  - `SYNC_TIMEOUT` — sync took >5 minutes
  - `BACKEND_MISMATCH` — schema version drift
- **New remediation tools (register in Heap):**
  - `AuraRemediator` — resume paused cloud instances, validate credentials
  - `SnapshotRemediator` — rebuild corrupted snapshots from git HEAD
  - `SyncRemediator` — diagnose and resolve sync pipeline failures
- **Autonomous healing agents:**
  - Bash loop: `scripts/graph/auto_heal_loop.sh` (runs every 30 min, checks failure log)
  - Healing dispatch: `scripts/graph/orchestrate_healers.py` (reads failure log, spawns specialist agents)
  - Specialist agents per category (e.g., `heal_aura.sh`, `heal_local_neo4j.sh`, `heal_snapshot.sh`)

#### Sentry-Driven Failure Intake (Phase 14.3 bridge)
- **Signal source:** Sentry project issues are treated as external failure signals, not direct remediation commands.
- **Polling model:** Non-blocking pull loop (scheduled + manual trigger) fetches new/open issues.
- **Normalization contract:** Convert Sentry issue payloads into existing failure categories:
  - `AURA_UNREACHABLE`
  - `LOCAL_NEO4J_START_FAIL`
  - `SNAPSHOT_CORRUPT`
  - `SYNC_TIMEOUT`
  - `BACKEND_MISMATCH`
- **Routing rule:** Only mapped categories with approved severities can trigger healing agents.
- **Audit trail:** Every ingested issue appends normalized records to `FAILURE_JOURNEY.md` and graph episode metadata.
- **Dedup policy:** Use issue fingerprint + first_seen timestamp + environment as dedupe key.
- **Guardrail:** Ingestion failures never block runtime fallback or session bootstrap.

#### Learning + Process Improvement Loop
- **Learning capture:** Every healed failure emits to `LEARNINGS.md` with:
  - Failure type + root cause
  - Solution applied
  - Recommended process change
- **Orchestration agent:** `scripts/graph/learning_orchestrator.py`
  - Runs daily
  - Parses `LEARNINGS.md` for new entries
  - Classifies learning type: `FAILSAFE_ADDITION`, `TIMEOUT_TUNING`, `FALLBACK_ORDER_CHANGE`
  - Spawns specialist agents to implement process changes
- **Process update agents:**
  - `update_timeouts.py` — adjust timeout constants in code
  - `add_failsafe.py` — generate new guardrail code
  - `update_fallback_logic.py` — modify cascade order/conditions
- **Verification:** All process changes run through governance gate before commit

#### Failure → Learning → Process Pipeline
```
1. Failure occurs → logged to FAILURE_JOURNEY.md
2. Auto-heal loop detects → spawns healing agent
3. Healing agent fixes → emits to LEARNINGS.md
4. Learning orchestrator detects → classifies learning type
5. Process update agent spawns → implements failsafe/improvement
6. Governance gate validates → commits if GREEN
7. Future sessions benefit from new process
```

#### Sentry Issue -> Triage -> Healing Pipeline
```
1. Sentry issue detected -> ingested by puller
2. Ingestor normalizes issue -> phase failure taxonomy
3. Normalized record appended to FAILURE_JOURNEY.md
4. Category/severity guard checks pass -> remediation dispatch
5. Healing outcome emitted -> LEARNINGS.md + graph episodes
6. Successful repeated patterns promoted to reusable remediation recipes
```

#### NullClaw Design Principles (Mandatory)
- **Single source of truth:** `remediation_registry.py` is the ONLY registry — all tools registered, all calls go through registry
- **Hyper-modular:** Every remediation is a discrete, pluggable tool registered in the Heap
- **Predictable:** Strict protocol (Probe → Validate → Act → Verify) — no ad-hoc fixes
- **Auditable:** All remediation attempts logged with outcomes for governance review
- **Recursive:** Dependency resolution is automatic (Neo4j → Docker → Host ENV)
- **VTable-style dispatch:** Registry maps service names to remediation tools dynamically
- **Fail-safe:** If remediation fails, system degrades gracefully (logs error, uses fallback backend)
- **Never block:** Remediation runs async — session starts immediately, healing happens in background
- **No direct tool imports:** All scripts must call tools via registry lookup — never import remediation classes directly

### Claude's Discretion
- Exact timeout values (within stated ranges)
- Cache TTL tuning based on observed failure patterns
- Healing agent retry logic and backoff strategies
- Log verbosity levels per environment

</decisions>

<specifics>
## Specific Ideas

- **Lightweight:** All timeouts ≤5s, cache aggressively, never block session start
- **Fast:** Parallel backend health checks (Aura + local Neo4j checked simultaneously)
- **High success rate:** Three-tier fallback means 99.9%+ availability (all three fail = catastrophic)
- **Autonomous healing:** No human intervention required for transient failures
- **Learning-driven:** System gets smarter with each failure category encountered
- **Self-improving:** Each failure class becomes a new process failsafe automatically
- **Build on Phase 14.2:** Extend existing NullClaw foundation (`infra_probe.py`, `universal_fixer.py`, `remediation_registry.py`)
- **Master learnings:** Root-level `LEARNINGS.md` shared across all agents (Gemini, Claude, Codex) — auto-injected into next session
- **No reinvention:** Use `NanoFixerLoop` protocol for all new remediation tools (DockerRemediator, RedisRemediator patterns)
- **Strategic impact:** Phase 14.3 treats "Infrastructure Downtime" as a solvable code problem, not a terminal error

</specifics>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope.

</deferred>

---

*Phase: 14.3-graph-availability-sync*
*Context gathered: 2026-03-01*
