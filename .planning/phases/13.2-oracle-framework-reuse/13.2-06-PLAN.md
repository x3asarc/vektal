---
phase: 13.2-oracle-framework-reuse
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/enrichment/oracle_contract.py
  - src/assistant/governance/graph_oracle_adapter.py
  - src/core/enrichment/oracles/content_oracle.py
  - src/core/enrichment/oracles/visual_oracle.py
  - src/core/enrichment/oracles/policy_oracle.py
  - tests/unit/test_enrichment_oracle_contract.py
  - tests/api/test_graph_oracle_adapter_contract.py
  - .planning/KNOWLEDGE_GRAPH_ORACLE.md
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "A single Oracle contract is defined and reused across all adapters"
    - "OracleDecision and OracleSignal use identical decision type vocabulary"
    - "Enrichment oracles (content/visual/policy) use the unified contract"
    - "Graph oracle adapter uses the unified contract"
    - "All 49 existing contract tests still pass"
  artifacts:
    - path: "src/core/enrichment/oracle_contract.py"
      provides: "Unified Oracle contract with harmonized decision types"
      exports: ["OracleDecision", "OracleDecisionType"]
      contains: "pass|fail|review|hold"
    - path: "src/assistant/governance/graph_oracle_adapter.py"
      provides: "Graph adapter using unified contract"
      contains: "from src.core.enrichment.oracle_contract import OracleDecision"
    - path: "src/core/enrichment/oracles/content_oracle.py"
      provides: "Content oracle using unified contract"
    - path: "src/core/enrichment/oracles/visual_oracle.py"
      provides: "Visual oracle using unified contract"
    - path: "src/core/enrichment/oracles/policy_oracle.py"
      provides: "Policy oracle using unified contract"
  key_links:
    - from: "src/assistant/governance/graph_oracle_adapter.py"
      to: "src/core/enrichment/oracle_contract.py"
      via: "import OracleDecision"
      pattern: "from src\\.core\\.enrichment\\.oracle_contract import"
    - from: "src/core/enrichment/oracles/content_oracle.py"
      to: "src/core/enrichment/oracle_contract.py"
      via: "import OracleDecision"
      pattern: "from.*oracle_contract import"
---

<objective>
Unify the two incompatible Oracle contracts (OracleDecision and OracleSignal) into a single reusable contract.

Purpose: Phase 14/15 cannot consume a unified Oracle framework while two incompatible contracts exist. OracleDecision uses decision types (accept|suggest|hold|reject) while OracleSignal uses (pass|fail|review). This creates integration friction and prevents framework reuse.

Output: Single OracleDecision contract with harmonized decision types used by all adapters (content, visual, policy, graph).
</objective>

<execution_context>
@C:/Users/Hp/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Hp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Gap source
@.planning/phases/13.2-oracle-framework-reuse/13.2-VERIFICATION.md

# Current contracts
@src/core/enrichment/oracle_contract.py
@src/assistant/governance/graph_oracle_adapter.py

# Affected adapters
@src/core/enrichment/oracles/content_oracle.py
@src/core/enrichment/oracles/visual_oracle.py
@src/core/enrichment/oracles/policy_oracle.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harmonize OracleDecision decision types to unified vocabulary</name>
  <files>src/core/enrichment/oracle_contract.py</files>
  <action>
Update OracleDecision contract to use harmonized decision types that work for both enrichment and governance use cases:

1. Change OracleDecisionType from `Literal["accept", "suggest", "hold", "reject"]` to `Literal["pass", "review", "hold", "fail"]`

2. Mapping rationale (document in docstring):
   - `accept` -> `pass` (clear automatic approval)
   - `suggest` -> `review` (requires human review but not blocking)
   - `hold` -> `hold` (unchanged, pending more information)
   - `reject` -> `fail` (clear rejection/blocking)

3. Add optional `source` field to OracleDecision dataclass (default: 'enrichment') to track origin

4. Update merchant_first_arbitration() function to use new decision types:
   - All `decision="accept"` -> `decision="pass"`
   - All `decision="suggest"` -> `decision="review"`
   - All `decision="reject"` -> `decision="fail"`

5. Add backward-compat helper function `normalize_legacy_decision(decision: str) -> str` that maps old vocabulary to new (for any external callers):
   - accept -> pass
   - suggest -> review
   - reject -> fail
   - pass/review/hold/fail -> unchanged

6. Update module docstring to state this is the canonical Oracle contract for all adapters.

Gap reason: Two contracts with incompatible decision vocabularies prevent framework reuse.
  </action>
  <verify>
python -c "from src.core.enrichment.oracle_contract import OracleDecision, OracleDecisionType, normalize_legacy_decision; print('Import OK'); assert 'pass' in OracleDecisionType.__args__; print('Decision types OK')"
  </verify>
  <done>
OracleDecision uses unified decision types (pass|review|hold|fail), source field added, backward-compat helper exists.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate enrichment oracles to unified contract</name>
  <files>src/core/enrichment/oracles/content_oracle.py, src/core/enrichment/oracles/visual_oracle.py, src/core/enrichment/oracles/policy_oracle.py, tests/unit/test_enrichment_oracle_contract.py</files>
  <action>
Update all enrichment oracles to use the new unified decision vocabulary:

1. For each oracle file (content_oracle.py, visual_oracle.py, policy_oracle.py):
   - Update any hardcoded decision strings:
     - "accept" -> "pass"
     - "suggest" -> "review"
     - "reject" -> "fail"
   - Verify imports still work
   - Set source='enrichment' when constructing OracleDecision

2. Update test file (test_enrichment_oracle_contract.py):
   - Update all assertions to expect new decision types
   - e.g., `assert result.decision == "accept"` -> `assert result.decision == "pass"`
   - Add test for normalize_legacy_decision() helper

3. Run tests to verify no regressions:
   - python -m pytest tests/unit/test_enrichment_oracle_contract.py -v

Reference: OracleDecision is imported from src.core.enrichment.oracle_contract
Gap reason: Enrichment oracles must use same vocabulary as graph adapter.
  </action>
  <verify>
python -m pytest tests/unit/test_enrichment_oracle_contract.py -v --tb=short
  </verify>
  <done>
All enrichment oracles use unified decision types, all enrichment oracle tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove OracleSignal and migrate graph adapter to unified contract</name>
  <files>src/assistant/governance/graph_oracle_adapter.py, src/assistant/governance/__init__.py, tests/api/test_graph_oracle_adapter_contract.py, .planning/KNOWLEDGE_GRAPH_ORACLE.md</files>
  <action>
Replace OracleSignal with OracleDecision in graph adapter:

1. In graph_oracle_adapter.py:
   - Remove OracleSignal dataclass definition
   - Add import: `from src.core.enrichment.oracle_contract import OracleDecision`
   - Create backward-compat alias: `OracleSignal = OracleDecision  # Deprecated alias for backward compatibility`
   - Update FAIL_OPEN_SIGNAL to use OracleDecision:
     ```python
     FAIL_OPEN_SIGNAL = OracleDecision(
         decision='pass',
         confidence=0.5,
         reason_codes=(),
         evidence_refs=(),
         requires_user_action=False,
         source='graph_unavailable'
     )
     ```
   - Update query_evidence() return type hints to OracleDecision
   - Update all OracleSignal(...) constructors to OracleDecision(..., source='graph')

2. In src/assistant/governance/__init__.py:
   - Update re-exports to include OracleDecision from the canonical location
   - Keep OracleSignal re-export for backward compatibility with deprecation note

3. Update test file (test_graph_oracle_adapter_contract.py):
   - Import OracleDecision instead of/alongside OracleSignal
   - Update type assertions if any check isinstance(result, OracleSignal)
   - Run tests to verify pass

4. Update KNOWLEDGE_GRAPH_ORACLE.md:
   - Replace references to OracleSignal with OracleDecision
   - Note the unified contract from src.core.enrichment.oracle_contract
   - Update code examples to use OracleDecision

Gap reason: Graph adapter must use same contract as enrichment adapters.
  </action>
  <verify>
python -m pytest tests/api/test_graph_oracle_adapter_contract.py -v --tb=short && python -c "from src.assistant.governance import OracleSignal, OracleDecision; print('Both imports work')"
  </verify>
  <done>
Graph adapter uses unified OracleDecision contract, OracleSignal is deprecated alias, all 13 graph adapter tests pass, documentation updated.
  </done>
</task>

</tasks>

<verification>
1. All contract tests pass:
   - python -m pytest tests/unit/test_enrichment_oracle_contract.py -v
   - python -m pytest tests/api/test_graph_oracle_adapter_contract.py -v

2. Full Phase 13.2 contract test suite passes:
   - python -m pytest tests/core/test_graphiti_client_contract.py tests/tasks/test_graphiti_sync_contract.py tests/api/test_graph_oracle_adapter_contract.py -v

3. Import verification:
   - All adapters import OracleDecision from canonical location
   - OracleSignal backward-compat alias works

4. Integration check:
   - grep -r "OracleSignal" src/ --include="*.py" | grep -v "# Deprecated" should return minimal/no results
</verification>

<success_criteria>
1. Single OracleDecision contract defined in src/core/enrichment/oracle_contract.py
2. Decision types unified: pass|review|hold|fail
3. Enrichment oracles (content/visual/policy) use unified contract
4. Graph adapter uses unified contract (OracleSignal is deprecated alias)
5. All 49+ contract tests pass without changes to test logic (only decision type updates)
6. KNOWLEDGE_GRAPH_ORACLE.md documents unified contract
</success_criteria>

<output>
After completion, create `.planning/phases/13.2-oracle-framework-reuse/13.2-06-SUMMARY.md`
</output>
