---
phase: 13.2-oracle-framework-reuse
plan: 04
type: execute
wave: 3
depends_on: ["13.2-01", "13.2-02", "13.2-03"]
files_modified:
  - scripts/governance/graph_gate.py
  - tests/core/test_graphiti_client_contract.py
  - tests/tasks/test_graphiti_sync_contract.py
  - tests/api/test_graph_oracle_adapter_contract.py
autonomous: true

must_haves:
  truths:
    - "Graph gate script returns binary GREEN/RED output"
    - "Contract tests pass without requiring live Neo4j"
    - "Static emission hook checks validate required points"
    - "All contract tests are discoverable by pytest"
  artifacts:
    - path: "scripts/governance/graph_gate.py"
      provides: "CI governance gate for graph integration"
      exports: ["--check-emission-hooks", "--dry-run"]
    - path: "tests/core/test_graphiti_client_contract.py"
      provides: "Contract tests for graph client"
      min_lines: 50
    - path: "tests/tasks/test_graphiti_sync_contract.py"
      provides: "Contract tests for episode emission"
      min_lines: 50
    - path: "tests/api/test_graph_oracle_adapter_contract.py"
      provides: "Contract tests for oracle adapter"
      min_lines: 50
  key_links:
    - from: "scripts/governance/graph_gate.py"
      to: "src/tasks/graphiti_sync.py"
      via: "static analysis for emit_episode hooks"
      pattern: "emit_episode"
---

<objective>
Create CI governance gate and comprehensive contract tests for graph integration.

Purpose: Ensure graph integration meets quality gates before phase closure.
Output: Deterministic governance script and contract test suite that runs without live Neo4j.
</objective>

<execution_context>
@C:/Users/Hp/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Hp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13.2-oracle-framework-reuse/13.2-01-SUMMARY.md
@.planning/phases/13.2-oracle-framework-reuse/13.2-02-SUMMARY.md
@.planning/phases/13.2-oracle-framework-reuse/13.2-03-SUMMARY.md
@claudesplan.md
@src/core/graphiti_client.py
@src/tasks/graphiti_sync.py
@src/assistant/governance/graph_oracle_adapter.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create governance gate script for graph integration</name>
  <files>
    scripts/governance/graph_gate.py
  </files>
  <action>
1. Create scripts/governance/graph_gate.py:

   a) Imports:
      - argparse, sys, os, re
      - pathlib.Path

   b) Emission hook check (--check-emission-hooks):
      - Define required emission points with file patterns:
        ```python
        REQUIRED_EMISSION_HOOKS = [
            ('src/assistant/governance/verification_oracle.py', 'emit_episode'),
            ('src/api/v1/chat/approvals.py', 'emit_episode'),
            ('src/tasks/enrichment.py', 'emit_episode'),
        ]
        ```
      - For each (file, pattern):
        1. Read file content
        2. Search for pattern
        3. Report FOUND or MISSING
      - Return GREEN if all found, RED if any missing

   c) Import check:
      - Verify all graph modules import without error
      - Check: src.core.graphiti_client
      - Check: src.core.synthex_entities
      - Check: src.tasks.graphiti_sync
      - Check: src.assistant.governance.graph_oracle_adapter

   d) Contract test runner (--run-tests):
      - Execute: python -m pytest tests/core/test_graphiti_client_contract.py tests/tasks/test_graphiti_sync_contract.py tests/api/test_graph_oracle_adapter_contract.py -q
      - Return GREEN if all pass, RED if any fail

   e) Combined gate (default):
      - Run emission hook check
      - Run import check
      - Run contract tests (if not --dry-run)
      - Print summary with GREEN/RED verdict
      - Exit 0 for GREEN, exit 1 for RED

   f) CLI arguments:
      - --check-emission-hooks: Only run emission hook check
      - --run-tests: Only run contract tests
      - --dry-run: Skip tests, only static checks
      - --verbose: Show detailed output

2. Make script executable with shebang: #!/usr/bin/env python

Script output format:
```
[Graph Gate] Emission Hook Check...
  src/assistant/governance/verification_oracle.py: FOUND
  src/api/v1/chat/approvals.py: FOUND
  src/tasks/enrichment.py: FOUND

[Graph Gate] Import Check...
  src.core.graphiti_client: OK
  src.core.synthex_entities: OK
  src.tasks.graphiti_sync: OK
  src.assistant.governance.graph_oracle_adapter: OK

[Graph Gate] Contract Tests...
  3 passed, 0 failed

[Graph Gate] VERDICT: GREEN
```
  </action>
  <verify>
python scripts/governance/graph_gate.py --check-emission-hooks --dry-run
python scripts/governance/graph_gate.py --help
  </verify>
  <done>
Governance gate script provides binary GREEN/RED output with emission hook validation and test execution.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create contract tests for graph client and entities</name>
  <files>
    tests/core/test_graphiti_client_contract.py
    tests/core/test_synthex_entities.py
  </files>
  <action>
1. Create tests/core/test_graphiti_client_contract.py:

   a) Test client singleton behavior:
      - test_get_graphiti_client_returns_none_when_disabled
        - Set GRAPH_ORACLE_ENABLED=false
        - Assert get_graphiti_client() returns None

      - test_check_graph_availability_returns_false_when_disabled
        - Set GRAPH_ORACLE_ENABLED=false
        - Assert check_graph_availability() returns False

   b) Test fail-open behavior:
      - test_query_with_fallback_returns_fallback_on_timeout
        - Mock async query that hangs
        - Assert returns fallback value
        - Assert does not raise

      - test_query_with_fallback_returns_fallback_on_error
        - Mock async query that raises
        - Assert returns fallback value
        - Assert logs warning

   c) Test environment configuration:
      - test_client_reads_neo4j_uri_from_env
      - test_client_reads_neo4j_credentials_from_env

   Use pytest fixtures to mock environment variables.
   Do NOT require live Neo4j - all tests use mocks.

2. Create tests/core/test_synthex_entities.py:

   a) Test entity contracts:
      - test_oracle_decision_entity_has_required_fields
      - test_failure_pattern_entity_has_required_fields
      - test_enrichment_outcome_entity_has_required_fields

   b) Test edge contracts:
      - test_was_verified_by_edge_has_required_fields
      - test_approved_by_user_edge_has_required_fields

   c) Test episode payload helper:
      - test_create_episode_payload_adds_required_fields
      - test_create_episode_payload_uses_provided_created_at
      - test_create_episode_payload_generates_created_at_if_missing

   d) Test episode type enum:
      - test_episode_type_enum_has_expected_values

All tests use Pydantic validation - no external dependencies.
  </action>
  <verify>
python -m pytest tests/core/test_graphiti_client_contract.py -q --tb=short
python -m pytest tests/core/test_synthex_entities.py -q --tb=short
  </verify>
  <done>
Contract tests verify client singleton, fail-open behavior, and entity/edge contracts without live Neo4j.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create contract tests for emission tasks and oracle adapter</name>
  <files>
    tests/tasks/test_graphiti_sync_contract.py
    tests/api/test_graph_oracle_adapter_contract.py
  </files>
  <action>
1. Create tests/tasks/test_graphiti_sync_contract.py:

   a) Test emit_episode task:
      - test_emit_episode_returns_early_when_graph_disabled
        - Mock GRAPH_ORACLE_ENABLED=false
        - Call emit_episode.apply()
        - Assert completes without error
        - Assert no graph client interaction

      - test_emit_episode_validates_episode_payload
        - Call with invalid payload (missing required fields)
        - Assert logs validation error
        - Assert does not retry validation errors

      - test_emit_episode_retries_transient_errors
        - Mock transient connection error
        - Assert task configured for retry
        - Assert max_retries=2

   b) Test sync_failure_journey task:
      - test_sync_failure_journey_handles_missing_file
        - Assert completes without error when FAILURE_JOURNEY.md missing

      - test_sync_failure_journey_parses_failure_entries
        - Create mock FAILURE_JOURNEY.md content
        - Assert extracts failure entries

   Use Celery task testing patterns (task.apply() for sync execution).

2. Create tests/api/test_graph_oracle_adapter_contract.py:

   a) Test fail-open behavior:
      - test_query_evidence_returns_fail_open_when_graph_unavailable
        - Mock check_graph_availability() returns False
        - Assert returns FAIL_OPEN_SIGNAL
        - Assert decision='pass'
        - Assert confidence=0.5

      - test_query_evidence_returns_fail_open_on_timeout
        - Mock query that exceeds timeout
        - Assert returns FAIL_OPEN_SIGNAL

      - test_query_evidence_returns_fail_open_on_error
        - Mock query that raises
        - Assert returns FAIL_OPEN_SIGNAL
        - Assert logs warning

   b) Test oracle signal contract:
      - test_oracle_signal_has_required_fields
        - Assert OracleSignal has: decision, confidence, reason_codes, evidence_refs

      - test_query_evidence_returns_oracle_signal
        - Mock successful query
        - Assert returns OracleSignal instance

   c) Test decision logic:
      - test_no_prior_failures_yields_pass_decision
      - test_prior_failures_yields_review_decision
      - test_critical_warnings_yields_fail_decision

All tests mock graph client - no live Neo4j required.
  </action>
  <verify>
python -m pytest tests/tasks/test_graphiti_sync_contract.py -q --tb=short
python -m pytest tests/api/test_graph_oracle_adapter_contract.py -q --tb=short
  </verify>
  <done>
Contract tests verify emission task behavior and oracle adapter fail-open logic without live Neo4j.
  </done>
</task>

</tasks>

<verification>
1. Governance gate script executes: `python scripts/governance/graph_gate.py --dry-run`
2. All contract tests pass: `python -m pytest tests/core/test_graphiti_client_contract.py tests/core/test_synthex_entities.py tests/tasks/test_graphiti_sync_contract.py tests/api/test_graph_oracle_adapter_contract.py -q`
3. Emission hook check passes: `python scripts/governance/graph_gate.py --check-emission-hooks`
</verification>

<success_criteria>
- scripts/governance/graph_gate.py returns binary GREEN/RED verdict
- Contract tests pass without live Neo4j dependency
- Static emission hook checks validate all required points
- All tests discoverable by pytest
</success_criteria>

<output>
After completion, create `.planning/phases/13.2-oracle-framework-reuse/13.2-04-SUMMARY.md`
</output>
