---
phase: 13.2-oracle-framework-reuse
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - .env.example
  - requirements.txt
  - src/core/graphiti_client.py
  - src/core/synthex_entities.py
autonomous: true
user_setup:
  - service: neo4j
    why: "Graph database for temporal knowledge storage"
    env_vars:
      - name: NEO4J_PASSWORD
        source: "Set a secure password in .env (not default 'changeit')"
    dashboard_config: []

must_haves:
  truths:
    - "Neo4j service starts and responds to health checks within 60 seconds"
    - "Graphiti client connects to Neo4j and performs basic availability check"
    - "Entity and edge contracts are importable and usable"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Neo4j service definition with health check"
      contains: "neo4j:"
    - path: "src/core/graphiti_client.py"
      provides: "Singleton graph client with fail-open behavior"
      exports: ["get_graphiti_client", "check_graph_availability"]
    - path: "src/core/synthex_entities.py"
      provides: "Entity and edge type contracts"
      exports: ["OracleDecisionEntity", "FailurePatternEntity", "WasVerifiedByEdge"]
  key_links:
    - from: "src/core/graphiti_client.py"
      to: "neo4j service"
      via: "NEO4J_URI environment variable"
      pattern: "os\\.environ.*NEO4J_URI"
---

<objective>
Deploy Neo4j runtime, integrate graphiti-core client, and define entity/edge contracts.

Purpose: Establish graph substrate for temporal knowledge storage that Phase 14/15 will consume.
Output: Running Neo4j service, configured Graphiti client, and typed entity contracts.
</objective>

<execution_context>
@C:/Users/Hp/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Hp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@claudesplan.md
@src/assistant/governance/verification_oracle.py
@src/core/enrichment/oracle_contract.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Neo4j service to Docker Compose and update environment</name>
  <files>
    docker-compose.yml
    .env.example
    requirements.txt
  </files>
  <action>
1. Add neo4j service to docker-compose.yml:
   - Image: neo4j:5.26
   - Environment: NEO4J_AUTH=neo4j/${NEO4J_PASSWORD}, memory settings (heap 1G, pagecache 512M)
   - Ports: 7474:7474 (browser), 7687:7687 (bolt)
   - Volumes: neo4j_data:/data, neo4j_logs:/logs
   - Healthcheck: curl -sf http://localhost:7474, interval 30s, timeout 10s, retries 5, start_period 60s
   - Restart: unless-stopped

2. Add volume definitions at bottom of docker-compose.yml:
   - neo4j_data
   - neo4j_logs

3. Add to backend service environment:
   - NEO4J_URI=${NEO4J_URI}
   - NEO4J_USER=${NEO4J_USER}
   - NEO4J_PASSWORD=${NEO4J_PASSWORD}
   - GRAPH_ORACLE_ENABLED=${GRAPH_ORACLE_ENABLED:-false}

4. Add to celery_worker service environment (same as backend).

5. Update .env.example with:
   - NEO4J_URI=bolt://neo4j:7687
   - NEO4J_USER=neo4j
   - NEO4J_PASSWORD=changeit
   - GRAPH_ORACLE_ENABLED=true

6. Add to requirements.txt (exact pins):
   - graphiti-core==0.26.0
   - neo4j==5.26.0

Do NOT use >= or ~= version specifiers. Exact pins only per project governance.
  </action>
  <verify>
docker compose config --quiet && echo "Compose config valid"
grep -q "neo4j:" docker-compose.yml && echo "Neo4j service present"
grep -q "graphiti-core==0.26.0" requirements.txt && echo "graphiti-core pinned"
grep -q "neo4j==5.26.0" requirements.txt && echo "neo4j driver pinned"
  </verify>
  <done>
Docker Compose parses without error, Neo4j service is defined with health check, dependencies pinned in requirements.txt, .env.example updated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Graphiti client singleton with fail-open behavior</name>
  <files>
    src/core/graphiti_client.py
    src/core/__init__.py
  </files>
  <action>
1. Create src/core/graphiti_client.py with:

   a) Import safety - wrap graphiti imports in try/except for environments without package installed.

   b) Environment variables:
      - NEO4J_URI (default: bolt://localhost:7687)
      - NEO4J_USER (default: neo4j)
      - NEO4J_PASSWORD (required when GRAPH_ORACLE_ENABLED=true)
      - GRAPH_ORACLE_ENABLED (default: false)

   c) Singleton pattern for client:
      - _graphiti_client: Optional[Graphiti] = None
      - get_graphiti_client() -> Optional[Graphiti]
      - Returns None if GRAPH_ORACLE_ENABLED=false or import failed

   d) Availability check with timeout:
      - check_graph_availability(timeout_seconds: float = 2.0) -> bool
      - Must complete within timeout
      - Returns False on any error (fail-open)

   e) Fail-open helper for Oracle queries:
      - query_with_fallback(query_fn: Callable, fallback_value: T, timeout: float = 2.0) -> T
      - Wraps async query in timeout
      - Returns fallback_value on timeout or error
      - Logs warning but does not raise

   f) Type hints throughout, docstrings for public functions.

2. Update src/core/__init__.py to export:
   - get_graphiti_client
   - check_graph_availability
   - query_with_fallback

Note: Use asyncio timeout patterns compatible with Celery (no asyncio.run in request handlers).
Async graph calls should be fire-and-forget via Celery task dispatch.
  </action>
  <verify>
python -c "from src.core.graphiti_client import get_graphiti_client, check_graph_availability; print('Imports OK')"
python -c "from src.core import get_graphiti_client; print('Re-export OK')"
  </verify>
  <done>
Graphiti client module exists with singleton, availability check, and fail-open query helper. All exports work when GRAPH_ORACLE_ENABLED=false.
  </done>
</task>

<task type="auto">
  <name>Task 3: Define entity and edge type contracts</name>
  <files>
    src/core/synthex_entities.py
    src/core/__init__.py
  </files>
  <action>
1. Create src/core/synthex_entities.py with Pydantic models for Graphiti entities:

   a) Base entity fields (all entities inherit):
      - entity_type: str (discriminator)
      - store_id: str
      - created_at: datetime (UTC)
      - correlation_id: Optional[str]

   b) Entity families (as per blueprint):
      - OracleDecisionEntity: decision, confidence, reason_codes, evidence_refs, requires_user_action, source_adapter
      - FailurePatternEntity: failure_type, module_path, error_signature, occurrence_count, last_seen_at
      - ModuleEntity: module_name, module_path, health_status, last_check_at
      - EnrichmentOutcomeEntity: product_id, profile_gear, fields_modified, quality_delta, oracle_arbitration_used
      - UserApprovalEntity: action_id, action_type, approval_decision, user_id, approved_at

   c) Edge families:
      - WasVerifiedByEdge: from_entity_id, to_entity_id, edge_type, verified_at, verification_outcome
      - HasFailureWarningEdge: from_entity_id, to_entity_id, edge_type, warning_level, warning_message
      - YieldedOutcomeEdge: from_entity_id, to_entity_id, edge_type, outcome_type, outcome_data
      - ApprovedByUserEdge: from_entity_id, to_entity_id, edge_type, user_id, approved_at

   d) Episode type enum for taxonomy:
      - EpisodeType(str, Enum): oracle_decision, failure_pattern, enrichment_outcome, user_approval, vendor_catalog_change

   e) Helper function:
      - create_episode_payload(episode_type: EpisodeType, store_id: str, **kwargs) -> dict
      - Ensures required base fields are present
      - Adds created_at if not provided

2. Update src/core/__init__.py to export entity classes.

All models use Pydantic v2 patterns (model_validator, field_validator as needed).
  </action>
  <verify>
python -c "from src.core.synthex_entities import OracleDecisionEntity, EpisodeType, create_episode_payload; print('Entity imports OK')"
python -c "from src.core.synthex_entities import WasVerifiedByEdge, HasFailureWarningEdge; print('Edge imports OK')"
python -m pytest tests/core/test_synthex_entities.py -q --tb=short 2>/dev/null || echo "Tests will be added in 13.2-04"
  </verify>
  <done>
Entity and edge contracts defined with Pydantic v2 models. EpisodeType enum covers taxonomy. Helper function creates compliant episode payloads.
  </done>
</task>

</tasks>

<verification>
1. Docker Compose validates: `docker compose config --quiet`
2. Graphiti client imports without error when GRAPH_ORACLE_ENABLED=false
3. Entity contracts are importable and serialize to JSON
4. No syntax errors in new files: `python -m py_compile src/core/graphiti_client.py src/core/synthex_entities.py`
</verification>

<success_criteria>
- Neo4j service definition exists in docker-compose.yml with health check
- Graphiti client singleton with fail-open behavior implemented
- Entity/edge contracts defined and importable
- Dependencies pinned in requirements.txt
- Environment variables documented in .env.example
</success_criteria>

<output>
After completion, create `.planning/phases/13.2-oracle-framework-reuse/13.2-01-SUMMARY.md`
</output>
