---
phase: 15.1-sentry-autonomous-intake-and-verified-auto-resolution
plan: 02
type: execute
wave: 1
depends_on: ["15.1-01"]
files_modified:
  - scripts/daemons/health_monitor.py
  - scripts/graph/sentry_auto_heal.py
  - tests/graph/test_sentry_integration.py
autonomous: true

must_haves:
  truths:
    - "Health monitor consumes normalized issue metadata from plan 01 output."
    - "Auto-heal triggers at most once per issue fingerprint per monitor cycle."
    - "Health cache records auto_heal_running with issue_id and timestamp."
    - "Issue routing emits canonical sentry_issue_routed events."
  artifacts:
    - path: "scripts/daemons/health_monitor.py"
      provides: "Issue-aware trigger control and telemetry"
    - path: "scripts/graph/sentry_auto_heal.py"
      provides: "Auto-heal entrypoint receiving issue metadata"
---

<objective>
Route each issue into remediation exactly once per cycle with auditable telemetry.
</objective>

<context>
@.planning/phases/15.1-sentry-autonomous-intake-and-verified-auto-resolution/15.1-01-PLAN.md
@.planning/phases/15.1-sentry-autonomous-intake-and-verified-auto-resolution/15.1-01-SUMMARY.md
@scripts/observability/sentry_issue_puller.py
@scripts/daemons/health_monitor.py
@scripts/graph/sentry_auto_heal.py
</context>

<tasks>

<task type="auto">
  <name>Add deduped issue routing in health monitor</name>
  <files>scripts/daemons/health_monitor.py</files>
  <action>
1. Read issue_id and fingerprint from plan 01 normalized payload.
2. Guard duplicate trigger attempts in the active cycle.
3. Persist routing metadata in `.graph/health-cache.json`.
  </action>
  <verify>python -m pytest tests/graph/test_sentry_integration.py -q</verify>
  <done>Duplicate auto-heal spawning is prevented for same issue in one cycle.</done>
</task>

<task type="auto">
  <name>Emit routing telemetry and canonical events</name>
  <files>scripts/daemons/health_monitor.py, scripts/graph/sentry_auto_heal.py</files>
  <action>
1. Emit `sentry_issue_routed` with issue scope and routing status.
2. Include context telemetry keys used by Phase 16 compatibility gates.
3. Keep telemetry compact and deterministic.
  </action>
  <verify>python -c "from scripts.daemons.health_monitor import HealthMonitor; print('ok')"</verify>
  <done>Routed issues are visible in memory stream and health cache with issue-level traceability.</done>
</task>

</tasks>

<verification>
1. Integration tests prove single-fire behavior per issue fingerprint.
2. Health cache includes issue metadata and auto_heal_running state.
3. Routed events are emitted once per trigger decision.
</verification>

<success_criteria>
- Monitor-trigger path is deterministic and non-duplicative for active issues.
</success_criteria>

<output>
After completion, create `.planning/phases/15.1-sentry-autonomous-intake-and-verified-auto-resolution/15.1-02-SUMMARY.md`.
</output>
