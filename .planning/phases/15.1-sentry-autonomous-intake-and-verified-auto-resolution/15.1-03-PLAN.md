---
phase: 15.1-sentry-autonomous-intake-and-verified-auto-resolution
plan: 03
type: execute
wave: 2
depends_on: ["15.1-02"]
files_modified:
  - src/core/sentry_client.py
  - src/graph/sentry_feedback_loop.py
  - tests/graph/test_sentry_feedback.py
autonomous: true

must_haves:
  truths:
    - "Production verification path requires a real SENTRY_AUTH_TOKEN."
    - "A fix is verified only when issue status is resolved and no newer activity exists."
    - "Recurrence keeps issue open and records failure evidence."
    - "Verification outcome emits canonical sentry_issue_verified event."
  artifacts:
    - path: "src/core/sentry_client.py"
      provides: "Authenticated Sentry API calls with strict fallback behavior"
    - path: "src/graph/sentry_feedback_loop.py"
      provides: "Resolution verification and recurrence handling"
---

<objective>
Stop false-positive closure by enforcing real verification against Sentry issue state and activity.
</objective>

<context>
@.planning/phases/15.1-sentry-autonomous-intake-and-verified-auto-resolution/15.1-02-PLAN.md
@.planning/phases/15.1-sentry-autonomous-intake-and-verified-auto-resolution/15.1-02-SUMMARY.md
@src/core/sentry_client.py
@src/graph/sentry_feedback_loop.py
@FAILURE_JOURNEY.md
</context>

<tasks>

<task type="auto">
  <name>Enforce token-gated live Sentry verification</name>
  <files>src/core/sentry_client.py</files>
  <action>
1. Remove production-path mock resolved behavior when token is absent.
2. Return explicit pending/error state for missing token or transport failure.
3. Keep test-mode fallback isolated behind explicit test controls only.
  </action>
  <verify>python -m pytest tests/unit/test_sentry_client_base_url.py -q</verify>
  <done>Client behavior cannot silently claim resolved without live validation.</done>
</task>

<task type="auto">
  <name>Harden feedback loop verification and recurrence logging</name>
  <files>src/graph/sentry_feedback_loop.py, tests/graph/test_sentry_feedback.py</files>
  <action>
1. Verify `status == resolved`.
2. Verify no activity newer than remediation completion timestamp.
3. On recurrence, keep unresolved and append evidence to `FAILURE_JOURNEY.md`.
4. Emit `sentry_issue_verified` canonical event with outcome metadata.
  </action>
  <verify>python -m pytest tests/graph/test_sentry_feedback.py -q</verify>
  <done>Closure promotion occurs only for verified non-recurring fixes.</done>
</task>

</tasks>

<verification>
1. Missing-token path remains pending and does not promote closure.
2. Recurrence path logs evidence and blocks resolution.
3. Verified path emits final lifecycle event and promotion metadata.
</verification>

<success_criteria>
- False resolved states are eliminated on the production Sentry path.
</success_criteria>

<output>
After completion, create `.planning/phases/15.1-sentry-autonomous-intake-and-verified-auto-resolution/15.1-03-SUMMARY.md`.
</output>
