---
phase: 15.1-sentry-autonomous-intake-and-verified-auto-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/observability/sentry_issue_puller.py
  - tests/unit/test_sentry_issue_puller_resolution.py
autonomous: true

must_haves:
  truths:
    - "Unresolved issue intake uses Link-header pagination until next cursor is exhausted."
    - "Each pulled issue is normalized with issue_id, level, error_type, affected_module, timestamp."
    - "Each normalized issue emits one canonical memory event: sentry_issue_pulled."
    - "Batch pull completion triggers incremental memory materialization."
  artifacts:
    - path: "scripts/observability/sentry_issue_puller.py"
      provides: "Cursor-safe intake and normalized issue payloads"
    - path: "tests/unit/test_sentry_issue_puller_resolution.py"
      provides: "Puller normalization and pagination coverage"
---

<objective>
Make intake deterministic, normalized, and memory-visible so downstream plans can route issues safely.
</objective>

<context>
@.planning/phases/15.1-sentry-autonomous-intake-and-verified-auto-resolution/15.1-CONTEXT.md
@.planning/phases/15.1-sentry-autonomous-intake-and-verified-auto-resolution/15.1-RESEARCH.md
@src/memory/event_schema.py
@src/memory/event_log.py
@scripts/memory/materialize_views.py
</context>

<tasks>

<task type="auto">
  <name>Harden intake pagination and normalization</name>
  <files>scripts/observability/sentry_issue_puller.py</files>
  <action>
1. Parse Link headers and continue while next results exist.
2. Normalize issue payload fields needed by remediation and telemetry.
3. Keep health output compact: status, events_processed, cursor_state, error.
  </action>
  <verify>python -m pytest tests/unit/test_sentry_issue_puller_resolution.py -q</verify>
  <done>Puller output is stable and complete for issue routing.</done>
</task>

<task type="auto">
  <name>Emit canonical issue-pulled events</name>
  <files>scripts/observability/sentry_issue_puller.py</files>
  <action>
1. Emit one event per normalized issue via create_event + append_event.
2. Use event type `sentry_issue_pulled` with phase and issue scope metadata.
3. Trigger `scripts/memory/materialize_views.py --mode incremental` once per successful batch.
  </action>
  <verify>python scripts/observability/sentry_issue_puller.py --manual</verify>
  <done>Pulled issue lifecycle is visible in canonical memory stream.</done>
</task>

</tasks>

<verification>
1. Unit tests for pagination and normalization pass.
2. Pull batch writes canonical events.
3. Incremental materializer runs after successful batch completion.
</verification>

<success_criteria>
- Intake output is sufficient for deduped routing and closure validation in later plans.
</success_criteria>

<output>
After completion, create `.planning/phases/15.1-sentry-autonomous-intake-and-verified-auto-resolution/15.1-01-SUMMARY.md`.
</output>
