---
phase: 12-tier-system-architecture
plan: 01
type: execute
wave: 1
depends_on: []
requirements:
  - TIER-01
  - TIER-02
  - TIER-06
  - TIER-07
files_modified:
  - src/models/assistant_tool_registry.py
  - src/models/assistant_tenant_tool_policy.py
  - src/models/assistant_profile.py
  - src/models/assistant_memory_fact.py
  - src/models/assistant_memory_embedding.py
  - src/models/assistant_route_event.py
  - src/models/__init__.py
  - migrations/versions/<new>_phase12_routing_registry_foundation.py
  - src/assistant/policy_resolver.py
  - src/assistant/tool_projection.py
  - src/assistant/memory_retrieval.py
  - src/api/v1/chat/routes.py
  - src/api/v1/chat/schemas.py
  - tests/api/test_chat_routing_contract.py
  - tests/api/test_tool_projection_contract.py
  - tests/api/test_assistant_profile_contract.py
  - tests/api/test_chat_memory_retrieval_contract.py
  - tests/api/test_tenant_rls_readiness_contract.py
autonomous: true

must_haves:
  truths:
    - "Routing authority is backend policy evaluation, not model self-selection."
    - "Each request yields deterministic route decision + confidence + explainability payload."
    - "Effective toolset is policy-filtered by tier, RBAC, tenant policy, and active integrations."
    - "User/team assistant profiles and enabled skill sets are persisted and enforced."
    - "Tools unavailable by policy or integration state are never exposed to runtime prompts."
    - "Cross-chat memory retrieval is tenant/RBAC-scoped and provenance-aware."
    - "Routing events are auditable and tenant-scoped."
  artifacts:
    - path: "src/assistant/policy_resolver.py"
      provides: "Canonical route decision and effective toolset contract"
      contains: "resolve_route_decision"
    - path: "src/assistant/tool_projection.py"
      provides: "Capability projection by tier + tenant + integration state"
      contains: "effective_toolset"
    - path: "tests/api/test_chat_routing_contract.py"
      provides: "Deterministic routing contract verification"
      contains: "route_decision + reasons + confidence"
    - path: "src/models/assistant_profile.py"
      provides: "User/team profile policy contract for enabled skill sets"
      contains: "enabled_skill_set"
    - path: "src/assistant/memory_retrieval.py"
      provides: "Scoped memory retrieval contract"
      contains: "POST /api/v1/chat/memory/retrieve"
  key_links:
    - from: "src/api/v1/chat/routes.py"
      to: "src/assistant/policy_resolver.py"
      via: "route and capability projection call path"
      pattern: "POST /api/v1/chat/route"
---

<objective>
Build the routing and capability foundation for Phase 12.

Purpose: introduce deterministic tier decisioning and policy-filtered tool visibility before runtime execution changes.
Output: route contract + effective tool projection + auditable routing events.
</objective>

<execution_context>
@codexclaude/get-shit-done/workflows/execute-plan.md
@codexclaude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-tier-system-architecture/12-CONTEXT.md
@.planning/phases/12-tier-system-architecture/12-RESEARCH.md
@src/models/user.py
@src/api/core/rate_limit.py
@src/api/v1/chat/routes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tool/profile registry and tenant policy persistence contracts</name>
  <files>
    - src/models/assistant_tool_registry.py
    - src/models/assistant_tenant_tool_policy.py
    - src/models/assistant_profile.py
    - src/models/assistant_memory_fact.py
    - src/models/assistant_memory_embedding.py
    - src/models/assistant_route_event.py
    - src/models/__init__.py
    - migrations/versions/<new>_phase12_routing_registry_foundation.py
  </files>
  <action>
Add persistence models for:
- canonical tool metadata (risk class, mutate/read flags, required integration, allowed tiers)
- tenant policy overlays (allowlist/denylist, role constraints, activation flags)
- user/team assistant profiles (enabled skills, profile-level policy toggles)
- memory fact store + embedding linkage (`assistant_memory_fact`, `assistant_memory_embedding`)
- routing event logs (`route_decision`, `confidence`, `policy_snapshot_hash`, `effective_toolset_hash`)

Apply tenant-scoped constraints and indexes needed for lookup speed and audit retrieval.
  </action>
  <verify>
- `python -m pytest -q tests/api/test_tool_projection_contract.py`
- `python -m pytest -q tests/api/test_assistant_profile_contract.py`
  </verify>
  <done>
- Tool governance state can be persisted and queried deterministically.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement policy resolver and effective toolset projection</name>
  <files>
    - src/assistant/policy_resolver.py
    - src/assistant/tool_projection.py
    - src/assistant/memory_retrieval.py
    - src/api/v1/chat/schemas.py
    - src/api/v1/chat/routes.py
  </files>
  <action>
Implement resolver contract:
- input: user, tenant, tier, intent, integration health, RBAC context
- output: route decision, confidence, reasons, effective toolset, approval mode, fallback stage

Projection rules:
- backend authority only
- deny-by-default policy
- capability = tier entitlement intersect tenant policy intersect integration readiness intersect RBAC

Expose route endpoint for deterministic decisioning (`POST /api/v1/chat/route`).
Add memory retrieval endpoint (`POST /api/v1/chat/memory/retrieve`) with:
- tenant + RBAC filtering
- top-k retrieval controls
- provenance/freshness metadata in response
  </action>
  <verify>
- `python -m pytest -q tests/api/test_chat_routing_contract.py`
- `python -m pytest -q tests/api/test_tool_projection_contract.py`
- `python -m pytest -q tests/api/test_chat_memory_retrieval_contract.py`
  </verify>
  <done>
- Tier routing and tool projection contracts are executable and machine-verifiable.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic routing contract tests</name>
  <files>
    - tests/api/test_chat_routing_contract.py
    - tests/api/test_tool_projection_contract.py
    - tests/api/test_assistant_profile_contract.py
    - tests/api/test_chat_memory_retrieval_contract.py
    - tests/api/test_tenant_rls_readiness_contract.py
  </files>
  <action>
Add tests covering:
- low-confidence fallback to Tier 1 with explicit escalation metadata
- no unauthorized tools in projected toolset
- integration-disconnected capability stripping
- tenant policy denylist precedence
- route event fields include stable explainability metadata
- user/team profile enabled-skill enforcement (TIER-07 explicit)
- memory retrieval is scoped by tenant/RBAC and returns provenance markers
- RLS-readiness contract checks for tenant-bound query enforcement points
  </action>
  <verify>
- `python -m pytest -q tests/api/test_chat_routing_contract.py`
- `python -m pytest -q tests/api/test_tool_projection_contract.py`
- `python -m pytest -q tests/api/test_assistant_profile_contract.py`
- `python -m pytest -q tests/api/test_chat_memory_retrieval_contract.py`
- `python -m pytest -q tests/api/test_tenant_rls_readiness_contract.py`
  </verify>
  <done>
- Wave-1 routing foundation is covered by deterministic contract tests.
  </done>
</task>

</tasks>

<verification>
- [ ] Route decision endpoint returns deterministic tier selection + explainability payload
- [ ] Effective toolset excludes unauthorized/disconnected tools
- [ ] Tenant policy overlays are applied server-side
- [ ] User/team profile enabled-skill policy is enforced by contract
- [ ] Memory retrieval endpoint is tenant/RBAC-scoped and provenance-aware
- [ ] Routing decisions are auditable with stable event fields
- [ ] Contract tests cover fallback and policy precedence paths
</verification>

<success_criteria>
- Phase 12 wave-1 capability is live: policy-resolved tier routing and permission-filtered tool projection.
</success_criteria>

<output>
After completion, create `.planning/phases/12-tier-system-architecture/12-01-SUMMARY.md`
and governance evidence:
- `reports/12/12-01/self-check.md`
- `reports/12/12-01/review.md`
- `reports/12/12-01/structure-audit.md`
- `reports/12/12-01/integrity-audit.md`
</output>
