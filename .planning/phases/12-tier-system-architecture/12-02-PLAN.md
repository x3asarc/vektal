---
phase: 12-tier-system-architecture
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
requirements:
  - TIER-03
  - TIER-04
  - TIER-08
files_modified:
  - src/api/v1/chat/orchestrator.py
  - src/api/v1/chat/routes.py
  - src/api/v1/chat/approvals.py
  - src/api/v1/chat/schemas.py
  - src/models/chat_action.py
  - src/assistant/runtime_tier1.py
  - src/assistant/runtime_tier2.py
  - frontend/src/features/chat/components/ChatWorkspace.tsx
  - frontend/src/features/chat/components/ActionCard.tsx
  - frontend/src/features/chat/hooks/useChatSession.ts
  - frontend/src/shared/contracts/chat.ts
  - tests/api/test_chat_tier_runtime_contract.py
  - tests/api/test_fallback_stage_telemetry_contract.py
  - frontend/src/features/chat/components/ChatWorkspace.test.tsx
autonomous: true

must_haves:
  truths:
    - "Tier 1 supports low-risk read-safe interaction without privileged mutation execution."
    - "Tier 2 executes governed skills and enforces semantic firewall (read auto, write approval-gated)."
    - "Write paths always remain dry-run first and product-scope approval first."
    - "Low-confidence routing presents explicit escalation UX and preserves continuity."
    - "Tier transition behavior is deterministic and auditable."
  artifacts:
    - path: "src/assistant/runtime_tier1.py"
      provides: "Read-safe runtime behavior and escalation handoff metadata"
      contains: "tier1_response"
    - path: "src/assistant/runtime_tier2.py"
      provides: "Tier 2 semantic firewall execution"
      contains: "read_auto_write_gate"
    - path: "tests/api/test_chat_tier_runtime_contract.py"
      provides: "Tier runtime and approval-path contract tests"
      contains: "dry_run_required + approval_required"
  key_links:
    - from: "src/api/v1/chat/routes.py"
      to: "src/api/v1/chat/approvals.py"
      via: "approved write execution path"
      pattern: "approve/apply"
---

<objective>
Implement Tier 1 and Tier 2 runtime behavior with explicit transition rules and semantic firewall policy.

Purpose: convert routing outputs into safe runtime behavior without bypassing existing dry-run and approval gates.
Output: stable Tier 1/Tier 2 execution semantics across backend and chat UI.
</objective>

<execution_context>
@codexclaude/get-shit-done/workflows/execute-plan.md
@codexclaude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-tier-system-architecture/12-CONTEXT.md
@.planning/phases/12-tier-system-architecture/12-RESEARCH.md
@src/api/v1/chat/orchestrator.py
@src/api/v1/chat/approvals.py
@src/resolution/policy.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce explicit Tier 1 and Tier 2 runtime handlers</name>
  <files>
    - src/assistant/runtime_tier1.py
    - src/assistant/runtime_tier2.py
    - src/api/v1/chat/orchestrator.py
    - src/api/v1/chat/routes.py
  </files>
  <action>
Implement runtime translation from route decision to execution behavior:
- Tier 1: read-safe response blocks only, with optional escalation card metadata
- Tier 2:
  - READ tools auto-execute
  - WRITE tools require dry-run token and approval gating

Ensure route fallback metadata is propagated into response payloads for explainability.
Persist fallback-stage transitions in action/runtime metadata for downstream traceability.
  </action>
  <verify>
- `python -m pytest -q tests/api/test_chat_tier_runtime_contract.py`
- `python -m pytest -q tests/api/test_fallback_stage_telemetry_contract.py`
  </verify>
  <done>
- Tier runtime handlers execute according to locked policy boundaries.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enforce semantic firewall in approval/apply path</name>
  <files>
    - src/api/v1/chat/approvals.py
    - src/api/v1/chat/schemas.py
    - src/models/chat_action.py
    - src/api/v1/chat/routes.py
  </files>
  <action>
Harden approval semantics:
- READ actions cannot enter mutation apply flow.
- WRITE actions must include dry-run reference and approved status before apply.
- Product-scope approval remains canonical approval unit.
- Preserve existing preflight and conflict routing behavior from Phase 8/11.

Add machine-readable errors for invalid tier/runtime action transitions.
  </action>
  <verify>
- `python -m pytest -q tests/api/test_chat_tier_runtime_contract.py`
- `python -m pytest -q tests/api/test_fallback_stage_telemetry_contract.py`
  </verify>
  <done>
- Semantic firewall is enforced with deterministic, test-covered behavior.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Tier transition and escalation UX contracts</name>
  <files>
    - frontend/src/features/chat/components/ChatWorkspace.tsx
    - frontend/src/features/chat/components/ActionCard.tsx
    - frontend/src/features/chat/hooks/useChatSession.ts
    - frontend/src/shared/contracts/chat.ts
    - frontend/src/features/chat/components/ChatWorkspace.test.tsx
  </files>
  <action>
Add UI support for:
- safe-tier fallback notice
- explicit escalation CTA for Tier 2/3 paths
- clear state badges for `read_safe`, `awaiting_approval`, `approved`, and `applying`
- continuity behavior when route changes between turns

Keep response rendering deterministic via typed block contracts.
  </action>
  <verify>
- `cd frontend && npm.cmd run test -- "src/features/chat/components/ChatWorkspace.test.tsx"`
- `cd frontend && npm.cmd run typecheck`
  </verify>
  <done>
- Tier transition UX is explicit, safe, and test-backed.
  </done>
</task>

</tasks>

<verification>
- [ ] Tier 1 runtime is read-safe and non-privileged
- [ ] Tier 2 runtime enforces semantic firewall for write actions
- [ ] Write actions require dry-run and product-scope approval before apply
- [ ] Low-confidence fallback behavior is explicit in UX payload and UI
- [ ] Fallback-stage telemetry is emitted with stable event semantics
- [ ] Tier transition contracts are covered by backend/frontend tests
</verification>

<success_criteria>
- Phase 12 wave-2 capability is live: Tier 1/Tier 2 runtime semantics enforce safe execution boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/12-tier-system-architecture/12-02-SUMMARY.md`
and governance evidence:
- `reports/12/12-02/self-check.md`
- `reports/12/12-02/review.md`
- `reports/12/12-02/structure-audit.md`
- `reports/12/12-02/integrity-audit.md`
</output>
