---
phase: 07-frontend-framework-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/tsconfig.json
  - frontend/eslint.config.mjs
  - frontend/next.config.ts
  - frontend/src/app/layout.tsx
  - frontend/src/app/page.tsx
  - frontend/src/app/(app)/dashboard/page.tsx
  - frontend/src/app/(app)/onboarding/page.tsx
  - frontend/src/app/(app)/jobs/[id]/page.tsx
  - frontend/src/app/(app)/chat/page.tsx
  - frontend/src/app/(app)/settings/page.tsx
  - frontend/src/app/(auth)/auth/login/page.tsx
  - frontend/src/app/(auth)/auth/verify/page.tsx
  - frontend/src/lib/api/client.ts
  - frontend/src/lib/api/problem-details.ts
  - frontend/src/lib/query/keys.ts
  - frontend/src/lib/auth/guards.ts
  - frontend/src/shell/providers.tsx
  - frontend/src/shared/contracts/index.ts
autonomous: true

must_haves:
  truths:
    - "Frontend runs on Next.js App Router with TypeScript strict mode and reproducible runtime baseline"
    - "Guard decision utility enforces deterministic A/V/S precedence with route exemptions and same-origin returnTo safety"
    - "API boundary normalizes RFC 7807/9457 responses into one typed frontend error contract"
    - "React Query key factories enforce [resource, scope, ...rest] shape with scope in segment 2"
    - "Shell/features/shared seam structure exists with route ownership skeleton for dashboard/onboarding/jobs/chat/settings/auth"
  artifacts:
    - path: "frontend/src/lib/auth/guards.ts"
      provides: "Pure deterministic route guard + returnTo sanitizer"
      contains: "getRedirectForRoute"
    - path: "frontend/src/lib/query/keys.ts"
      provides: "Scope-aware query key factories"
      contains: "scope"
    - path: "frontend/src/lib/api/problem-details.ts"
      provides: "RFC problem-details normalization for UI consumption"
      contains: "fieldErrors"
    - path: "frontend/src/app/(app)/dashboard/page.tsx"
      provides: "Canonical dashboard route ownership in App Router"
      contains: "dashboard"
  key_links:
    - from: "frontend/src/lib/auth/guards.ts"
      to: "frontend/src/app/layout.tsx"
      via: "server-side redirect decision before protected route render"
      pattern: "getRedirectForRoute"
    - from: "frontend/src/lib/query/keys.ts"
      to: "frontend/src/lib/api/client.ts"
      via: "typed query/mutation key usage"
      pattern: "scope"
---

<objective>
Execute the Phase 7 foundation plan for framework, contracts, and route ownership.

Purpose: establish low-risk primitives before onboarding and realtime UI integration.
Output: Next.js 16 App Router baseline, strict TS/lint gates, API/query/guard contracts, and route skeleton.
Assumptions: Node.js >= 20.9 is available in CI/runtime and backend `/api/v1/*` contracts remain stable.
</objective>

<execution_context>
@codexclaude/get-shit-done/workflows/execute-plan.md
@codexclaude/get-shit-done/templates/summary.md
@codexclaude/get-shit-done/references/sequential-thinking-framework.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-frontend-framework-setup/07-CONTEXT.md
@.planning/phases/07-frontend-framework-setup/07-RESEARCH.md
@frontend/package.json
@frontend/pages/index.js
@src/api/v1/jobs/routes.py
@src/api/jobs/events.py
@src/api/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade frontend baseline to Next 16 App Router + strict TS/lint gates</name>
  <files>
    - frontend/package.json
    - frontend/tsconfig.json
    - frontend/eslint.config.mjs
    - frontend/next.config.ts
  </files>
  <action>
Migrate `frontend/` from Next 14 Pages Router placeholder to Next 16 App Router baseline with React 19 and TypeScript strict mode.
Add reproducible scripts (`dev`, `build`, `start`, `lint`, `typecheck`, `test`) and enforce lint gates:
- disallow `@ts-ignore`
- disallow `any` in `src/features/**`
- fail on unused imports/vars
Prefer `@ts-expect-error` only in boundary modules with ticket id and removal condition comments.
  </action>
  <verify>
- `cd frontend && npm run lint`
- `cd frontend && npm run typecheck`
- `cd frontend && npm run build`
  </verify>
  <done>
- FRONTEND-01 baseline complete
- strict TS/lint policy enforceable in CI
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement contract primitives for guards, API errors, and scoped query keys</name>
  <files>
    - frontend/src/lib/auth/guards.ts
    - frontend/src/lib/api/client.ts
    - frontend/src/lib/api/problem-details.ts
    - frontend/src/lib/query/keys.ts
    - frontend/src/shared/contracts/index.ts
  </files>
  <action>
Create a deterministic guard utility implementing Phase 7 A/V/S precedence and exemptions:
1. `!A` -> `/auth/login?returnTo=...`
2. `A && !V` -> `/auth/verify?returnTo=...`
3. `A && V && !S` on S-required routes -> `/onboarding?returnTo=...`
Enforce same-origin `returnTo` sanitization and loop protection.
Implement API boundary normalization for RFC 7807/9457 compatibility (`errors` map and `violations` list) into one `fieldErrors` contract.
Create query key factories with locked shape `[resource, scope, ...rest]` where segment 2 is scope.
  </action>
  <verify>
- `cd frontend && npm run test -- src/lib/auth/guards.test.ts`
- `cd frontend && npm run test -- src/lib/api/problem-details.test.ts`
- `cd frontend && npm run test -- src/lib/query/keys.test.ts`
  </verify>
  <done>
- FRONTEND-02 contract baseline complete
- guard and error behavior are test-backed and deterministic
  </done>
</task>

<task type="auto">
  <name>Task 3: Create App Router route ownership skeleton and shell/provider seams</name>
  <files>
    - frontend/src/app/layout.tsx
    - frontend/src/app/page.tsx
    - frontend/src/app/(app)/dashboard/page.tsx
    - frontend/src/app/(app)/onboarding/page.tsx
    - frontend/src/app/(app)/jobs/[id]/page.tsx
    - frontend/src/app/(app)/chat/page.tsx
    - frontend/src/app/(app)/settings/page.tsx
    - frontend/src/app/(auth)/auth/login/page.tsx
    - frontend/src/app/(auth)/auth/verify/page.tsx
    - frontend/src/shell/providers.tsx
  </files>
  <action>
Create canonical route skeleton for onboarding, dashboard, jobs detail, chat, settings, login, and verify routes.
Establish `shell/`, `features/`, and `shared/` boundaries and provider composition without runtime module federation.
Keep placeholders minimal but wired through shared contracts so follow-up plans can integrate behavior without route reshaping.
Rejected branch for this plan: keep Pages Router during onboarding implementation. Reason: higher migration debt and duplicate routing logic.
  </action>
  <verify>
- `cd frontend && npm run build`
- `cd frontend && npm run test -- src/app/routing.contract.test.ts`
  </verify>
  <done>
- FRONTEND-03 route ownership foundation complete
- App Router cutover is in place with low blast radius for next plans
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm run lint` passes
- [ ] `cd frontend && npm run typecheck` passes
- [ ] `cd frontend && npm run build` passes
- [ ] Guard tests prove deterministic precedence and loop protection
- [ ] Query key tests prove scope object is always segment 2
- [ ] RFC problem-details normalizer handles both `errors` and `violations` shapes
- [ ] Legacy backend endpoints remain unchanged (frontend-only blast radius)
- [ ] Rollback path documented: revert App Router landing changes while preserving new contracts
</verification>

<success_criteria>
- All tasks completed
- FRONTEND-01/02/03 foundation is executable and test-backed
- No routing/auth regressions introduced in baseline shell
</success_criteria>

<output>
After completion, create `.planning/phases/07-frontend-framework-setup/07-01-SUMMARY.md`
</output>
