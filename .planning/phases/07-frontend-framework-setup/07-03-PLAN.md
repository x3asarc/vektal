---
phase: 07-frontend-framework-setup
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - frontend/src/state/ui-prefs-store.ts
  - frontend/src/state/drafts-store.ts
  - frontend/src/features/jobs/observer/job-observer.ts
  - frontend/src/features/jobs/observer/transport-ladder.ts
  - frontend/src/features/jobs/components/GlobalJobTracker.tsx
  - frontend/src/features/jobs/components/JobTerminalNotifications.tsx
  - frontend/src/features/jobs/hooks/useJobRehydrate.ts
  - frontend/src/features/jobs/hooks/useJobDetailObserver.ts
  - frontend/src/features/jobs/hooks/useJobListStateFromUrl.ts
  - frontend/src/shared/errors/error-presenter.ts
  - frontend/src/features/manifest.ts
  - frontend/eslint.config.mjs
  - .planning/phases/07-frontend-framework-setup/07-MODULE-BOUNDARY-MAP.md
autonomous: true

must_haves:
  truths:
    - "State ownership boundaries are enforced: server state in query cache, global UI in Zustand-style stores, local interactions in component state"
    - "Draft and preference persistence follows Phase 7 policy (sessionStorage drafts with TTL/version and localStorage UI prefs)"
    - "Global job tracker rehydrates active jobs on mount/focus/reconnect/refresh and treats backend as source of truth"
    - "Job observation uses SSE primary with inactivity probe fallback to polling and clear degradation signaling rules"
    - "Terminal event policy is implemented: success/cancel transient, error sticky, burst collapse summary support"
    - "Module federation preparedness remains contract-only with static manifests and enforced shell/features/shared import boundaries"
  artifacts:
    - path: "frontend/src/features/jobs/observer/transport-ladder.ts"
      provides: "SSE to polling fallback controller with inactivity probe"
      contains: "inactivityThresholdMs"
    - path: "frontend/src/features/jobs/components/GlobalJobTracker.tsx"
      provides: "Layout-level tracker with rehydrate and ghost transition handling"
      contains: "rehydrate"
    - path: "frontend/src/state/drafts-store.ts"
      provides: "Session draft persistence with TTL and versioned keys"
      contains: "ttlMs"
    - path: ".planning/phases/07-frontend-framework-setup/07-MODULE-BOUNDARY-MAP.md"
      provides: "Ownership/import direction map for shell/features/shared seams"
      contains: "no cross-feature imports"
  key_links:
    - from: "frontend/src/features/jobs/hooks/useJobRehydrate.ts"
      to: "frontend/src/lib/api/client.ts"
      via: "backend re-derivation of active job state"
      pattern: "jobs"
    - from: "frontend/src/features/manifest.ts"
      to: ".planning/phases/07-frontend-framework-setup/07-MODULE-BOUNDARY-MAP.md"
      via: "static feature ownership contracts"
      pattern: "requiredState"
---

<objective>
Execute Phase 7 hardening plan for persistence boundaries, async job reliability, and module-boundary preparedness.

Purpose: close the highest-risk runtime failure modes (state drift, transport silence, boundary erosion) before Phase 8 dependency work.
Output: resilient state/recovery layer, transport ladder behavior, terminal notification policy, and module-boundary enforcement artifacts.
Assumptions: Plan 07-02 UI routes/shell are stable and the backend job APIs remain source-of-truth endpoints.
</objective>

<execution_context>
@codexclaude/get-shit-done/workflows/execute-plan.md
@codexclaude/get-shit-done/templates/summary.md
@codexclaude/get-shit-done/references/sequential-thinking-framework.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-frontend-framework-setup/07-CONTEXT.md
@.planning/phases/07-frontend-framework-setup/07-RESEARCH.md
@.planning/phases/07-frontend-framework-setup/07-02-SUMMARY.md
@src/api/v1/jobs/routes.py
@src/api/jobs/events.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Phase 7 persistence boundaries and state ownership contracts</name>
  <files>
    - frontend/src/state/ui-prefs-store.ts
    - frontend/src/state/drafts-store.ts
    - frontend/src/features/jobs/hooks/useJobListStateFromUrl.ts
  </files>
  <action>
Implement persistence policy exactly:
- UI preferences (example: sidebar collapse) in localStorage and device-specific
- non-sensitive unsubmitted drafts in sessionStorage with 2-hour TTL and versioned keys
- clear drafts on submit success, explicit reset/cancel, logout, and tab close
- list/search/filter/pagination state in URL query params
Implement explicit `Reset Workspace` action as clean-slate mechanism.
Reject alternative branch of broad localStorage persistence for all form state due to security and stale-data risk.
  </action>
  <verify>
- `cd frontend && npm run test -- src/state/drafts-store.test.ts`
- `cd frontend && npm run test -- src/state/ui-prefs-store.test.ts`
- `cd frontend && npm run test -- src/features/jobs/hooks/useJobListStateFromUrl.test.ts`
  </verify>
  <done>
- FRONTEND-07 persistence boundaries are implemented and tested
- sensitive draft persistence is prevented by design
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement global job tracker, transport ladder, and terminal-state UX policy</name>
  <files>
    - frontend/src/features/jobs/observer/job-observer.ts
    - frontend/src/features/jobs/observer/transport-ladder.ts
    - frontend/src/features/jobs/components/GlobalJobTracker.tsx
    - frontend/src/features/jobs/components/JobTerminalNotifications.tsx
    - frontend/src/features/jobs/hooks/useJobRehydrate.ts
    - frontend/src/features/jobs/hooks/useJobDetailObserver.ts
    - frontend/src/shared/errors/error-presenter.ts
  </files>
  <action>
Implement job observation policy:
- job lifecycle: idle -> submitting -> accepted -> in_progress -> terminal(success|error|cancelled)
- SSE primary transport, inactivity-probe triggers polling fallback
- degradation banner only when polling also fails
Implement global recovery:
- rehydrate active jobs on mount/focus/reconnect/refresh
- treat local registry as UI projection only; re-derive from backend
Implement terminal policy defaults:
- success transient ~10-15s
- cancelled transient ~5s
- error sticky until retry/dismiss
- collapse burst terminal events (3+ in ~10s) into summary notifications.
Ensure unknown field errors are surfaced at page level and logged diagnostically.
  </action>
  <verify>
- `cd frontend && npm run test -- src/features/jobs/observer/transport-ladder.test.ts`
- `cd frontend && npm run test -- src/features/jobs/hooks/useJobRehydrate.test.ts`
- `cd frontend && npm run test -- src/features/jobs/components/JobTerminalNotifications.test.ts`
  </verify>
  <done>
- Ack-first + recovery contracts are reliable under refresh/network degradation
- job state UX rules from 07-CONTEXT are implemented
  </done>
</task>

<task type="auto">
  <name>Task 3: Finalize Module Federation preparedness and boundary hardening artifacts</name>
  <files>
    - frontend/src/features/manifest.ts
    - frontend/eslint.config.mjs
    - .planning/phases/07-frontend-framework-setup/07-MODULE-BOUNDARY-MAP.md
  </files>
  <action>
Deliver FRONTEND-08 contract-only preparedness:
- keep runtime federation explicitly out of scope
- define static feature manifests with minimum widget contract (`id`, `requiredState`, render entry, optional metadata hints)
- enforce import direction rules (shell->shared/contracts, feature->shared/self only, shared->shared only, no cross-feature imports)
- document route/module ownership and seam contracts in module-boundary map artifact
Add lint rules to prevent deep-internal imports across seams.
  </action>
  <verify>
- `cd frontend && npm run lint`
- `cd frontend && npm run test -- src/features/manifest.contract.test.ts`
- `cd frontend && npm run build`
  </verify>
  <done>
- FRONTEND-08 preparedness is documented and lint-enforced
- phase exits with explicit seam governance and reduced coupling risk
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm run lint` passes with seam import rules active
- [ ] `cd frontend && npm run typecheck` passes
- [ ] job observer and rehydrate tests pass
- [ ] draft TTL/session persistence tests pass
- [ ] integration check confirms `/jobs/[id]` uses SSE then polling fallback on silence
- [ ] compatibility check confirms `/api/v1/jobs*` remains backend source of truth
- [ ] rollback path documented: disable tracker fallback/terminal policy flags and keep baseline routes operational
</verification>

<success_criteria>
- All tasks completed
- FRONTEND-07 and FRONTEND-08 contracts are delivered with tests and boundary documentation
- Phase 7 reliability policies are enforceable before Phase 8 starts
</success_criteria>

<output>
After completion, create `.planning/phases/07-frontend-framework-setup/07-03-SUMMARY.md`
</output>
