---
phase: 07-frontend-framework-setup
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - frontend/src/app/(app)/layout.tsx
  - frontend/src/app/(app)/dashboard/page.tsx
  - frontend/src/app/(app)/onboarding/page.tsx
  - frontend/src/app/(app)/settings/page.tsx
  - frontend/src/app/(auth)/auth/login/page.tsx
  - frontend/src/app/(auth)/auth/verify/page.tsx
  - frontend/src/features/onboarding/components/OnboardingWizard.tsx
  - frontend/src/features/onboarding/state/onboarding-machine.ts
  - frontend/src/features/onboarding/api/onboarding-mutations.ts
  - frontend/src/shell/components/Sidebar.tsx
  - frontend/src/shell/components/ChatSurface.tsx
  - frontend/src/shell/components/GlobalPendingIndicator.tsx
  - frontend/src/shell/components/NotificationStack.tsx
autonomous: false

must_haves:
  truths:
    - "3-step onboarding flow exists with Connect Shopify, ingest-path selection, and Preview/Start Import progression"
    - "Guard and landing rules follow Phase 7 contracts (onboarding-first pre-activation, dashboard default post-activation, settings requires A+V only)"
    - "Critical writes are ack-first with non-blocking global pending indicator and no optimistic updates for jobs/billing/oauth/auth"
    - "Responsive shell behavior matches contract: sidebar and chat regions adapt by breakpoint and mobile remains status-first"
    - "Dashboard route is actionable jobs-health surface, not decorative"
  artifacts:
    - path: "frontend/src/features/onboarding/components/OnboardingWizard.tsx"
      provides: "Progressive onboarding UI with 3-step state progression"
      contains: "Preview and Start Import"
    - path: "frontend/src/features/onboarding/state/onboarding-machine.ts"
      provides: "Onboarding state machine with explicit transition guards"
      contains: "connect_shopify"
    - path: "frontend/src/shell/components/GlobalPendingIndicator.tsx"
      provides: "Non-blocking pending feedback for ack-first writes"
      contains: "submitting"
    - path: "frontend/src/app/(app)/dashboard/page.tsx"
      provides: "Jobs-health hub surface with needs-attention and recovery actions"
      contains: "needs-attention"
  key_links:
    - from: "frontend/src/lib/auth/guards.ts"
      to: "frontend/src/app/(app)/layout.tsx"
      via: "guard enforcement for protected routes"
      pattern: "getRedirectForRoute"
    - from: "frontend/src/features/onboarding/api/onboarding-mutations.ts"
      to: "frontend/src/shell/components/GlobalPendingIndicator.tsx"
      via: "mutation lifecycle drives pending indicator state"
      pattern: "submitting"
---

<objective>
Execute Phase 7 integration plan for route behavior, onboarding UX, and responsive shell contracts.

Purpose: convert foundation primitives into user-visible flows without violating locked routing/write/recovery policies.
Output: guarded route integration, onboarding wizard, jobs-health dashboard surface, and responsive shell behavior.
Assumptions: Plan 07-01 contracts and route skeleton are complete and backend auth/oauth/jobs APIs remain compatible.
</objective>

<execution_context>
@codexclaude/get-shit-done/workflows/execute-plan.md
@codexclaude/get-shit-done/templates/summary.md
@codexclaude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-frontend-framework-setup/07-CONTEXT.md
@.planning/phases/07-frontend-framework-setup/07-RESEARCH.md
@.planning/phases/07-frontend-framework-setup/07-01-SUMMARY.md
@src/auth/login.py
@src/auth/oauth.py
@src/billing/checkout.py
@src/api/v1/jobs/routes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire route ownership, guard waterfall, and landing contracts</name>
  <files>
    - frontend/src/app/(app)/layout.tsx
    - frontend/src/app/(app)/dashboard/page.tsx
    - frontend/src/app/(app)/onboarding/page.tsx
    - frontend/src/app/(app)/settings/page.tsx
    - frontend/src/app/(auth)/auth/login/page.tsx
    - frontend/src/app/(auth)/auth/verify/page.tsx
  </files>
  <action>
Integrate server-side guard decisions into App Router layouts/pages using deterministic precedence from 07-CONTEXT.
Apply route exemptions exactly:
- `/auth/*` exempt from global A/V/S checks
- `/onboarding` exempt from S check only
- `/settings` requires A + V only
Implement landing logic:
- not store-connected -> `/onboarding`
- activated -> `/dashboard`
Keep same-origin `returnTo` safety and loop protection active on login/verify transitions.
  </action>
  <verify>
- `cd frontend && npm run test -- src/lib/auth/guards.test.ts`
- `cd frontend && npm run test -- src/app/routing.guard.integration.test.ts`
  </verify>
  <done>
- FRONTEND-03 route behavior contracts are operational
- redirect loop regressions are test-covered
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement 3-step onboarding wizard with ack-first write behavior</name>
  <files>
    - frontend/src/features/onboarding/components/OnboardingWizard.tsx
    - frontend/src/features/onboarding/state/onboarding-machine.ts
    - frontend/src/features/onboarding/api/onboarding-mutations.ts
    - frontend/src/shell/components/GlobalPendingIndicator.tsx
  </files>
  <action>
Build onboarding flow with locked steps:
1. Connect Shopify
2. Choose ingest path (Sync Store primary CTA, Upload CSV secondary)
3. Preview and Start Import with transition to import progress
Sync defaults to import everything; advanced scope/filter options are hidden by default and cannot silently change intent when opened.
All onboarding-critical writes must be ack-first with pending UI feedback.
Do not use optimistic updates for jobs, billing, oauth, or auth/session-changing actions.
  </action>
  <verify>
- `cd frontend && npm run test -- src/features/onboarding/state/onboarding-machine.test.ts`
- `cd frontend && npm run test -- src/features/onboarding/api/onboarding-mutations.test.ts`
  </verify>
  <done>
- FRONTEND-04 onboarding flow is implemented with explicit transitions and failure recovery
- ack-first contract is enforced on critical writes
  </done>
</task>

<task type="auto">
  <name>Task 3: Build responsive shell behavior and dashboard minimum contract</name>
  <files>
    - frontend/src/shell/components/Sidebar.tsx
    - frontend/src/shell/components/ChatSurface.tsx
    - frontend/src/shell/components/NotificationStack.tsx
    - frontend/src/app/(app)/dashboard/page.tsx
  </files>
  <action>
Implement shell IA/responsive contracts:
- sidebar off-canvas on `sm`, non-persistent on `md`, persistent on `lg`
- chat overlay on `sm/md`, docked on `lg`, canonical route `/chat`
- mobile status-first recovery controls, not desktop parity clone
Implement notification priority ordering:
global blocking banner > page banner > inline field > toast.
Ensure dashboard provides actionable jobs-health areas:
health summary, in-progress visibility, needs-attention queue, and fast recovery actions.
  </action>
  <verify>
- `cd frontend && npm run test -- src/shell/responsive-layout.test.ts`
- `cd frontend && npm run test -- src/app/dashboard.contract.test.ts`
- `cd frontend && npm run build`
  </verify>
  <done>
- FRONTEND-05 and FRONTEND-06 are met by contract tests and responsive behavior checks
- dashboard default surface is operational and actionable
  </done>
</task>

<task type="auto">
  <name>Task 4: Start dev server for human UI checkpoint</name>
  <files>frontend/package.json</files>
  <action>Run `npm run dev` in `frontend/`, keep server running, and confirm app is reachable before handoff.</action>
  <verify>`curl http://localhost:3000` returns HTTP 200</verify>
  <done>Human reviewer can verify visual/interaction behavior in browser without local setup friction.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 7 onboarding + shell integration build. Server running at http://localhost:3000</what-built>
  <how-to-verify>
1. Visit `/auth/login` and verify safe return behavior after login.
2. Verify onboarding 3-step progression and ingest-path choices on desktop and mobile widths.
3. Confirm critical write actions show non-blocking pending indicator and do not fake success.
4. Visit `/dashboard` and verify jobs-health sections are actionable.
5. Verify sidebar/chat region behavior across `sm`, `md`, and `lg`.
  </how-to-verify>
  <resume-signal>Type "approved" or list failing checks with route details.</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend && npm run lint` passes
- [ ] `cd frontend && npm run typecheck` passes
- [ ] Guard, onboarding, and responsive contract tests pass
- [ ] Human checkpoint approved
- [ ] Backward compatibility check: backend auth/oauth/billing/jobs endpoints require no contract change
- [ ] Rollback path documented: restore previous landing defaults and disable onboarding shell feature flags
</verification>

<success_criteria>
- All tasks completed
- FRONTEND-04/05/06 route and UX contracts are delivered
- No auth redirect loops or broken return flows introduced
</success_criteria>

<output>
After completion, create `.planning/phases/07-frontend-framework-setup/07-02-SUMMARY.md`
</output>
