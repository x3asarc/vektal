---
phase: 20-system-integrity-audit-remediation-waves
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/20-system-integrity-audit-remediation-waves/audit/src-core/
  - .planning/phases/20-system-integrity-audit-remediation-waves/audit/src/
  - .planning/phases/20-system-integrity-audit-remediation-waves/audit/src-api/
  - .planning/phases/20-system-integrity-audit-remediation-waves/audit/src-jobs/
  - .planning/phases/20-system-integrity-audit-remediation-waves/audit/src-graph/
autonomous: true

must_haves:
  truths:
    - "Core Python infrastructure has complete 8-surface audit coverage"
    - "All import chains and Celery tasks are documented"
    - "Cross-domain coupling between core modules is mapped"
  artifacts:
    - path: "audit/src-core/"
      contains: "8 JSON files (ownership, blast-radius, import-chain, data-access, api-surface, async-surface, config-surface, cross-domain)"
    - path: "audit/src/"
      contains: "8 JSON files"
    - path: "audit/src-api/"
      contains: "8 JSON files"
    - path: "audit/src-jobs/"
      contains: "8 JSON files"
    - path: "audit/src-graph/"
      contains: "8 JSON files"
  key_links:
    - from: "src/core"
      to: "src/jobs, src/api, src/graph, src/memory"
      pattern: "cross-domain imports"
    - from: "src/api"
      to: "src/core, src/jobs"
      pattern: "Flask route handlers"
---

<objective>
Audit 5 core Python infrastructure units (src/core/, src/, src/api/, src/jobs/, src/graph/) across all 8 surfaces. Creates 40 JSON files (5 units × 8 surfaces).

Purpose: Enable Agent SDK to understand the core backend architecture for intelligent routing and impact analysis.
Output: 40 JSON files in audit/ subdirectories
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-system-integrity-audit-remediation-waves/20-CONTEXT.md
@.planning/phases/20-system-integrity-audit-remediation-waves/20-RESEARCH.md
@.planning/phases/20-system-integrity-audit-remediation-waves/audit/universal_vendor_scraper/ownership.json
</context>

<tasks>

<task type="auto">
  <name>Audit src/core/ (8 surfaces)</name>
  <files>
    audit/src-core/ownership.json
    audit/src-core/blast-radius.json
    audit/src-core/import-chain.json
    audit/src-core/data-access.json
    audit/src-core/api-surface.json
    audit/src-core/async-surface.json
    audit/src-core/config-surface.json
    audit/src-core/cross-domain.json
  </files>
  <action>
    Read existing audit pattern from audit/universal_vendor_scraper/ownership.json for schema reference.

    For src/core/ audit each surface:
    1. ENUMERATE: glob src/core/**/*.py (exclude __pycache__)
    2. OWNERSHIP: git blame summary for files, count by author
    3. BLAST-RADIUS: For each .py file, trace imports depth-3 (direct + transitive). Collect all reachable modules.
    4. IMPORT-CHAIN: AST parse each .py file, extract outbound imports. Track cross-prefix imports.
    5. DATA-ACCESS: grep for SQLAlchemy model definitions, .query., .filter., session usage
    6. API-SURFACE: grep for @app.route, @bp.route decorators (note: src/core/ has no routes, document as N/A)
    7. ASYNC-SURFACE: grep for @app.task, @celery.task decorators
    8. CONFIG-SURFACE: grep for os.getenv, os.environ, app.config.get patterns
    9. CROSS-DOMAIN: Filter import-chain for imports going to src/, src/api/, src/jobs/, src/graph/, src/memory/, src/auth/, src/billing/

    Follow the exact JSON schema from existing audit files. Include coverage object with evidence_source, graph_backed, filesystem_backed, coverage_confidence, contract_satisfied.
  </action>
  <verify>
    ls audit/src-core/*.json | wc -l (must be 8)
    grep -l "folder_path" audit/src-core/*.json | wc -l (must be 8)
    grep -l "summary" audit/src-core/*.json | wc -l (must be 8)
  </verify>
  <done>
    All 8 JSON files exist in audit/src-core/ with valid schema
  </done>
</task>

<task type="auto">
  <name>Audit src/ root (8 surfaces)</name>
  <files>
    audit/src/ownership.json
    audit/src/blast-radius.json
    audit/src/import-chain.json
    audit/src/data-access.json
    audit/src/api-surface.json
    audit/src/async-surface.json
    audit/src/config-surface.json
    audit/src/cross-domain.json
  </files>
  <action>
    For src/ audit (root-level modules: app.py, celery_app.py, database.py, and subdirs: api/, jobs/, graph/, memory/, auth/, billing/, integrations/, etc.):

    Note: src/ as a unit means the ROOT src/ level plus its subdirectories that are separate units. Use glob src/**/*.py to enumerate all Python files, then categorize by which subdirectory they belong to.

    For this audit, focus on:
    1. Root-level .py files (app.py, celery_app.py, database.py, etc.)
    2. Files in src/api/, src/jobs/, src/graph/, src/memory/, src/auth/, src/billing/, src/integrations/ subdirectories

    For cross-domain analysis: Identify how these root modules and subdirectories couple to each other and to external units (src/core/, frontend/, etc.)
  </action>
  <verify>
    ls audit/src/*.json | wc -l (must be 8)
  </verify>
  <done>
    All 8 JSON files exist in audit/src/ with valid schema covering root src/ level
  </done>
</task>

<task type="auto">
  <name>Audit src/api/ (8 surfaces)</name>
  <files>
    audit/src-api/ownership.json
    audit/src-api/blast-radius.json
    audit/src-api/import-chain.json
    audit/src-api/data-access.json
    audit/src-api/api-surface.json
    audit/src-api/async-surface.json
    audit/src-api/config-surface.json
    audit/src-api/cross-domain.json
  </files>
  <action>
    For src/api/ (Flask REST API endpoints):

    1. ENUMERATE: glob src/api/**/*.py (app.py, core/, jobs/, v1/ subdirs)
    2. API-SURFACE: Extract all Flask routes - grep for @app.route, @bp.route. For each route: endpoint, methods, path, auth decorator
    3. Import src/app.py if possible to extract routes programmatically
    4. ASYNC-SURFACE: Check for Celery task dispatch within API handlers
    5. Cross-domain: How do API handlers call into src/core/, src/jobs/, src/graph/?
  </action>
  <verify>
    ls audit/src-api/*.json | wc -l (must be 8)
    grep -l "@app.route\|@bp.route" audit/src-api/api-surface.json
  </verify>
  <done>
    All 8 JSON files exist in audit/src-api/ with Flask routes documented
  </done>
</task>

<task type="auto">
  <name>Audit src/jobs/ (8 surfaces)</name>
  <files>
    audit/src-jobs/ownership.json
    audit/src-jobs/blast-radius.json
    audit/src-jobs/import-chain.json
    audit/src-jobs/data-access.json
    audit/src-jobs/api-surface.json
    audit/src-jobs/async-surface.json
    audit/src-jobs/config-surface.json
    audit/src-jobs/cross-domain.json
  </files>
  <action>
    For src/jobs/ (Celery orchestrator, dispatcher, queueing, finalizer, checkpoints, metrics):

    1. ENUMERATE: glob src/jobs/**/*.py
    2. ASYNC-SURFACE: Extract ALL Celery tasks - grep for @app.task, @celery.task. For each: task name, queue, retry policy
    3. Document orchestrator.py, dispatcher.py, queueing.py, finalizer.py, checkpoints.py, metrics.py, cancellation.py, graphiti_ingestor.py, progress.py
    4. Blast-radius: What happens if orchestrator.py changes? Trace to all tasks that depend on it
    5. Cross-domain: How do jobs couple to src/core/, src/graph/, src/memory/?
  </action>
  <verify>
    ls audit/src-jobs/*.json | wc -l (must be 8)
    grep -c "@app.task\|@celery.task" audit/src-jobs/async-surface.json (should be > 0)
  </verify>
  <done>
    All 8 JSON files exist in audit/src-jobs/ with Celery tasks documented
  </done>
</task>

<task type="auto">
  <name>Audit src/graph/ (8 surfaces)</name>
  <files>
    audit/src-graph/ownership.json
    audit/src-graph/blast-radius.json
    audit/src-graph/import-chain.json
    audit/src-graph/data-access.json
    audit/src-graph/api-surface.json
    audit/src-graph/async-surface.json
    audit/src-graph/config-surface.json
    audit/src-graph/cross-domain.json
  </files>
  <action>
    For src/graph/ (MCP server, sentry ingestion, sandbox, perf profiler, 40+ files):

    1. ENUMERATE: glob src/graph/**/*.py (44 files identified)
    2. API-SURFACE: Check for FastAPI/MCP endpoints in mcp_server.py, mcp_response_metadata.py
    3. ASYNC-SURFACE: Check for Graphiti sync tasks, sentry_feedback_loop tasks
    4. Cross-domain: How does graph couple to src/memory/, src/core/, src/jobs/?
    5. Notable files: mcp_server.py, sentry_ingestor.py, sandbox_*.py files, performance_profiler.py, root_cause_classifier.py
    6. Use .graph/local-snapshot.json as reference for cross-file relationships
  </action>
  <verify>
    ls audit/src-graph/*.json | wc -l (must be 8)
  </verify>
  <done>
    All 8 JSON files exist in audit/src-graph/ with MCP server and graph integration documented
  </done>
</task>

</tasks>

<verification>
- 5 units × 8 surfaces = 40 JSON files created
- Each file follows existing schema (folder_path, generated_at, coverage, summary)
- Cross-domain references between the 5 units are documented
- Celery tasks from src/jobs/ and src/ properly captured
- Flask routes from src/api/ properly captured
</verification>

<success_criteria>
- 40 JSON files exist in audit/src-core/, audit/src/, audit/src-api/, audit/src-jobs/, audit/src-graph/
- All files have valid JSON schema
- grep -r "matched_file_count" audit/src-*/ownership.json shows counts > 0
- Cross-domain shows coupling between these 5 units and other units
</success_criteria>

<output>
After completion, create .planning/phases/20-system-integrity-audit-remediation-waves/20-01-SUMMARY.md
</output>
