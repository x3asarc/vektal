# Phase 17.2 Summary

Status: GREEN  
Completed at: 2026-03-06

## Delivered Files

1. `src/models/shopify.py` (Extended ShopifyStore)
2. `src/models/job.py` (Added INGEST_SHOPIFY)
3. `src/jobs/finalizer.py` (Integrated metrics calculation)

## What Changed

1. **Ingest Watermarking:**
   - Added `last_full_ingest_at` and `last_shopify_cursor` to `ShopifyStore`.
   - This anchors the reconciliation poller in Wave 17.3.
2. **Job Lifecycle Extension:**
   - Added `INGEST_SHOPIFY` to `JobType` for bootstrap flows.
   - Updated unique constraint to prevent concurrent ingest jobs per store.
3. **Automated Metrics Generation:**
   - Integrated `_finalize_product_metrics` into `finalize_job`.
   - Ingest jobs now automatically recalculate `completeness_score` for all products in the catalog upon completion.
   - Uses `bulk_update_mappings` for performance efficiency.

## Verification Evidence

1. **Finalizer Integration:** `finalize_job` now correctly routes `INGEST_SHOPIFY` and `INGEST_CATALOG` to the metrics engine.
2. **Watermark Persistence:** Store watermarks are updated only on successful bootstrap completion.

## Handoff to 17.3

Use these concrete outputs:
1. `ShopifyStore.last_shopify_cursor` as the starting point for incremental reconciliation.
