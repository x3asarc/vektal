---
phase: 09-real-time-progress-tracking
plan: 01
type: execute
wave: 1
depends_on: []
requirements:
  - PROGRESS-01
  - PROGRESS-03
  - PROGRESS-05
  - PROGRESS-06
files_modified:
  - src/jobs/progress.py
  - src/api/jobs/schemas.py
  - src/api/jobs/events.py
  - src/api/v1/jobs/schemas.py
  - src/api/v1/jobs/routes.py
  - src/jobs/orchestrator.py
  - src/jobs/finalizer.py
  - src/jobs/cancellation.py
  - tests/jobs/test_progress_payload.py
  - tests/api/test_jobs_retry.py
  - tests/api/test_jobs_progress_contract.py
  - tests/api/test_jobs_stream_status_contract.py
autonomous: true

must_haves:
  truths:
    - "A single canonical progress payload is used by SSE, polling status, job list, and job detail endpoints."
    - "Lifecycle transitions (queue/start/chunk/cancel/finalize) emit progress broadcasts using canonical payload."
    - "Retry endpoint only accepts terminal failed/cancelled jobs and returns deterministic conflict behavior."
    - "Named SSE events continue to be emitted as `job_{id}` with payload shape parity to polling/status endpoints."
  artifacts:
    - path: "src/jobs/progress.py"
      provides: "Canonical progress payload builder and announcer entry-point"
      contains: "build_progress_payload"
    - path: "src/api/jobs/events.py"
      provides: "SSE + polling parity through shared payload"
      contains: "def get_job_status"
    - path: "src/api/v1/jobs/routes.py"
      provides: "Retry endpoint + enriched list/detail response contracts"
      contains: "def retry_job"
  key_links:
    - from: "src/jobs/orchestrator.py"
      to: "src/jobs/progress.py"
      via: "progress broadcasts after queue/chunk milestones"
      pattern: "announce_job_progress"
    - from: "src/jobs/finalizer.py"
      to: "src/jobs/progress.py"
      via: "terminal-state broadcasts"
      pattern: "announce_job_progress"
    - from: "src/api/v1/jobs/routes.py"
      to: "src/jobs/progress.py"
      via: "list/detail payload enrichment via canonical builder"
      pattern: "build_progress_payload"
---

<objective>
Implement backend real-time progress contracts and retry semantics from Phase 9 research.

Purpose: enforce transport parity and deterministic state/action behavior so frontend can render trustworthy live status.
</objective>

<execution_context>
@codexclaude/get-shit-done/workflows/execute-plan.md
@codexclaude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-real-time-progress-tracking/09-CONTEXT.md
@.planning/phases/09-real-time-progress-tracking/09-RESEARCH.md
@src/models/job.py
@src/jobs/orchestrator.py
@src/jobs/finalizer.py
@src/jobs/cancellation.py
@src/api/jobs/events.py
@src/api/v1/jobs/routes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canonicalize job progress payload and schema parity</name>
  <files>
    - src/jobs/progress.py
    - src/api/jobs/schemas.py
    - src/api/jobs/events.py
    - src/api/v1/jobs/schemas.py
    - src/api/v1/jobs/routes.py
  </files>
  <action>
Create and adopt a shared payload contract with required fields:
- counters and percent
- step semantics (`current_step`, `current_step_label`, `step_index`, `step_total`)
- ETA (`eta_seconds`)
- retry metadata (`can_retry`, `retry_url`)
- result navigation (`results_url`).

Use the same payload in:
- SSE initial/update events
- polling status endpoint
- jobs list response
- jobs detail response.

Contract must remain additive (no breakage to existing consumers).
Field expectations must be explicit and test-asserted:
- `current_step` / `current_step_label`
- `step_index` / `step_total`
- `eta_seconds`
- `can_retry`, `retry_url`, `results_url`.
  </action>
  <verify>
- `python -m pytest -q tests/api/test_jobs_progress_contract.py`
- `python -m pytest -q tests/api/test_jobs_stream_status_contract.py`
- `python -m pytest -q tests/api/test_endpoints.py`
  </verify>
  <done>
- Progress semantics no longer drift between transport and API responses.
  </done>
</task>

<task type="auto">
  <name>Task 2: Broadcast lifecycle transitions with deterministic step semantics</name>
  <files>
    - src/jobs/orchestrator.py
    - src/jobs/finalizer.py
    - src/jobs/cancellation.py
    - src/jobs/progress.py
    - tests/jobs/test_progress_payload.py
  </files>
  <action>
Emit progress updates at lifecycle transitions:
- queued/start ingest
- chunk completion updates
- cancellation requested/cancelled convergence
- terminal finalize states.

Implement conservative ETA behavior:
- nullable when insufficient signal
- non-negative integer when running with measurable throughput.

Ensure step inference is deterministic and consistent with Phase 9 UX language.
Include explicit handling for terminal status mapping:
- `failed_terminal` is retryable
- `completed` and active states are not retryable.
  </action>
  <verify>
- `python -m pytest -q tests/jobs/test_progress_payload.py`
- `python -m pytest -q tests/jobs/test_finalizer.py tests/jobs/test_cancellation.py`
  </verify>
  <done>
- Stream consumers receive meaningful, stable state transitions with compatible ETA behavior.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement guarded retry endpoint for terminal states</name>
  <files>
    - src/api/v1/jobs/routes.py
    - src/api/v1/jobs/schemas.py
    - tests/api/test_jobs_retry.py
  </files>
  <action>
Add `POST /api/v1/jobs/{job_id}/retry` with strict rules:
- allowed: `failed`, `failed_terminal`, `cancelled`
- denied: active + completed states with deterministic `409`.

Behavior:
- create new ingest job row
- persist lineage (`retry_of_job_id`)
- enqueue control task
- return `202` with stream URL.

Ownership and active-ingest uniqueness constraints must be preserved.
Retry responses must include:
- `retry_of_job_id`
- new `job_id`
- `stream_url`.
  </action>
  <verify>
- `python -m pytest -q tests/api/test_jobs_retry.py`
- `python -m pytest -q tests/jobs/test_non_blocking_api_flow.py`
  </verify>
  <done>
- Retry semantics are backend-enforced and safe under conflict conditions.
  </done>
</task>

</tasks>

<verification>
- [ ] Canonical payload fields match across stream/status/list/detail.
- [ ] Named transition states produce coherent step and ETA outputs.
- [ ] Retry endpoint accepts only terminal retryable states.
- [ ] Existing jobs API behavior remains stable for auth/ownership/contracts.
- [ ] `failed_terminal` retryability behavior is explicitly covered by tests.
- [ ] SSE named-event delivery format remains stable (`job_{id}`).
</verification>

<success_criteria>
- Backend satisfies PROGRESS-01, PROGRESS-03, PROGRESS-05, PROGRESS-06 with test-backed contracts.
- No contract drift remains between SSE, polling, and REST list/detail payloads.
</success_criteria>

<output>
After completion, create `.planning/phases/09-real-time-progress-tracking/09-01-SUMMARY.md`
</output>
