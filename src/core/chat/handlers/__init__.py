"""
Intent Handlers

Generate structured responses without LLM overhead.
Each handler processes intent entities and returns data directly.

Phase 5 integration: Handlers are called by ChatRouter, responses serialized to JSON
Phase 10 integration: React UI renders the structured responses
"""
